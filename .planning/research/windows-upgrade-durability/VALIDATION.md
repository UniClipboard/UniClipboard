# Windows 修复验证结果

## 代码

- Engine：`2a298a2c4fab4719e57b0ac53fe1080eaab08abb`，分支 `fix/alpha5-storage-upgrade`。
- Desktop：`8748b851af537b1cea0995311870c084c933bd59`，固定上述 Engine 提交。
- 新增 Windows CI 配置已按用户要求撤回，验证通过 SSH 在用户提供的 Windows 机器执行。

## Windows 原生结果

最小探针实际返回：只读 FlushFileBuffers 失败，系统错误码 5；可写方式成功，原字节不变。

修复前，存储升级内部测试为 9 项通过、5 项失败、1 项忽略；失败均包含拒绝访问和系统错误码 5。

修复后：

| 检查 | 通过数 | 说明 |
| --- | --- | --- |
| 普通文件刷新 | 6 | 字节保留、不创建缺失文件、拒绝目录、Windows 只读句柄、只读属性与共享冲突 |
| 存储升级内部行为 | 17 | 正文、blob、派生内容、旧表兼容和脱敏错误字段 |
| 控制库处理 | 2 | 控制代发布；读取者阻塞检查点时拒绝成功，释放后可完成 |
| 升级集成 | 17 | 新旧格式、安全材料、来源改变、激活及后续清理 |
| 独立进程中断恢复 | 1 | 一个测试实际执行五个退出位置，并确认重启后的历史与原库保留 |
| alpha.5 实际生成基线 | 1 | 从既有合成基线恢复后，真实运行期升级入口通过 |
| 用户数据库副本 | 1 | 使用私有临时副本执行真实数据库拆分，源文件摘要未变化 |
| 合计 | 45 | 未把零匹配测试或被忽略的辅助入口计入通过数 |

alpha.5 基线在 Windows tar 解压后出现了 macOS 元数据旁文件。验收前按原 manifest 清除这些非业务旁文件，并逐文件核对大小与摘要；业务输入字节不变。用户数据库副本不经过此处理。

用户数据仅进入机器私有临时目录，没有提交或上传到 GitHub；验证后已删除这次 Windows 临时输入。验证目录中的报告只含提交号、测试范围和退出码。

## 本机结果

Mac 上相关单元、升级集成与进程中断检查通过；全 workspace、全部目标编译检查通过；格式、仓库架构与日志隐私检查通过。

## 限制

- Windows 测试仍有三处既有未使用变量警告，不影响测试退出结果；没有扩展本次修复去清理无关代码。
- 原用户 Windows 凭据环境下的完整桌面安装和启动仍需实际验收。本次用户数据检查只证明数据库拆分和源文件保留。
- 未做物理断电、磁盘填满测试；不把进程退出模拟等同于断电测试。
- 测试包构建使用现有手动流程，不新增自动检查任务。
