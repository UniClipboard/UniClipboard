# 桌面主窗口设计规范

更新日期：2026-09-12。文字规范覆盖整个主窗口；设置页布局以用户确认的「通用设置」内容区为基准。

主窗口已接入第 5 节的统一文字样式。第 16 节保留改动前的扫描结果，并记录实现与验证范围；历史分布不代表当前界面仍在使用这些字号。

## 1. 适用范围与目标

文字规范适用于主窗口的历史记录、设备、全部九个设置页，以及首次设置、解锁、启动中、启动失败和这些页面打开的弹窗、菜单、提示、预览。九个设置页为通用设置、外观设置、快捷键、快捷面板、同步设置、安全与隐私、网络设置、存储管理、关于。

独立快捷面板窗口、独立更新窗口和开发预览不在本次页面范围内。设置中的「快捷面板」页仍在范围内。共享组件的主窗口用法必须遵循本规范；后续修改共享样式时也要检查其他窗口的影响。

第 5、16 节约束整个主窗口的文字；其余章节的布局、间距、表单结构要求仍只适用于设置页，不要求把历史记录和设备页改成设置表单。

设计目标是让人快速识别「这是哪一页、这一组管什么、这一项当前是什么、在哪里修改」。以清楚的文字层级、稳定的控件位置和克制的分隔线组织内容，不把设置页做成仪表盘或展示型落地页。

窗口框、交通灯、侧栏、返回入口、外层圆角容器、平台窗口差异保持现有实现。本规范调整的是它们内部的内容；不能为了统一设置项样式顺手替换这些外层布局。

界面调整不能改变设置保存路径、设备同步范围、密码处理、自动启动注册、重启要求或业务行为。隐私与加密约束继续服从 [VISION.md](VISION.md)。

## 2. 实现入口与所有权

| 入口 | 负责内容 | 禁止放入的内容 |
| --- | --- | --- |
| `src/pages/SettingsPage.tsx` | 选择当前类别，在共同入口插入一次页头 | 各页业务设置的保存条件 |
| `src/components/setting/SettingsPageHeader.tsx` | 页标题、页面说明与页头间距 | 某一页专用的按钮、卡片、统计值 |
| `src/layouts/SettingContentLayout.tsx` | 内容最大宽度、字距、组间距离 | 平台窗口框、侧栏布局 |
| `src/components/setting/SettingGroup.tsx` | 表单分组、legend、组内分隔线 | 配置读取、网络请求、模式判断 |
| `src/components/setting/SettingRow.tsx` | 标签、说明、附加标记、右侧控件位置 | 复制子控件状态或替它保存 |
| `src/components/ui/`、`src/components/motion/` | 输入、开关、菜单、弹层、键盘及焦点行为 | 设置页专属业务条件 |
| 各个设置页组件 | 内容顺序、具体值、修改动作和反馈 | 自行重新定义普通设置行的样式 |

普通设置项必须复用 `SettingGroup` / `SettingRow`。特殊控件可保留自己的内部结构，但外围使用同一字号、间距和语义颜色。禁止另外建立一套「新设置组件」，让新旧页面长期并行。

## 3. 尺寸的解释方式

以下像素换算均以根字号 16px 为阅读参照，不是硬编码要求。实际尺寸会受系统缩放、应用缩放和已有平台适配影响。实现使用 Tailwind 工具类、`rem`、比例或已有组件尺寸参数。

- 基本间距单位：`0.25rem`，名义 4px。
- 内容字距：`tracking-normal`，即 0。不要继承正文的额外字距，也不要在标题使用负字距。
- 不按窗口宽度缩放文字。窄窗口通过换行、控件移到下一行和滚动适配。
- 不为整行固定高度。行高由文字与控件中较高的一方决定。
- 1px 边框是细线定义，不是用固定像素搭建整页布局。

## 4. 内容层级与语义

```text
原有设置页外层容器
  内容区，最大宽度 48rem
    页面页头：h1 + 一句说明
    32px 组间距的内容流
      独立基础信息行（可选，例如设备名称）
      fieldset
        legend
        设置行
        细分隔线
        设置行
      fieldset
        legend
        设置行
```

### 4.1 页面页头

每页只能出现一个页面级 `h1`，文字与侧栏类别名称一致。页头由公共入口渲染，各页组件不得再复制一份。

说明是一句简短的范围介绍，不是使用教程，不堆叠所有能力，不重复标题。正常桌面宽度争取一行；较长翻译允许自然换行。

页头先于加载、错误或业务内容展示，不能因为数据尚未加载而让用户失去页面位置感。

### 4.2 设置分组

相关设置使用原生 `fieldset + legend`。标题必须是这个 fieldset 的 legend，不能只把普通 section 中的 h2 改成 legend。

- legend 是分组名称，例如「启动设置」「语言」「同步行为」「历史保留」。
- 不重复页面主标题，例如「同步设置」页中第一组叫「同步行为」。
- 分组不额外画外框，不加背景卡片、投影或标题下横线。
- 只有一个设置项时允许不显示额外组名，例如安全页的自动解锁，避免三层标题表达同一件事。
- 设备名称属于当前设备的基础信息，放在启动分组之前，不归类为启动行为。

### 4.3 设置行

设置名称是普通文字，不是 h2/h3/h4。全页到处出现粗体「小标题」会破坏层级，也会干扰辅助技术按标题导航。

左侧固定由「设置名称、可选说明」组成；实验标记、帮助入口等跟在名称旁。右侧是当前值或操作控件。

关于页的应用身份、赞助名单不是表单项，可以使用普通容器或带名称的 section；如果有内容标题，使用 h2，并采用分组标题的视觉规格。不要为了统一标签而把非表单内容也强行包成 fieldset。

## 5. 文字规格

### 5.1 唯一字号表

所有应用绘制的文字只使用以下四档。数值以根字号 16px、应用缩放 100% 为参照，实现使用 `rem`，不修改根字号来补偿局部布局。

| 统一角色 | 名义字号 / 实际定义 | 默认行高 | 默认字重 | 用途 |
| --- | --- | --- | --- | --- |
| 辅助 `caption` | 12px / `0.75rem` | 16px / `1rem` | 400 | 时间、数量、状态、标签、快捷键提示、设置项说明 |
| 正文 `body` | 14px / `0.875rem` | 20px / `1.25rem` | 400 | 正文、列表名称、输入值、按钮、菜单、导航、搜索、错误原因 |
| 分组 `section` | 16px / `1rem` | 24px / `1.5rem` | 600 | 分组标题、弹窗标题、预览内的内容标题、邀请码输入格数字 |
| 页面 `title` | 24px / `1.5rem` | 32px / `2rem` | 600 | 页面主标题、设备详情主名称、首次设置与解锁标题、展示用邀请码 |

四档沿用已确认的设置页 24/16/14/12 层级。正文保持 14，辅助信息最低 12，消除历史和设备区域的 9～11.5 小字；16 足以区分局部分组与正文，24 用于页面定位。取消 13、15、18、20、28、30、32、36 等独立档位，不用更多字号表达选中、强调或紧凑程度。

同一字号允许明确的字重分工：普通正文 400；按钮、导航选中项、列表主名称和辅助标记 500；标题 600。不能把整页标签全部加粗。颜色沿用默认前景、辅助前景和错误等语义色，不通过缩小字号弱化内容。

长说明有且只有一个舒展行高变体：正文 14/22.75、辅助 12/19.5，对应 1.625 倍行高。设置页说明和设置项说明沿用此变体。它必须由统一文字样式提供，不由页面单独选择行高数值。其余文字使用表内行高；禁止标题 `leading-none`、正文任意倍数行高和额外字距。字距统一为 0。

### 5.2 各处文字如何选档

| 页面 / 区域 | 页面或局部标题 | 主要文字 | 辅助文字 |
| --- | --- | --- | --- |
| 窗口内导航、侧栏、工具栏 | 不为统一字号新增大标题 | 导航项、搜索输入、筛选选项 14 | 数量、分组提示、快捷键 12 |
| 历史记录 | 已有页面标题 24；详情内容标题 16 | 文本摘要、代码摘要、链接名称、文件名、搜索建议 14 | 时间、来源、类型标签、传输进度、文件大小 12 |
| 剪贴板详情 | 应用提供的内容标题 16 | 纯文本、代码、文件列表、链接说明 14 | 元数据、行号、语言标记 12 |
| 设备列表与详情 | 详情主设备名 24；详情分组 16 | 列表设备名、设置名称、连接说明、可编辑值 14 | 在线状态、版本、标识、连接渠道、数量 12 |
| 九个设置页 | 共同页头 24；分组 16 | 设置名称、页面说明、控件值 14 | 设置项说明、状态、单位、修改标记 12 |
| 设置中的关于 / 存储 | 页头 24；应用名称、内容分组、容量主数值 16 | 赞助者名称、版本说明正文 14 | 版本号、统计图例、补充统计 12 |
| 首次设置、解锁 | 主标题 24；步骤内分组 16 | 引导正文、普通表单与操作 14；展示邀请码 24，邀请码输入格数字 16 | 次要说明、步骤状态 12 |
| 启动中、启动失败、页面空状态 | 全页标题 24；区域内空状态标题 16 | 状态说明、失败原因、重试操作 14 | 进度数值、补充信息 12 |
| 添加设备、信任确认、删除、反馈、更新等弹窗 | 标题统一 16，即使内容复杂也不放大 | 描述、后果、错误、普通输入和操作 14；展示邀请码 24，邀请码输入格数字 16 | 次要说明、状态 12 |
| 菜单、选项、悬浮提示、通知 | 通知标题 14/500；菜单分组标签 12/500 | 菜单项、选项、通知操作 14 | 菜单快捷键、工具提示、通知补充信息 12 |

邀请码需要跨设备辨认和抄录：展示值使用 `text-ui-title`（24/32），输入格数字由共享 `InputOTPSlot` 使用 `text-ui-section`（16/24）。这两种用途复用现有档位，保留等宽字体与分组分隔，不增加独立字号或字距；说明、倒计时和普通表单输入仍用正文档位。

重要错误、危险操作后果和必须阅读的说明属于正文，不能因为位于辅助区域而降成小字。紧凑按钮仍用正文 14；缩小按钮外边距不能连带缩小文字。图标按钮不显示文字时不需要字号。

图片、二维码、图标内部的图形不属于文字字号。设备头像中的实际字母、主题预览中的实际文字、代码行号、开发环境标记仍属于文字，最低 12。纯装饰缩略图可用线条表示排版，不能保留难以辨认的小字。

纯文本和代码预览遵守正文 14，等宽字体不意味着应更小。富文本原文、图片中的字属于用户内容，保留其内容表现；外围标题、工具栏、状态和元数据必须遵守本表。版本说明由应用排版，正文 14、内容标题 16，不继承第三方排版插件的独立字号表。

### 5.3 统一定义与使用约束

本节是字号决策的唯一规范；`src/styles/globals.css` 是实现数值的唯一来源。实现使用 `--font-size-caption/body/section/title`，其中 `title` 为 1.5rem；旧的 `small/body-lg` 已删除。统一语义样式为 `text-ui-caption`、`text-ui-body`、`text-ui-section`、`text-ui-title`，舒展变体为 `text-ui-caption-relaxed`、`text-ui-body-relaxed`。`src/lib/utils.ts` 将这些样式注册为同一字号组，保证组合样式时只保留一个文字角色，且不误删文字颜色。

- 页面只选择文字角色，或继承共享组件已经确定的角色。禁止自行写 `font-size`、内联 `fontSize`、任意 `text-[…]`，也禁止在业务页面直接写 `text-xs/sm/base/lg/xl/2xl` 等数值档位。
- 语义样式同时定义字号与行高；共享控件负责自己的角色。调用方不得通过 `className`、子元素选择器、响应式类或 `!important` 覆盖控件文字大小。
- CSS 变量、Tailwind 语义类与组件必须指向同一份值，不复制一套数字到 TypeScript 常量或页面 CSS。无需增加通用文字组件或新的配置层。
- 清除 `src/lib/window-ui.ts` 中 Windows 专属的局部字号覆盖。所有平台使用同一角色表；保留字体回退与已有整窗缩放功能，不增加页面缩放补偿。
- 普通输入框在窄窗口仍为正文 14，不保留当前 `text-base md:text-sm` 的宽度切换。缩放由现有原生窗口缩放入口统一负责，不使用 CSS `zoom` 或按窗口宽度改变字号。
- 全局字号变更须同步核查主窗口弹层和共享组件在其他窗口的使用。不能全局覆盖 Tailwind 的 `text-sm` 等基础档位来悄悄改变其他窗口。
- 需要新文字用途时，先归入现有四档。确实需要新增档位，必须先修改本表及适用场景，再修改统一定义；不能先在页面留下例外。

说明文字最大宽度为 `34rem`，名义 544px。名称和说明都允许折行；长路径、长单词不能把控件推到容器外。

不得通过全大写、额外字距、同级全加粗或放大所有行标签来「增强层级」。真正的层级由 24/16/14/12 的分工和留白共同表达。

## 6. 间距规格

| 位置 | 标准值 | 代码入口 |
| --- | --- | --- |
| 内容最大宽度 | `48rem` / 768px | `max-w-3xl` |
| 内容外侧内边距 | 保留现有：16px；sm 起 24px；lg 起 32px | SettingsPage 的 `p-4 sm:p-6 lg:p-8` |
| 标题与页面说明 | `0.25rem` / 4px | `mt-1` |
| 页头与后续内容 | `1.5rem` / 24px | 页头 `mb-6` |
| 两个设置组之间 | `2rem` / 32px | 内容流 `gap-8` |
| legend 左右内边距 | `0.25rem` / 4px | `px-1` |
| legend 与组内容盒 | `1rem` / 16px | `mb-4` |
| 普通设置行左右内边距 | `0.25rem` / 4px | `px-1` |
| 普通设置行上下内边距 | 各 `1rem` / 16px | `py-4` |
| 左文案与右控件的最小水平间距 | `2rem` / 32px | `gap-x-8` |
| 控件折到下一行时的垂直间距 | `0.75rem` / 12px | `gap-y-3` |
| 名称与说明 | `0.25rem` / 4px | 文案列 `gap-1` |
| 名称与帮助 / 标记 | `0.5rem` / 8px | 名称行 `gap-2` |
| 同一操作簇内按钮 | 通常 `0.5rem` / 8px；紧凑工具条按对应组件定义 | `gap-2` 等 |

### 6.1 不要重复计算间距

- `legend` 下已有 16px，第一行自身还有 16px 上内边距，因此从 legend 盒下缘到第一行内容盒通常是 32px。不要再额外补一层 `pt-4`。
- 最后一行有 16px 下内边距，组间再有 32px。不要给每个组同时加 `mb-8`。
- 一个页面如果用自己的外层 div 包住多个组，也必须用 `flex flex-col gap-8`，不能改用 `space-y-5/6` 形成另一套密度。
- 独立设备名称行可以 `pt-0`，因为它紧接页头；其余行按默认值执行。
- 复合编辑器内部可以更密，但它与其他组的关系仍遵循 32px。

### 6.2 行高计算

普通行不是固定高度：

```text
行外部高度 = max(左侧文案总高, 右侧控件总高) + 32px 上下内边距
文案总高 = 名称行高 +（有说明时：4px + 说明总行高）
```

例如名义 16px 根字号下，只有名称且右侧是 36px 选择框时，整行约 68px；名称加一行说明时约 75.5px。长说明允许自然增高，不能把文字裁切进预设高度。

## 7. 分隔线、表面与颜色

### 7.1 普通行分隔线

- 同组相邻行之间使用 `divide-y divide-border/25`。
- 线宽为 1px；颜色来自 `--border`，以 25% 强度混合。不是把整行透明度设为 25%。
- 第一行之前、最后一行之后不额外补线。
- legend 下不画线；组与组主要靠 32px 留白区分。
- 不给每个设置行添加完整边框、圆角背景或阴影。

### 7.2 控件边界

控件边框用于表达「这里可以操作」，因此比普通行分隔线清楚。使用现有 Input / Select / Button 的 `border-input`、`border-border`，不要把控件边框也统一淡到 25%。

表单输入、弹出菜单、提示和错误区域使用现有语义颜色。默认正文使用前景色，说明使用 muted foreground，错误使用 destructive；不能以降低整个父容器透明度替代这些颜色角色。

### 7.3 深色与例外色

- 浅色、深色都读取相同的语义变量，不另写一套背景灰度。
- 不给设置页增加大面积渐变、光斑或磨砂装饰。
- 关于页展示真实应用图标，不用渐变方块和自绘图标代替。
- 主题预览的选中边框、单选圆点和键盘焦点框统一跟随当前页面的 `--primary`，包含用户自定义强调色；不能固定为蓝色，也不从缩略图本身推断选中颜色。
- 主题缩略图的交通灯和赞助等级颜色属于明确的语义例外，不推广成普通设置行的装饰色。
- 外层 InsetSurface 的现有背景、边框、圆角不由本规范覆盖。

## 8. 控件尺寸与选型

| 需求 | 控件与尺寸 | 规则 |
| --- | --- | --- |
| 是 / 否 | 现有 `Switch` 默认尺寸 | 不改成两个文字按钮；保持开关本身的自然宽度 |
| 普通文本 | 现有 `Input`；默认高 2rem / 32px | 通常外包 `w-40` / 10rem；不为单页重写输入样式 |
| 普通选项 | 现有 `Select`；默认高 2.25rem / 36px | 通常宽 `w-40` / 10rem；快捷面板也使用标准高度 |
| 带单位数值 | 使用现有数值控件 | 可采用 `w-44` / 11rem，为数值、单位和校验预留空间 |
| 配色选择 | 色块 + 名称的 Select | 宽 12.5rem；选项展开后再显示完整预设集合 |
| 三种互斥模式 | 分段单选控件 | 宽上限 20rem，每项等分；保留真实 radio 语义 |
| 快捷键 | 快捷键显示 + 录入弹层 | 显示入口最小 5rem、最大 13rem；内部按组合键需要排布 |
| 普通命令 | 现有 Button | 默认高 2rem；紧凑 `sm` 高 1.75rem；不把所有按钮升为大号 |
| 图标工具 | Button 的 icon 系列 | 默认 2rem 方形；已有紧凑 1.5/1.75rem 档位按用途使用 |
| 界面缩放 | 减号 + 比例选择 + 加号 + 恢复 | 20rem 操作区，36px 图标按钮，12px 内部间隙 |

开关的现有默认轨道为 32 × 18.4 CSS px，拇指 1rem，位移由共享组件管理。这是存量组件事实，不授权其他页面照抄像素值另画一个开关。

普通输入、选择框与默认按钮使用已有 8px 圆角；小号按钮受现有 `--radius-md` 上限控制。重复的中继编辑器、赞助者条目可使用 8px 边框容器。普通分组不能变成卡片；不要把这些条目再装进第二层装饰卡片。

菜单使用当前 Base UI / 公共 motion 封装，不用原生 select 与另一套自制菜单混搭。长选项说明可把菜单扩展到 18rem，仍需 `max-width: calc(100vw - 2rem)`；关闭后的控件只显示简洁的当前值。

## 9. 文案与信息密度

设置名称尽量短，说明只回答该项对当前用户有什么影响。长解释不能默认占据一整段。

通用设置的既定例子：

- 「设备名称」：说明为「用于区分你的不同设备。」；不放在启动分组中。
- 「启动方式」：默认只显示当前模式的一句说明。展开后，正常、静默、轻量各有一句解释，方便比较。
- 「启动时恢复剪贴板」：说明为「将最近一条历史记录重新放回剪贴板。」；不要把后台就绪过程写成面向用户的操作说明。
- 「流畅模式」：默认只显示当前结果；具体判断原因通过信息按钮查看。

独立信息入口不能只有一个没有名称的图标。需要可见 tooltip、可访问名称和键盘可达性。帮助内容不能承载用户必须先知道的危险后果；删除、清理、退出等影响应在动作附近或确认窗口明确说明。

所有设置页标题与说明均有六种语言资源。不得直接显示翻译键，也不得把同一段说明分别硬编码到页头和内容组件。

## 10. 状态与交互

### 正常与修改

- 控件直接反映真实设置来源。没有保存失败时，不展示额外的「已保存」常驻标记。
- 普通开关/选择沿用当前即时或乐观保存方式；设备名称沿用失焦保存，不能因样式统一改成每次输入都保存。
- 保存状态属于发起操作的控件，不让一个无关设置行同时变暗。
- 改变主题或视觉效果不重挂载整个页面，避免丢失输入、滚动和焦点。

### 保存失败与重启

- 失败必须有可见反馈；沿用现有回滚、错误提示与重试约定，不能只写控制台。
- 涉及重启的设置沿用 `RestartBanner`，持续说明尚需重启；不能用短暂 toast 冒充已完全生效。
- 错误说明靠近对应行或对应分组，用 `role="alert"`；一般状态使用 `role="status"` 或现有 output。
- 危险动作继续使用明确确认，视觉统一不能删除确认或缩小提示信息。

### 加载、禁用与空状态

- 加载时保持控件或图形的尺寸，避免文字「加载中」导致按钮变宽。
- 已有加载图形应与真实统计布局同宽；加载、失败、零数据是三种不同状态。
- 禁用态不能只靠淡化颜色表达，继续使用真实 disabled / aria-disabled。
- 关闭动效后，加载、传输和错误仍需有文字、数值或静态图标反馈。

## 11. 焦点与键盘

- 页面 h1 用于页面导航；fieldset 的 legend 为分组命名；普通设置名称不是标题。
- 每个实际控件应有可访问名称。新增输入必须提供 label 关联或 aria-label；装饰图标使用 aria-hidden。
- 单选组使用方向键、空格；Select 保留键盘导航；录入快捷键、颜色输入等使用内容弹层，不套操作菜单的键盘模型。
- 沿用共享控件的 focus-visible 外圈。不得全局去掉 outline 后不补焦点反馈。
- 弹层关闭后焦点返回触发入口；Escape 不应让父页面先离开。
- 聚焦、悬停、选中不改变元素占位尺寸；边框加重采用预留边框或 outline。

## 12. 窄窗口与长内容

普通 SettingRow 使用 flex-wrap。文案列基准为 14rem，可增长/缩小；控件列不随便压扁，空间不够时移到下一行，保持右对齐，与文案间隔 12px。

所有行、分组、控件容器使用 `min-w-0`，控件容器限制 `max-w-full`。长描述自然换行；只有路径、快捷键展示等受控内容可以截断，并保留完整值的查看方式。

外观页的宽单选组是特殊变体：

- 常规布局左侧文案 + 最大 20rem 控件。
- 内容容器不超过 34rem 时改成单列；同时保留小视口回退规则。
- 模式说明与控件一起落在第二列；单列时回到第一列。
- 主题预览仍为三个等分选项，缩略图保持 1.9 宽高比，单项上限 11rem；窄内容降低预览间隙并允许选项文案换行。

窄窗口允许内容垂直滚动，不允许整个设置内容横向滚动。应用外层的窗口最小尺寸、侧栏行为继续采用原有设计，不能用这份规范擅自更改。

## 13. 特殊页面的推广规则

| 页面 | 普通结构 | 保留的特殊内容 |
| --- | --- | --- |
| 通用 | 独立设备名称 + 启动、语言、声音、隐私、诊断分组 | 启动选项的按需解释；诊断确认窗口 |
| 外观 | 公共页头与分组间距；配色使用 SettingGroup | 三种主题缩略图、色块菜单、自定义颜色折叠、宽分段控件 |
| 快捷键 | 组间 32px；ShortcutRow 复用 SettingRow | 组合键显示、冲突处理和录入弹层 |
| 快捷面板 | 标准分组和 36px Select | 双击修饰键权限提示、重启提示 |
| 同步 | 同步行为与文件同步分组 | 数值单位、路径选择、未开放功能标记 |
| 安全 | 页头后直接展示当前设置行 | 无意义的重复组名不再出现 |
| 网络 | 连接、传输、中继分组 | 每个中继编辑器是独立带边框工具，不套额外外框 |
| 存储 | 统一行、组和普通 Select 宽度 | 空间图形、图例、索引状态；图形左右内边距对齐 4px |
| 关于 | 应用图标/名称/版本为紧凑信息行；更新项使用普通分组 | 真实应用图标、赞助者重复条目、版本说明与更新确认 |

统计图、快捷键录入、中继地址编辑器不是普通单值设置，不要求内部也套统一两列。它们可以拥有自己的局部密度，但不能把特殊样式扩散到其他普通行。

## 14. 推荐实现模板

```tsx
const header = useMemo(() => <SettingsPageHeader category="general" />, [])

return (
  <SettingContentLayout header={header}>
    <SettingGroup title={t('settings.sections.general.startupTitle')}>
      <SettingRow label={label} description={description}>
        <Switch aria-label={label} checked={enabled} onCheckedChange={setEnabled} />
      </SettingRow>
    </SettingGroup>
  </SettingContentLayout>
)
```

生产设置页的 header 已由 SettingsPage 统一提供；上例主要用于说明结构或独立预览，子页面不要照抄后重复添加 header。

复杂内容需要分组但不需要默认行分割时，先用一个内容容器包住它，再作为 SettingGroup 的子元素；不能靠层层 border-none 抵消默认样式。

## 15. 验收方法

### 代码与结构

- 九页均只有一个页面 h1，标题和说明随类别变化。
- 普通表单分组使用 fieldset / legend；无复制的组标题线样式。
- 普通行复用 SettingRow；快捷键行不再维持另一套 padding。
- 页头下 24px，组间 32px，legend 下 16px，行上下各 16px。
- 普通行分隔为 1px、border 25%；标签字重 400，legend 字重 600。
- 外层容器、侧栏、导航与业务保存逻辑保持原有行为。

### 实际页面

- 九页分别检查浅色、深色；至少一轮窄内容布局与英文长文本。
- 检查原生计算后的字号、字重、margin，而不只检查 className。
- 展开菜单、修改设备名称、切换启动方式、打开颜色编辑并恢复，确认说明与值相符。
- 检查现有错误、禁用、重启、加载和空状态；这些状态不能因视觉统一消失。
- 图标正常加载，文字不溢出，焦点可见，菜单退出后能继续操作。

复现入口：

```bash
npx vitest run src/components/setting/__tests__ src/i18n/__tests__/setup-i18n.test.ts
bun run build
UI_FIXTURE_ENTRY=e2e/fixtures/settings.tsx UI_FIXTURE_PORT=1450 node e2e/visual-effects-server.mjs
PLAYWRIGHT_CHANNEL=chrome node e2e/settings-design-browser.mjs
```

浏览器脚本需要 Playwright；可通过 `PLAYWRIGHT_MODULE` 指定独立安装的模块入口。`SETTINGS_TEST_OUTPUT` 可指定截图和检查结果目录。

测试预览使用真实页面组件和合成配置；不读取个人剪贴板，不连接真实后台，不执行系统设置修改。它证明视觉与受控交互，不等价于各操作系统的业务端到端验收。具体操作仍依靠对应功能测试与真实客户端验证。

维护设计时，先改共享组件，再修正确有必要的例外，同时更新本文件中的数值与验收脚本；不能让文档成为与实际组件并行的第二套规范。

## 16. 主窗口字号扫描与迁移验收

### 16.1 扫描口径（2026-09-12）

页面范围依据 `src/components/app/AuthenticatedRoutes.tsx`、`AppContent.tsx` 和 `src/components/setting/settings-config.ts` 确认：历史记录、设备、九类设置，以及路由之前的首次设置、解锁、启动和故障状态。继续扫描 `src/pages/`、`src/layouts/`、`src/components/` 中全部非测试 TS、TSX、CSS 文件，同时检查全局样式、主窗口入口、平台字号和整窗缩放来源。

下表统计源码中的字号声明出现次数，包含条件分支、响应式状态、共享组件默认值及注释中的匹配；不是屏幕上文字节点的数量，也不代表所有共享组件均已被当前页面使用。没有单独声明的文字继承父级，不能理解为没有字号。像素值按 16px 根字号换算；`em` 取决于父级，不并入固定字号。

| 源码区域 | 声明次数 | 发现的名义字号 |
| --- | ---: | --- |
| `src/pages/`（含首次设置子步骤） | 63 | 10、11、12、14、16、24、30、36 |
| `src/layouts/` | 0 | 继承页面或公共组件 |
| `src/components/history/` | 45 | 9、10、10.5、11、12、13、14 |
| `src/components/clipboard/` | 49 | 10、11、12、13、14、18，以及相对父级的 0.8em |
| `src/components/device/` | 159 | 9、10、11、11.5、12、13、14、16、18、24、28、32 |
| `src/components/setting/`（含外观 CSS） | 108 | 11、12、14、16、20、24 |
| `src/components/layout/` | 4 | 12、14 |
| `src/components/app/` | 21 | 12、14、16、24 |
| `src/components/ui/` | 45 | 12、12.8、14、16 |
| `src/components/motion/` | 17 | 10、12、13、14、16 |
| `src/components/feedback/` | 4 | 12、14 |
| `src/components/update/` | 9 | 12、14 |
| `DevProfileIndicator.tsx` / `InvitationCodeInput.tsx` | 各 1 | 10 / 18 |

上述范围共 526 次源码匹配，出现 17 种固定名义字号：9、10、10.5、11、11.5、12、12.8、13、14、16、18、20、24、28、30、32、36，另有 0.8em 这一相对字号。全局变量中另有 15px，未计入组件声明分布。

### 16.2 页面分布与具体问题

| 页面 / 状态 | 当前分布与来源 | 目标调整 |
| --- | --- | --- |
| 历史记录 | 页级状态 12；卡片、搜索建议和进度大量使用 9～13；`HistoryCardContent.tsx` 正文 13，`CodeEntryContent.tsx` 代码 11 | 摘要统一 14，标签与进度统一 12，代码不再另缩小 |
| 历史详情 | `ClipboardPreviewInfo.tsx` 使用 11 和 10；预览子组件含 0.8em，标题有 18 | 元数据 12，正文 14，内容标题 16 |
| 设备 | 页内分组 10/11/12；列表和详情混用 9～16，主设备名 24；`AddDeviceInvitation.tsx` 邀请码使用 28/32 | 列表名称和邀请码 14、状态 12、详情主名称 24 |
| 通用设置 | 公共页头 24/14、分组 16、设置行 14/12；诊断和配置备份等附属内容主要 14/12 | 保留四档分工，迁移统一样式 |
| 外观设置 | 公共层级相同；外观 CSS 有 14/12，`AppearanceColorPicker.tsx` 色值有 11 | 色值等实际文字最低 12，局部 CSS 不再定义字号 |
| 快捷键设置 | 公共层级相同；快捷键显示 14，录入及说明 12/14 | 快捷键显示和控件值 14，提示 12 |
| 快捷面板设置 | 公共层级相同；权限说明、选项、重启提示 12/14 | 保留角色分工，检查菜单继承 |
| 同步设置 | 公共层级相同；数值 14、单位和路径提示 12 | 数值和输入统一正文，单位辅助 |
| 安全与隐私 | 公共页头和设置行提供字号，无本页独立声明 | 沿用共同角色，不新增重复分组 |
| 网络设置 | 公共层级相同；编辑器、探测状态、错误等 12/14 | 输入 14、状态 12，必须阅读的错误原因升为 14 |
| 存储管理 | 公共层级相同；`StorageUsageGroup.tsx` 容量数值 20，其他统计 12/14 | 容量主数值 16，辅助统计 12 |
| 关于 | 公共页头 24；应用名称和内容标题 16，版本 12、正文 14 | 保留角色；版本说明内容标题统一 16 |
| 首次设置 | `src/pages/setup/screens.tsx` 使用 12/14/16/24/30/36，包含按宽度改变的标题 | 各步骤标题统一 24，正文和输入 14 |
| 解锁 | `UnlockPage.tsx` 使用 12/14/16/30/36 | 标题 24，正文和输入 14，辅助 12 |
| 启动 / 故障 | `AppStatusScreen.tsx`、`StartupProgressScreen.tsx` 使用 12/14/16/24 | 按全页标题、正文、进度角色迁移 |
| 弹窗、通知与操作菜单 | Dialog 标题 16 但 `leading-none`；菜单项 13，分组和快捷键 10；小按钮 12.8；部分业务弹窗另用 24 | 弹窗标题 16/24，菜单项和文字按钮 14/20，辅助 12/16 |

设置页表内的公共层级来自 `SettingsPageHeader.tsx`、`SettingGroup.tsx` 和 `SettingRow.tsx`；公共控件的 12.8、13、16 等额外字号也会进入这些页面，不能只扫描页面文件就认定符合规范。

全局还有三处不一致来源：

- `src/styles/globals.css` 声明 12/13/14/15/16/20，但目前只有 `body` 的字号通过该组变量直接消费；组件大量使用独立 Tailwind 档位或任意值，变量表没有实际约束页面。
- `src/lib/window-ui.ts` 在 Windows 将这些变量覆盖成 11/12/13/14/15/18。继承 body 的文字随之变小，显式 `text-sm` 等文字不会一起改变，形成两套比例。
- `src/components/ui/input.tsx` 使用 `text-base md:text-sm`；首次设置和解锁标题也有响应式字号。窗口宽度因此会改变文字层级，与统一设计冲突。

### 16.3 落地顺序与完成标准

以下为统一文字样式的维护与验收要求：

1. 在现有全局样式建立四个角色和对应行高，先让一个真实设置页连同输入、菜单、弹窗完整使用它们，确认可用后推广。
2. 统一共享控件与平台字号来源，然后迁移历史记录、预览、设备、九个设置页、首次设置、解锁及启动状态。删除替代掉的旧变量和局部声明，不长期保留两套规范。
3. 扫描主窗口可达源码，确认没有局部字号、响应式字号和角色覆盖；富文本原文等例外须按第 5.2 节判定。检查未显式声明字号的继承路径和弹层挂载位置。
4. 在真实页面或使用真实组件的受控预览中检查计算后的字号与行高，覆盖所有页面和关键弹层。只检查类名或文档不能算通过。
5. 浅色、深色、窗口最小尺寸及常用桌面尺寸下，检查中文、英文及较长翻译；标题、标签、输入、状态、菜单均不重叠、不裁字，长名称有完整值的查看方式。空间不足通过换行、布局调整和滚动解决，不缩字。
6. 检查应用默认缩放、最小/最大支持缩放及恢复操作；同角色随整窗一致变化。分别核查 macOS、Windows、Linux，不把单平台截图当作跨平台证明。
7. 修改共享组件后回归独立快捷面板和更新窗口；完成相关现有测试、构建以及实际输入、菜单选择、弹窗开关、历史预览流程。记录未能运行的平台和状态。

源码复查入口（命中结果仍需区分颜色、图形与字号）：

```bash
rg -n 'text-(xs|sm|base|lg|xl|[2-9]xl)|text-\[|font-size|fontSize' src/pages src/layouts src/components src/styles src/lib/window-ui.ts -g '!**/__tests__/**' -g '!**/*.test.*'
```

### 16.4 本次实现与验证范围

- 已迁移主窗口页面与共享控件的字号，删除 Windows 独立字号覆盖和按窗口宽度改变字号的路径。按钮、输入、菜单项使用正文；重要错误说明不再使用辅助小字。
- 历史记录行使用最小高度并随内容增长，状态行允许换行，避免两行正文和标签被旧固定高度裁切。代码行号保持辅助字号，行号容器跟随正文行高对齐。文件类型图形扩大到可容纳 12px 标签的尺寸。
- 版本说明使用统一文字角色排版，不再依赖排版插件的独立字号和行高。富文本原文、图片和独立窗口的页面设计范围不变。
- `src/styles/__tests__/typography.test.ts` 检查页面和共享组件不得重新引入独立字号、行高、字距、响应式字号或覆盖公共控件文字角色；同时验证字号样式组合不会破坏颜色。
- 九类设置继续使用 `e2e/settings-design-browser.mjs` 检查浅色、深色、窄内容与输入/选项操作。新增 `e2e/typography-browser.mjs` 检查真实历史记录、预览和设备组件，以及首次设置各步骤、解锁、启动、故障、反馈、邀请码和版本说明的中英文、浅深色、360/1100 宽度；检查实际计算后的字号、字距与横向溢出，并验证菜单、代码行对齐和弹窗操作。
- 浏览器检查使用合成数据与真实组件；历史和设备预览是组件组合，并不冒充已连接真实后台的完整业务页面。快捷面板和更新窗口的相关现有测试纳入回归；Windows、Linux 原生窗口及各平台实际整窗缩放仍需对应环境验证。

2026-09-12 验证记录：85 个相关测试文件、484 项测试通过；设置页 27 组主题/宽度检查与主窗口组件 136 组语言/主题/宽度检查通过，另验证文件传输进度在窄、宽布局下不覆盖正文。应用构建和 macOS 12.5 前端兼容检查通过。截图已人工检查；这些结果不替代尚未执行的原生跨平台验证。

复现命令（使用项目依赖支持的 Node.js 版本，例如 24）：

```bash
npx vitest run src/styles/__tests__/typography.test.ts src/components src/pages src/layouts src/quick-panel/__tests__ src/updater/__tests__
bun run build
UI_FIXTURE_ENTRY=e2e/fixtures/typography.tsx UI_FIXTURE_PORT=1451 node e2e/visual-effects-server.mjs
PLAYWRIGHT_CHANNEL=chrome node e2e/typography-browser.mjs
```

`PLAYWRIGHT_MODULE` 可以指定独立安装的浏览器工具；`TYPOGRAPHY_TEST_URL`、`TYPOGRAPHY_TEST_OUTPUT` 分别指定预览地址和截图/检查记录目录。测试不会提交反馈、修改真实配对或读取个人剪贴板。
