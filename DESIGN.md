# 桌面设置页设计规范

更新日期：2026-09-08。基准：用户确认的「通用设置」内容区。

## 1. 适用范围与目标

本规范适用于通用设置、外观设置、快捷键、快捷面板、同步设置、安全与隐私、网络设置、存储管理、关于，共九个设置页。

设计目标是让人快速识别「这是哪一页、这一组管什么、这一项当前是什么、在哪里修改」。以清楚的文字层级、稳定的控件位置和克制的分隔线组织内容，不把设置页做成仪表盘或展示型落地页。

窗口框、交通灯、侧栏、返回入口、外层圆角容器、平台窗口差异保持现有实现。本规范调整的是它们内部的内容；不能为了统一设置项样式顺手替换这些外层布局。

界面调整不能改变设置保存路径、设备同步范围、密码处理、自动启动注册、重启要求或业务行为。隐私与加密约束继续服从 [VISION.md](VISION.md)。

## 2. 实现入口与所有权

| 入口 | 负责内容 | 禁止放入的内容 |
| --- | --- | --- |
| `src/pages/SettingsPage.tsx` | 选择当前类别，在共同入口插入一次页头 | 各页业务设置的保存条件 |
| `src/components/setting/SettingsPageHeader.tsx` | 页标题、页面说明与页头间距 | 某一页专用的按钮、卡片、统计值 |
| `src/layouts/SettingContentLayout.tsx` | 内容最大宽度、字距、组间距离 | 平台窗口框、侧栏布局 |
| `src/components/setting/SettingGroup.tsx` | 表单分组、legend、组内分隔线 | 配置读取、网络请求、模式判断 |
| `src/components/setting/SettingRow.tsx` | 标签、说明、附加标记、右侧控件位置 | 复制子控件状态或替它保存 |
| `src/components/ui/`、`src/components/motion/` | 输入、开关、菜单、弹层、键盘及焦点行为 | 设置页专属业务条件 |
| 各个设置页组件 | 内容顺序、具体值、修改动作和反馈 | 自行重新定义普通设置行的样式 |

普通设置项必须复用 `SettingGroup` / `SettingRow`。特殊控件可保留自己的内部结构，但外围使用同一字号、间距和语义颜色。禁止另外建立一套「新设置组件」，让新旧页面长期并行。

## 3. 尺寸的解释方式

以下像素换算均以根字号 16px 为阅读参照，不是硬编码要求。实际尺寸会受系统缩放、应用缩放和已有平台适配影响。实现使用 Tailwind 工具类、`rem`、比例或已有组件尺寸参数。

- 基本间距单位：`0.25rem`，名义 4px。
- 内容字距：`tracking-normal`，即 0。不要继承正文的额外字距，也不要在标题使用负字距。
- 不按窗口宽度缩放文字。窄窗口通过换行、控件移到下一行和滚动适配。
- 不为整行固定高度。行高由文字与控件中较高的一方决定。
- 1px 边框是细线定义，不是用固定像素搭建整页布局。

## 4. 内容层级与语义

```text
原有设置页外层容器
  内容区，最大宽度 48rem
    页面页头：h1 + 一句说明
    32px 组间距的内容流
      独立基础信息行（可选，例如设备名称）
      fieldset
        legend
        设置行
        细分隔线
        设置行
      fieldset
        legend
        设置行
```

### 4.1 页面页头

每页只能出现一个页面级 `h1`，文字与侧栏类别名称一致。页头由公共入口渲染，各页组件不得再复制一份。

说明是一句简短的范围介绍，不是使用教程，不堆叠所有能力，不重复标题。正常桌面宽度争取一行；较长翻译允许自然换行。

页头先于加载、错误或业务内容展示，不能因为数据尚未加载而让用户失去页面位置感。

### 4.2 设置分组

相关设置使用原生 `fieldset + legend`。标题必须是这个 fieldset 的 legend，不能只把普通 section 中的 h2 改成 legend。

- legend 是分组名称，例如「启动设置」「语言」「同步行为」「历史保留」。
- 不重复页面主标题，例如「同步设置」页中第一组叫「同步行为」。
- 分组不额外画外框，不加背景卡片、投影或标题下横线。
- 只有一个设置项时允许不显示额外组名，例如安全页的自动解锁，避免三层标题表达同一件事。
- 设备名称属于当前设备的基础信息，放在启动分组之前，不归类为启动行为。

### 4.3 设置行

设置名称是普通文字，不是 h2/h3/h4。全页到处出现粗体「小标题」会破坏层级，也会干扰辅助技术按标题导航。

左侧固定由「设置名称、可选说明」组成；实验标记、帮助入口等跟在名称旁。右侧是当前值或操作控件。

关于页的应用身份、赞助名单不是表单项，可以使用普通容器或带名称的 section；如果有内容标题，使用 h2，并采用分组标题的视觉规格。不要为了统一标签而把非表单内容也强行包成 fieldset。

## 5. 文字规格

| 角色 | 工具类 / 数值 | 名义字号 | 行高 | 字重 | 颜色 |
| --- | --- | --- | --- | --- | --- |
| 页面标题 | `text-2xl leading-8` | 24px | 32px | 600，`font-semibold` | 默认前景色 |
| 页面说明 | `text-sm leading-relaxed` | 14px | 约 22.75px | 400 | `text-muted-foreground` |
| 分组 legend | `text-base` | 16px | 默认约 24px | 600 | 默认前景色 |
| 普通设置名称 | `text-sm font-normal` | 14px | 默认约 20px | 400 | 默认前景色 |
| 设置项说明 | `text-xs leading-relaxed` | 12px | 约 19.5px | 400 | `text-muted-foreground` |
| 控件中的值 | 复用控件默认 `text-sm` | 14px | 由控件控制 | 400 或控件默认 | 对应表面前景色 |
| 状态 / 辅助信息 | `text-xs` | 12px | 需要换行时 `leading-relaxed` | 400 | 语义对应的前景色 |
| 小型修改标记 | `text-xs font-medium` | 12px | 默认 | 500 | `text-primary` 或语义色 |

`leading-relaxed` 是 Tailwind 的 1.625 倍行高，不应与全局同名自定义变量的 1.6 混为一谈。表格数值以这里列出的实际类名为准。

说明文字最大宽度为 `34rem`，名义 544px。名称和说明都允许折行；长路径、长单词不能把控件推到容器外。

不得通过全大写、额外字距、同级全加粗或放大所有行标签来「增强层级」。真正的层级由 24/16/14/12 的分工和留白共同表达。

## 6. 间距规格

| 位置 | 标准值 | 代码入口 |
| --- | --- | --- |
| 内容最大宽度 | `48rem` / 768px | `max-w-3xl` |
| 内容外侧内边距 | 保留现有：16px；sm 起 24px；lg 起 32px | SettingsPage 的 `p-4 sm:p-6 lg:p-8` |
| 标题与页面说明 | `0.25rem` / 4px | `mt-1` |
| 页头与后续内容 | `1.5rem` / 24px | 页头 `mb-6` |
| 两个设置组之间 | `2rem` / 32px | 内容流 `gap-8` |
| legend 左右内边距 | `0.25rem` / 4px | `px-1` |
| legend 与组内容盒 | `1rem` / 16px | `mb-4` |
| 普通设置行左右内边距 | `0.25rem` / 4px | `px-1` |
| 普通设置行上下内边距 | 各 `1rem` / 16px | `py-4` |
| 左文案与右控件的最小水平间距 | `2rem` / 32px | `gap-x-8` |
| 控件折到下一行时的垂直间距 | `0.75rem` / 12px | `gap-y-3` |
| 名称与说明 | `0.25rem` / 4px | 文案列 `gap-1` |
| 名称与帮助 / 标记 | `0.5rem` / 8px | 名称行 `gap-2` |
| 同一操作簇内按钮 | 通常 `0.5rem` / 8px；紧凑工具条按对应组件定义 | `gap-2` 等 |

### 6.1 不要重复计算间距

- `legend` 下已有 16px，第一行自身还有 16px 上内边距，因此从 legend 盒下缘到第一行内容盒通常是 32px。不要再额外补一层 `pt-4`。
- 最后一行有 16px 下内边距，组间再有 32px。不要给每个组同时加 `mb-8`。
- 一个页面如果用自己的外层 div 包住多个组，也必须用 `flex flex-col gap-8`，不能改用 `space-y-5/6` 形成另一套密度。
- 独立设备名称行可以 `pt-0`，因为它紧接页头；其余行按默认值执行。
- 复合编辑器内部可以更密，但它与其他组的关系仍遵循 32px。

### 6.2 行高计算

普通行不是固定高度：

```text
行外部高度 = max(左侧文案总高, 右侧控件总高) + 32px 上下内边距
文案总高 = 名称行高 +（有说明时：4px + 说明总行高）
```

例如名义 16px 根字号下，只有名称且右侧是 36px 选择框时，整行约 68px；名称加一行说明时约 75.5px。长说明允许自然增高，不能把文字裁切进预设高度。

## 7. 分隔线、表面与颜色

### 7.1 普通行分隔线

- 同组相邻行之间使用 `divide-y divide-border/25`。
- 线宽为 1px；颜色来自 `--border`，以 25% 强度混合。不是把整行透明度设为 25%。
- 第一行之前、最后一行之后不额外补线。
- legend 下不画线；组与组主要靠 32px 留白区分。
- 不给每个设置行添加完整边框、圆角背景或阴影。

### 7.2 控件边界

控件边框用于表达「这里可以操作」，因此比普通行分隔线清楚。使用现有 Input / Select / Button 的 `border-input`、`border-border`，不要把控件边框也统一淡到 25%。

表单输入、弹出菜单、提示和错误区域使用现有语义颜色。默认正文使用前景色，说明使用 muted foreground，错误使用 destructive；不能以降低整个父容器透明度替代这些颜色角色。

### 7.3 深色与例外色

- 浅色、深色都读取相同的语义变量，不另写一套背景灰度。
- 不给设置页增加大面积渐变、光斑或磨砂装饰。
- 关于页展示真实应用图标，不用渐变方块和自绘图标代替。
- 主题预览的选中边框、单选圆点和键盘焦点框统一跟随当前页面的 `--primary`，包含用户自定义强调色；不能固定为蓝色，也不从缩略图本身推断选中颜色。
- 主题缩略图的交通灯和赞助等级颜色属于明确的语义例外，不推广成普通设置行的装饰色。
- 外层 InsetSurface 的现有背景、边框、圆角不由本规范覆盖。

## 8. 控件尺寸与选型

| 需求 | 控件与尺寸 | 规则 |
| --- | --- | --- |
| 是 / 否 | 现有 `Switch` 默认尺寸 | 不改成两个文字按钮；保持开关本身的自然宽度 |
| 普通文本 | 现有 `Input`；默认高 2rem / 32px | 通常外包 `w-40` / 10rem；不为单页重写输入样式 |
| 普通选项 | 现有 `Select`；默认高 2.25rem / 36px | 通常宽 `w-40` / 10rem；快捷面板也使用标准高度 |
| 带单位数值 | 使用现有数值控件 | 可采用 `w-44` / 11rem，为数值、单位和校验预留空间 |
| 配色选择 | 色块 + 名称的 Select | 宽 12.5rem；选项展开后再显示完整预设集合 |
| 三种互斥模式 | 分段单选控件 | 宽上限 20rem，每项等分；保留真实 radio 语义 |
| 快捷键 | 快捷键显示 + 录入弹层 | 显示入口最小 5rem、最大 13rem；内部按组合键需要排布 |
| 普通命令 | 现有 Button | 默认高 2rem；紧凑 `sm` 高 1.75rem；不把所有按钮升为大号 |
| 图标工具 | Button 的 icon 系列 | 默认 2rem 方形；已有紧凑 1.5/1.75rem 档位按用途使用 |
| 界面缩放 | 减号 + 比例选择 + 加号 + 恢复 | 20rem 操作区，36px 图标按钮，12px 内部间隙 |

开关的现有默认轨道为 32 × 18.4 CSS px，拇指 1rem，位移由共享组件管理。这是存量组件事实，不授权其他页面照抄像素值另画一个开关。

普通输入、选择框与默认按钮使用已有 8px 圆角；小号按钮受现有 `--radius-md` 上限控制。重复的中继编辑器、赞助者条目可使用 8px 边框容器。普通分组不能变成卡片；不要把这些条目再装进第二层装饰卡片。

菜单使用当前 Base UI / 公共 motion 封装，不用原生 select 与另一套自制菜单混搭。长选项说明可把菜单扩展到 18rem，仍需 `max-width: calc(100vw - 2rem)`；关闭后的控件只显示简洁的当前值。

## 9. 文案与信息密度

设置名称尽量短，说明只回答该项对当前用户有什么影响。长解释不能默认占据一整段。

通用设置的既定例子：

- 「设备名称」：说明为「用于区分你的不同设备。」；不放在启动分组中。
- 「启动方式」：默认只显示当前模式的一句说明。展开后，正常、静默、轻量各有一句解释，方便比较。
- 「启动时恢复剪贴板」：说明为「将最近一条历史记录重新放回剪贴板。」；不要把后台就绪过程写成面向用户的操作说明。
- 「流畅模式」：默认只显示当前结果；具体判断原因通过信息按钮查看。

独立信息入口不能只有一个没有名称的图标。需要可见 tooltip、可访问名称和键盘可达性。帮助内容不能承载用户必须先知道的危险后果；删除、清理、退出等影响应在动作附近或确认窗口明确说明。

所有设置页标题与说明均有六种语言资源。不得直接显示翻译键，也不得把同一段说明分别硬编码到页头和内容组件。

## 10. 状态与交互

### 正常与修改

- 控件直接反映真实设置来源。没有保存失败时，不展示额外的「已保存」常驻标记。
- 普通开关/选择沿用当前即时或乐观保存方式；设备名称沿用失焦保存，不能因样式统一改成每次输入都保存。
- 保存状态属于发起操作的控件，不让一个无关设置行同时变暗。
- 改变主题或视觉效果不重挂载整个页面，避免丢失输入、滚动和焦点。

### 保存失败与重启

- 失败必须有可见反馈；沿用现有回滚、错误提示与重试约定，不能只写控制台。
- 涉及重启的设置沿用 `RestartBanner`，持续说明尚需重启；不能用短暂 toast 冒充已完全生效。
- 错误说明靠近对应行或对应分组，用 `role="alert"`；一般状态使用 `role="status"` 或现有 output。
- 危险动作继续使用明确确认，视觉统一不能删除确认或缩小提示信息。

### 加载、禁用与空状态

- 加载时保持控件或图形的尺寸，避免文字「加载中」导致按钮变宽。
- 已有加载图形应与真实统计布局同宽；加载、失败、零数据是三种不同状态。
- 禁用态不能只靠淡化颜色表达，继续使用真实 disabled / aria-disabled。
- 关闭动效后，加载、传输和错误仍需有文字、数值或静态图标反馈。

## 11. 焦点与键盘

- 页面 h1 用于页面导航；fieldset 的 legend 为分组命名；普通设置名称不是标题。
- 每个实际控件应有可访问名称。新增输入必须提供 label 关联或 aria-label；装饰图标使用 aria-hidden。
- 单选组使用方向键、空格；Select 保留键盘导航；录入快捷键、颜色输入等使用内容弹层，不套操作菜单的键盘模型。
- 沿用共享控件的 focus-visible 外圈。不得全局去掉 outline 后不补焦点反馈。
- 弹层关闭后焦点返回触发入口；Escape 不应让父页面先离开。
- 聚焦、悬停、选中不改变元素占位尺寸；边框加重采用预留边框或 outline。

## 12. 窄窗口与长内容

普通 SettingRow 使用 flex-wrap。文案列基准为 14rem，可增长/缩小；控件列不随便压扁，空间不够时移到下一行，保持右对齐，与文案间隔 12px。

所有行、分组、控件容器使用 `min-w-0`，控件容器限制 `max-w-full`。长描述自然换行；只有路径、快捷键展示等受控内容可以截断，并保留完整值的查看方式。

外观页的宽单选组是特殊变体：

- 常规布局左侧文案 + 最大 20rem 控件。
- 内容容器不超过 34rem 时改成单列；同时保留小视口回退规则。
- 模式说明与控件一起落在第二列；单列时回到第一列。
- 主题预览仍为三个等分选项，缩略图保持 1.9 宽高比，单项上限 11rem；窄内容降低预览间隙并允许选项文案换行。

窄窗口允许内容垂直滚动，不允许整个设置内容横向滚动。应用外层的窗口最小尺寸、侧栏行为继续采用原有设计，不能用这份规范擅自更改。

## 13. 特殊页面的推广规则

| 页面 | 普通结构 | 保留的特殊内容 |
| --- | --- | --- |
| 通用 | 独立设备名称 + 启动、语言、声音、隐私、诊断分组 | 启动选项的按需解释；诊断确认窗口 |
| 外观 | 公共页头与分组间距；配色使用 SettingGroup | 三种主题缩略图、色块菜单、自定义颜色折叠、宽分段控件 |
| 快捷键 | 组间 32px；ShortcutRow 复用 SettingRow | 组合键显示、冲突处理和录入弹层 |
| 快捷面板 | 标准分组和 36px Select | 双击修饰键权限提示、重启提示 |
| 同步 | 同步行为与文件同步分组 | 数值单位、路径选择、未开放功能标记 |
| 安全 | 页头后直接展示当前设置行 | 无意义的重复组名不再出现 |
| 网络 | 连接、传输、中继分组 | 每个中继编辑器是独立带边框工具，不套额外外框 |
| 存储 | 统一行、组和普通 Select 宽度 | 空间图形、图例、索引状态；图形左右内边距对齐 4px |
| 关于 | 应用图标/名称/版本为紧凑信息行；更新项使用普通分组 | 真实应用图标、赞助者重复条目、版本说明与更新确认 |

统计图、快捷键录入、中继地址编辑器不是普通单值设置，不要求内部也套统一两列。它们可以拥有自己的局部密度，但不能把特殊样式扩散到其他普通行。

## 14. 推荐实现模板

```tsx
const header = useMemo(() => <SettingsPageHeader category="general" />, [])

return (
  <SettingContentLayout header={header}>
    <SettingGroup title={t('settings.sections.general.startupTitle')}>
      <SettingRow label={label} description={description}>
        <Switch aria-label={label} checked={enabled} onCheckedChange={setEnabled} />
      </SettingRow>
    </SettingGroup>
  </SettingContentLayout>
)
```

生产设置页的 header 已由 SettingsPage 统一提供；上例主要用于说明结构或独立预览，子页面不要照抄后重复添加 header。

复杂内容需要分组但不需要默认行分割时，先用一个内容容器包住它，再作为 SettingGroup 的子元素；不能靠层层 border-none 抵消默认样式。

## 15. 验收方法

### 代码与结构

- 九页均只有一个页面 h1，标题和说明随类别变化。
- 普通表单分组使用 fieldset / legend；无复制的组标题线样式。
- 普通行复用 SettingRow；快捷键行不再维持另一套 padding。
- 页头下 24px，组间 32px，legend 下 16px，行上下各 16px。
- 普通行分隔为 1px、border 25%；标签字重 400，legend 字重 600。
- 外层容器、侧栏、导航与业务保存逻辑保持原有行为。

### 实际页面

- 九页分别检查浅色、深色；至少一轮窄内容布局与英文长文本。
- 检查原生计算后的字号、字重、margin，而不只检查 className。
- 展开菜单、修改设备名称、切换启动方式、打开颜色编辑并恢复，确认说明与值相符。
- 检查现有错误、禁用、重启、加载和空状态；这些状态不能因视觉统一消失。
- 图标正常加载，文字不溢出，焦点可见，菜单退出后能继续操作。

复现入口：

```bash
npx vitest run src/components/setting/__tests__ src/i18n/__tests__/setup-i18n.test.ts
bun run build
UI_FIXTURE_ENTRY=e2e/fixtures/settings.tsx UI_FIXTURE_PORT=1450 node e2e/visual-effects-server.mjs
PLAYWRIGHT_CHANNEL=chrome node e2e/settings-design-browser.mjs
```

浏览器脚本需要 Playwright；可通过 `PLAYWRIGHT_MODULE` 指定独立安装的模块入口。`SETTINGS_TEST_OUTPUT` 可指定截图和检查结果目录。

测试预览使用真实页面组件和合成配置；不读取个人剪贴板，不连接真实后台，不执行系统设置修改。它证明视觉与受控交互，不等价于各操作系统的业务端到端验收。具体操作仍依靠对应功能测试与真实客户端验证。

维护设计时，先改共享组件，再修正确有必要的例外，同时更新本文件中的数值与验收脚本；不能让文档成为与实际组件并行的第二套规范。
