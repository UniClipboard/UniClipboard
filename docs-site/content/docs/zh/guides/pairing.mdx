---
title: 配对与同步
description: 局域网内的设备发现机制。
---

配对会交换长期加密密钥，之后的每次同步都会复用该密钥——
全程不依赖第三方中转。

## 发现

UniClipboard 通过 mDNS 广播服务。处于同一广播域的对端会自动出现在
**设备列表**中。

## 信任

配对需要一次性、限时的配对码。一旦确认，对端会被写入本地信任库；
后续会话直接重连，无需再次确认。

## 传输

同步走 TLS。负载使用配对时派生的「每对端」密钥端到端加密。
