## 0.13.1 - 2026-06-04

### Fixes

- Fix excessive disk usage on Linux: copying an image no longer spawns a flood of duplicate synced copies, and the configured cache size limit is now actually enforced — cached images are reclaimed once they exceed the size limit or retention period (#957)
