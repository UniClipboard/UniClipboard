## 0.13.1 - 2026-06-04

### Fixes

- Fix excessive disk usage on Linux, where copying a single image could flood the clipboard cache with hundreds of near-duplicate captures, and re-copying existing content now reuses the existing history entry instead of piling up duplicates (#957)
