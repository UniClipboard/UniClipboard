# 流畅模式校准状态

日期：2026-09-08。状态：未完成，生产能力分类维持 unknown。

## 已实现

- Linux 自动固定流畅，不进行硬件探测或运行采样。
- macOS/Windows 探测入口按需读取原生 CPU/内存，并设 500 ms 等待上限。
- 尚无经实际 WebView 合成器与代表机器验证的图形能力分类。因此探测返回 unknown；CPU/内存不单独产生 capable。
- 手动效果优先可立即使用，系统减少动效仍优先。
- 三次独立严重卡顿、下一次 GUI 启动才采用、手动覆盖保护等规则有受控输入测试。生产 unknown 路径不会启动采样。

## 尚缺证据

| 平台 | 原生编译/执行 | 弱强机器对照 | 图形能力组合规则 | 监测开销实测 |
| --- | --- | --- | --- | --- |
| macOS | 见验收记录 | 未取得代表设备对照 | 未冻结 | 未完成 |
| Windows | 未运行 | 未完成 | 未冻结 | 未完成 |
| Linux | 策略由模拟输入测试 | 未实测，默认流畅规则已确定 | 不需要 | 应为无采样，原生验证待补 |

此版本不能宣布“性能充足的 macOS/Windows 设备会自动启用完整效果”。实现此项必须先完成规格 S5，使用真实代表设备冻结规则；不能把测试注入的 capable 或开发者手动模式当作完成证据。

## 后续执行输入

按规格 S5 的 200 条合成历史、五轮固定操作、三种效果/监测组合记录逐轮结果。报告必须补齐机器条件、实际图形能力读取来源、训练与留出轮次、延迟与长帧统计、监测 CPU 开销。没有数据时保留“未完成”，不要填写推测数字。
