# 调研记录

## Requirements

- 桌面、iOS、Android、HarmonyOS 使用同一套完整 P2P 核心。
- 除文件内容外，所有持久化业务内容必须先加密；用户已明确指定文件落盘不加密。仓库现有根规则、产品文档和计划仍有相反表述，必须先统一。
- 新核心是宿主唯一公开入口，不能暴露旧 facade、内部接口或任务句柄。
- 暂停后释放节点，恢复时保持身份与事件流，不自动重试已取消工作。
- 每个阶段必须用计划指定的构建、测试和真机结果验收。

## Research Findings

- 2026-07-21 本次续作确认：工作区起点干净，最新提交为 `a02bd1051`；Phase 2 仍停在 daemon 生产所有权、webserver 历史/剪贴板/blob/传输能力和 CLI 旧运行时未迁完的阶段。
- 现有 `QueryHistory` 走搜索投影，只能覆盖分页概要；webserver 历史接口还承诺详情、删除、收藏、统计、资源和清空结果，不能用现有操作直接替代整组接口。
- 本轮先补齐并迁移历史基础能力；重发、接收进度、取消和投递视图需要分别保持现有逐目标与逐任务信息，留到后续独立切片。
- 历史列表公开投影包含预览、缩略图、链接、文件大小、图片尺寸、标签和收藏状态；详情包含完整文本；资源结果可含内联字节和 URL。稳定结果必须完整保留字段，但 `Debug` 只能报告种类和数量。
- 收藏底层以 `bool` 表示条目是否存在，统一操作应把 `false` 转成稳定的不存在错误，HTTP 继续返回 404，不能把“未找到”伪装成成功。
- 历史清空结果包含删除数与逐条失败；统一边界需保留失败条目标识，但不能让底层异常、磁盘路径或用户内容进入公开错误或日志。
- 历史基础迁移后，`clipboard.rs` 的列表到清空处理区已不再引用 `ClipboardHistoryFacade`、`ClipboardHistoryError`、`ClipboardListInput` 或任何对应直接方法；后半段的接收、发送、重发、投递和传输取消仍需后续切片。
- 接收状态组可无损迁移为四个稳定操作：单条进度、全部活动进度、按条目与尝试编号取消、按传输编号与原因取消；现有响应字段和枚举结果都能完整表达。
- 条目接收取消结果共有 `cancellation_requested`、`cancelled`、`not_receiving`、`too_late`、`already_terminal`、`superseded` 六种；文件传输取消结果为 `cancelled`、`not_inflight` 两种。
- 当前四条 HTTP 失败路径直接把应用层错误字符串返回 500，可能包含存储或传输细节；统一核心应只返回稳定的输入、暂不可用和内部错误编号，HTTP 保留状态码但改用通用消息。
- 恢复接口不能直接映射为普通 `EngineError`：现有 410 契约必须返回记录编号、表示编号和丢失状态，而公开错误按 Plan 003 只能包含编号、类别和是否可重试。恢复需把可预期业务失败建模为稳定结果，不能丢字段或把内部详情塞回公开错误。
- `CaptureCurrentClipboard` 没有上述缺口：成功只返回可选记录编号，空表示当前没有可捕获内容；失败可安全归一为稳定内部错误。因此先独立迁移捕获动作。

- 2026-07-21 第五次接续的全量审计确认：webserver 仍直接调用创建空间、加入空间、取消邀请、重置设置、查询设置状态、切换空间、查询迁移进度、历史重发和移动局域网设备列表；不能仅迁移交接摘要列出的三个设置动作就宣告 daemon 完成。
- 当前旧类型命中还分布在 daemon 的启动装配、移动局域网生命周期、搜索与网络后台任务，以及 CLI 开发会话。后续必须区分核心业务、桌面职责和诊断工具，不能用扩大 `uc-engine` 职责来消除搜索命中。
- `CliAppRuntime` 的唯一直接持有点仍是 `apps/cli/src/commands/app_session.rs`；普通初始化、加入、发送和搜索命令已经通过 daemon client，删除旧入口的主要剩余工作是迁移开发工具而非重写普通命令。
- 统一 `JoinSpace` 不能只返回空间标识：首次加入和切换都需要双方设备与身份信息，切换还需要迁移数量。最终契约使用同一完整结果，`migrated_records = None` 表示首次加入，`Some(count)` 表示切换。
- daemon 的 `/redeem` 与 `/switch-space` 必须保留显式模式，不能直接共用自动判断：公开操作可按设置状态自动选择，但两个既有 HTTP 路径各自承诺了不同前置条件和错误语义。
- 加入输入的设备名应允许省略：移动宿主可显式传入并保存，daemon 的现有请求没有该字段，必须继续沿用已保存的设置；空字符串仍是无效输入。
- `crates/uc-webserver/src/api/v2/setup.rs` 的创建、邀请、加入、取消、重置、状态、切换和迁移进度现已全部通过 `uc-engine` 的单一实现；该模块不再导入或取得旧 `SpaceSetupFacade`。
- CLI 的 `CliAppRuntime` 只在 `dev-tools` 下使用，但调用面横跨原始 blob、系统剪贴板、指定地址邀请、文件捕获、种子数据和移动局域网调试；删除旧类型前必须分别迁移到统一核心操作、桌面诊断能力或移动局域网兼容工具，不能整组删功能。
- 2026-07-21 第六次接续复核确认：`DaemonProcessRuntime` 仍通过 `build_app_facade_from_deps` 自行组装并持有 `AppFacade`；`Engine::start` 则通过 `ProductionRuntime::start` 独立组装另一份数据库、节点、后台任务和 facade。两者当前没有共享生产所有权，不能并排启动。
- daemon 已直接依赖 `uc-engine`，但这个依赖目前只用于调用 `uc_engine::internal::*` 的共享业务实现；它尚未持有 `Engine`，webserver 状态也仍以 `AppFacade` 为核心业务入口。因此“复用实现”不等于“生产路径已切换”。
- 下一步的完成证据必须先锁定所有权：daemon 进程运行时只持有一个 `Engine`，webserver 的跨平台业务通过 `Engine::execute`，桌面升级、HTTP、移动 LAN 和连接诊断继续由 daemon 外壳拥有；不新增 facade 包装接口。
- daemon/webserver 的实时调用面可明确分为两组：创建、加入、邀请、加密会话、历史、搜索、发送、成员和稳定设备状态属于跨平台核心；HTTP 监听、升级记录、daemon 运行状态、移动 LAN、地址/通道诊断和产品分析属于桌面外壳。迁移不能用一个新的总 facade 重新混合这两组。
- 当前 `HostCapabilities` 已能表达目录、安全存储、剪贴板和文件句柄，但桌面旧装配仍额外拥有 run mode、产品分析、移动 LAN、HTTP 事件和若干后台 worker。直接用现有 `Engine::start` 替换 daemon 会丢失这些桌面行为，必须先把核心 worker 归入 `Engine`、外壳依赖从总 facade 拆出。
- 现有 `Engine::start` 的生产运行时已自行启动搜索、剪贴板、文件传输和 blob 任务；daemon 旧运行时也启动对应任务。所有权替换完成前不得同时运行两者，否则不仅数据库和节点重复，后台任务也会重复消费同一份状态。
- 升级检测与确认只依赖 `UpgradeFacade`，属于桌面外壳能力；启动判断和 HTTP 处理现已直接取得该能力，不再先索取总 `AppFacade`。当前 `DaemonApiState::new` 仍从旧对象克隆它，待最终装配切换时改由桌面外壳装配直接提供。
- 调试模式查询、调试开关和日志导出只依赖 `DiagnosticsFacade`，同样属于桌面外壳；三条 HTTP 路径现已直接取得该能力，不再经总 `AppFacade`。
- 配置导出、导入预览和导入暂存只依赖 `ConfigMigrationFacade`；三条 HTTP 路径已直接取得这项桌面迁移能力，不再经总 `AppFacade`。
- 设置读取、设置更新和 relay 连通性探测都由同一 `SettingsFacade` 提供；三条 HTTP 路径已直接取得该桌面设置能力，不再经总 `AppFacade`。旧错误映射注释声称 webserver 不暴露探测端点，与真实路由矛盾，已同步修正。
- `/storage/stats` 与 `/storage/clear-cache` 不能作为桌面外壳能力直接拆出：ADR-005 规定数据库、缓存和持久格式由核心拥有。它们必须新增稳定核心操作并经 `Engine::execute`，因此本轮没有把 `StorageFacade` 暴露到新的后台状态字段。
- 移动 LAN 当前借 `AppFacade.mobile_sync` 的 `OnceLock` 打破真实构造循环：`MobileSyncFacade` 更新设置时调用 LAN 生命周期控制器，而控制器启动监听器又需要 `MobileSyncFacade`。应改为只保存 `MobileSyncFacade` 的专用一次性槽位；该槽位属于桌面兼容外壳，不得借此访问其他核心能力。
- 移动 LAN 构造循环现已由 `MobileSyncFacadeSlot` 解决：槽位只保存 `Weak<MobileSyncFacade>`，在首次 `apply` 前安装；监听器无法再通过总 `AppFacade` 访问其他能力，也不会因槽位形成新的强引用环。
- webserver 的移动设备管理、移动兼容设置和 LAN 网卡列表现直接读取可选 `MobileSyncFacade`；未装配时仍返回既有暂不可用错误，不再通过总 `AppFacade.mobile_sync` 查找。
- 外壳能力拆分后重新扫描，`DaemonApiState::new` 全仓只有 daemon 一个生产调用点；未来增加真正的 `Engine` 所有权不需要迁移测试专用构造器。
- webserver 剩余 `app_facade_or_error` 基本都属于跨平台核心：设置流程、加密会话、历史/恢复/投递视图、搜索、成员、设备、存储和 blob。下一步应扩充稳定 `Operation`，不能继续把这些子 facade 当作外壳字段拆出。
- 存储统计与缓存清理现由 `QueryStorageStats` 和 `ClearStorageCache` 表达；公开结果只包含分类字节数与释放量，不包含旧 `StorageStatsView.data_dir`。HTTP 层复用核心唯一实现，底层错误不再原样返回。
- 设备列表存在两个不同概念：统一核心的稳定设备编号/显示名/在线状态，与桌面连接诊断的 peer id/通道/地址。后者属于桌面外壳，不能为了复用一个名字塞入跨平台 `ListDevices`。
- 2026-07-21 恢复时已再次核对 `VISION.md`：Plan 003 的完成必须满足统一核心入口、禁止新旧双轨，以及除文件正文外的持久化业务内容默认加密三项底线。
- `plans/README.md` 仍将 Plan 003 标为 `TODO`，与已完成的大量实现和验收记录不一致；只有在本轮把生产调用、旧入口和全部验收逐项核实后才能更新正式状态。
- Plan 003 明确要求 daemon 成为首个生产消费者，CLI 与 Tauri 只能通过 daemon client 或 `uc-engine`，完成后必须删除被替代的公开入口且不保留运行时双轨。
- 当前生产代码仍未满足 Plan 003：daemon 多处直接持有 `AppFacade`/`AppDeps`，CLI 仍持有 `uc_bootstrap::CliAppRuntime`，`uc-bootstrap` 仍公开这些旧入口；计划规定的搜索命令会在生产路径命中。
- `apps/daemon` 目前没有直接依赖 `uc-engine`，而是通过 `uc-bootstrap` 的旧装配入口建立进程内核心；因此 Phase 2 的下一项确实是生产迁移，不是单纯同步计划状态。
- daemon HTTP 与后台任务当前直接调用 `AppFacade` 的范围远大于 `uc-engine::Operation` 已覆盖的动作，包含设置、加密状态、标签、诊断、升级、移动 LAN、配对和在线状态等；不能把 HTTP 或桌面专属对象塞进核心来规避迁移。
- 完整迁移必须按业务动作扩充稳定核心契约，并让 HTTP/后台任务依赖该契约；简单把旧 `AppFacade` 藏在新类型后面仍违反“外部宿主不接触 facade”的完成标准。
- CLI 的常规业务流程已经连接 daemon；旧 `CliAppRuntime` 主要服务 `dev-tools` 下的诊断、种子、文件捕获、邀请和移动调试命令，不能简单删除而改变既有开发能力。
- `uc-engine` 当前生产运行时内部已拥有唯一 `AppFacade`，但公开契约仅提供计划列出的核心操作；迁移难点是让 daemon/诊断调用逐步改用稳定操作，而不是再建第二套运行时。
- ADR-005 明确规定 `uc-engine` 不能依赖 `uc-webserver` 或任何 daemon 包，稳定边界类型必须由 `uc-engine` 自己拥有；因此合法方向只能是 webserver/daemon 将请求映射到 `Operation`，不能反向让核心接管 HTTP。
- ADR-005 还明确 CLI 最终应统一走 daemon client；开发命令不应继续作为保留 `CliAppRuntime` 的理由，后续应补齐缺失的 daemon 诊断端点或直接映射稳定操作。
- daemon 当前共有五个对外字段直接暴露 `AppFacade` 或 `AppDeps`，分布在装配、运行循环和启动恢复输入结构中；第一条边界测试将精确锁定这五处。
- CLI 剩余 in-process 路径横跨原始 blob、系统剪贴板探针、指定地址邀请和移动 LAN 调试；其中移动 LAN 明确属于桌面外壳，不能通过扩充 `uc-engine` 来迁移，需后续分别改走 daemon 诊断端点或独立宿主能力。
- daemon `run_loop` 当前同时负责核心关闭顺序、桌面升级检测和启动恢复，职责混合；升级检测与启动恢复应在桌面启动层触发，核心运行循环只保留运行与关闭顺序。
- 启动工作必须继续在 `tokio::spawn` 的主任务内执行；若在 `start_in_process` 返回前直接等待升级检测，会改变句柄返回时序，虽不改变 HTTP 就绪顺序但会扩大调用方阻塞面。
- 按 `codebase-design` 的 seam 纪律，不应为 daemon 再建一个只有旧 facade adapter 的新 trait；当前已有 `DaemonProcessRuntime` 模块，问题是其接口过浅、直接暴露 `app_facade()`，应通过加深该模块收拢启动装配。
- 目标形态是调用方只要求进程运行时启动 daemon 并取得句柄，`AppFacade`、持久依赖和任务登记留在模块实现内；最终可在同一模块内把实现替换为 `Engine`，调用方无需再次迁移。
- `ProcessRuntimeHandles` 和 `start_in_process` 当前仍从 `uc-daemon` 库公开，但全仓没有外部代码调用；它们暴露 `WiredDependencies` 和进程内装配能力，属于可以立即删除的旧对外入口。
- daemon 库还公开 `build_process_runtime` 和 `ProcessRuntimeContext`，同样没有外部调用，并直接暴露完整 `WiredDependencies`/后台依赖；它们属于同一批旧组合根入口。
- `DaemonProcessRuntime::task_registry()` 只有 host 一个调用点，调用方取出登记器后又调用 bootstrap 启动 blob 任务；这段任务所有权应由 runtime 自己封装，删除模块后才会把复杂度重新摊回 host，符合深模块的删除测试。
- `DaemonProcessRuntime::app_facade()` 同样只有 host 一个调用点；可先用高层 `start` 动作封装现有内部启动实现，明确后续在该模块内替换为 `Engine`，同时立即阻止调用方继续依赖 facade。
- 初版 `DaemonProcessRuntime::start` 回调 `host::start_in_process` 造成模块双向依赖；提交前已将完整启动实现迁入运行时私有文件，并把旧 helper 改名为内部 `start_runtime`，依赖恢复单向。
- daemon 启动恢复当前复制了四步编排：按宿主策略决定是否读安全存储、解锁加密会话、恢复空间与在线状态、恢复接收门禁；这应成为稳定的跨平台核心操作，而不是 desktop 特例。
- 合适的稳定输入只有 `allow_secure_storage_unlock`；宿主负责根据 attended/unattended 等平台事实决定该布尔值，核心不需要知道 daemon、GUI 或移动生命周期概念。
- 直接在 daemon 旁启动 `Engine::start` 会装配第二份数据库和 P2P 节点，违反当前进程级单实例约束；启动恢复迁移必须复用现有 daemon 的同一份 facade 与接收生命周期。
- 三种 `DaemonRunMode` 的 `auto_triggers_deferred_services()` 当前都为真，因此用统一 `RecoverSession` 实现接管接收恢复不会改变任何现有运行模式。
- 本切片采用替换而非叠加：把 `RecoverSession` 的内部编排提成 `uc-engine` 唯一实现，正式 `Engine::execute` 与 daemon 迁移桥共同调用；daemon 只保留是否允许访问安全存储的宿主策略，并删除旧解锁 helper 与空间恢复步骤。
- 稳定结果应只报告 `unlocked` 与 `resumed`，详细失败进入脱敏日志并映射为稳定错误；恢复成功时核心还应恢复搜索与接收能力。
- 2026-07-21 第三次接续复查确认：恢复迁移提交后，daemon 内部仍有多处 `AppFacade`/`AppDeps` 生产依赖，CLI 的旧 `CliAppRuntime` 也仍只存在于开发工具路径；Plan 003 不能更新为完成。
- 下一迁移切片必须复用 daemon 当前已经装配的同一份数据库、节点和 facade；若为调用公开 `Engine` 再执行一次 `Engine::start`，会违反进程级单实例约束。
- `Engine::start` 当前始终自行完成宿主适配、数据库和节点装配，没有从 daemon 现有运行实例接管的生产构造路径；因此不能为了形式上调用 `Engine::execute` 而制造第二份运行时。
- 下一条可控的生产迁移是 `/clipboard/resend`：公开核心已有 `Operation::ResendEntry`，daemon client 与桌面界面都实际使用该端点，可先把重发业务判断收敛到 `uc-engine`，让 HTTP 层只做请求和结果映射。
- 深读响应契约后否决立即迁移 `/clipboard/resend`：现有 HTTP 响应包含接受、重复、离线、失败、等待等逐类统计，而 `OperationResult::EntryResent` 只保留条目标识；直接切换会造成可见行为回退。
- `/v2/setup/issue-invitation` 是更窄的下一切片：核心操作已经存在，只缺服务端权威到期时间；补齐该稳定结果后，HTTP 层可以无损改用核心实现，并保持原有状态码和响应字段。
- 第四次接续比较确认：`/v2/setup/initialize` 接受可缺省设备名并会回退到已保存设置，而公开 `CreateSpaceInput` 强制要求字符串；结果还缺本机设备标识和指纹，不能直接无损迁移。
- `/v2/setup/redeem` 同样依赖已保存设备名，且现有响应包含双方设备标识和指纹，而 `SpaceJoined` 只返回空间标识；它比创建路径缺口更大，暂不作为下一切片。
- passphrase 解锁是可迁移的下一切片，但公开 `SpaceUnlocked` 目前缺少 HTTP 响应需要的空间标识。
- 当前核心把“尚未完成设置”和“本机没有空间材料”合并为同一错误编号，而现有 HTTP 契约分别返回两个稳定错误码；迁移前必须让核心错误编号保持可区分。
- 核心解锁已经负责搜索恢复和接收放行；HTTP 的 `on_session_ready` 也做同样工作。迁移时必须把 WS 广播与业务恢复拆开，避免成功后重复执行。
- daemon 注入 `DaemonApiState.receive_readiness` 的实际对象是协调器：一次 `ensure_receive_ready` 会恢复接收、打开剪贴板门禁并唤醒延迟服务。因此核心桥调用该 port 一次即可保留宿主行为，HTTP 成功路径只需广播 WS 事件。
- 静默恢复迁移还需要区分两类稳定失败：安全存储/空间恢复原本返回 500，接收恢复原本返回 503；当前 `execute_recover_session` 把两者都映射为 1214，直接接入会改变 HTTP 状态。
- Plan 001 已完成。
- Plan 002 的本地构建与生命周期修复已完成，Android 和 HarmonyOS 真机证据仍缺失。
- Plan 003 已完成公开契约、生命周期协调器、宿主能力接口和应用层依赖清理。
- `Engine::start` 已能从宿主能力建立真实持久依赖、完整节点和后台任务，并支持同一实例暂停后重建会话。
- 下一处明确边界是将桌面路径、安全存储、剪贴板、日志和分析准备移出纯核心装配。
- 当前 `wiring/wire.rs` 同时拥有桌面默认目录解析、安全存储准备、桌面剪贴板选择、分析/日志准备和纯核心装配。
- `wire_dependencies` 的生产调用点只有 daemon 与 CLI；对外顶层导出可以保持名称不变，避免本次纯移动扩大影响。
- 纯装配中还残留了 `UC_PROFILE` 驱动的网络数据目录计算；它也必须由桌面模块先准备再传入。
- 旧身份目录仍被配置迁移和兼容字段引用，但当前身份的读写已由安全存储承担。
- `SyncEngineDeps` 中的旧身份目录字段没有任何消费者，仍宣称身份由普通文件保存；已确认可以删除。
- `uc-engine` 当前只依赖通用异步与序列化库，不依赖任何现有核心实现；真实组装仍全部在 `uc-bootstrap`。
- daemon 的启动分为进程级依赖、后台文件处理、网络节点与业务 facade、HTTP 服务四层，目前分别散在 `uc-bootstrap` 和 `apps/daemon`。
- 让 `Engine::start` 直接调用 `uc-bootstrap` 会造成依赖方向相反，无法让 `uc-bootstrap` 最终成为调用 `uc-engine` 的桌面宿主。
- 下一步必须先建立宿主能力到现有内部接口的适配，并逐步把纯组装移入 `uc-engine`，不能用旧入口包一层冒充完成。
- 架构记录明确禁止 `uc-engine` 依赖任何具体平台包，只允许依赖核心、应用层和基础设施层。
- 用户主动重发在应用层已完整实现，并已由 `AppFacade` 暴露；Plan 003 缺的是 `Operation::ResendEntry` 到该入口的映射，不是新建用例。
- 身份从旧文件迁入安全存储的包装器目前位于 `uc-platform`，会阻挡纯组装移入 `uc-engine`；它们本身是通用文件/存储逻辑，应归基础设施层。
- `entrypoint/non_gui.rs` 的前半段只组装通用业务入口，应由 `uc-engine` 拥有；后半段的旧命令行运行时和桌面环境变量解析仍属于 `uc-bootstrap`。
- 交接建议称 `build_app_facade_from_deps` 会在未提供移动入站处理器时创建 fallback，但当前实现明确把 `None` 留给 daemon 后续装入完整能力；命令行仍需显式构造 fallback，迁移时必须保留这一区别。
- `Engine` 目前只有测试构造器，生产侧没有 `start`，其运行时接口也尚未连接真实组装。
- `HostCapabilities` 当前只包含三类目录、安全存储、剪贴板和文件访问；真实核心组装还需要从这些能力派生完整路径，并注入通用分析观察者与宿主事件转发，不能在 `Engine::start` 内回退到桌面默认值。
- 现有 `CoreWiringInputs` 已是生产启动所需的最窄组装输入，可作为宿主适配的目标，避免重新复制数据库、加密和搜索装配。
- 宿主安全存储可以直接适配到现有核心接口，并按稳定错误类别映射且不携带平台详情。
- 宿主剪贴板的内存表示可无损转换；文件句柄不能转换成本机路径，也不能先写明文临时文件。文件句柄必须由后续操作层通过 `HostFileAccess` 流式读取，或引入受加密约束的专用物化路径。
- 产品分析的具体组装已在早前提交中移到桌面宿主；统一核心公共边界不应接收产品分析对象，核心组装可使用通用空观察实现，宿主状态与进度走 `EventStream`。
- daemon 运行时装配混合两类职责：完整节点、入站应用和文件清理属于核心；系统剪贴板监听、HTTP/WS 事件与移动 LAN listener 属于桌面外壳。统一运行时只能迁入前者。
- `AppFacade` 已覆盖创建、邀请、加入、成员、发送、重发和搜索，但公开操作中的解锁、历史分页与显式导出还需要对齐现有子入口，不能先用“不可用”占位冒充完成。
- 持久依赖应跨暂停保留，P2P 节点和会话级 `AppFacade` 应在暂停时释放、恢复时重新创建，避免复用其中的单次安装状态。
- 后台 blob worker 原先登记在统一任务管理器中却忽略取消信号，会让关闭必然耗尽期限；现在已改为收到取消后退出。
- `ClipboardHostEvent::NewContent` 只携带刷新提示和占位预览，不能构造完整 `EntrySummary`；核心事件流应发送重新查询通知，不能伪造内容类型或时间。
- 空间创建已有顶层应用入口；空间解锁原先只停留在子入口，统一操作层需要先补顶层转发再映射。
- 创建空间的口令不一致属于输入错误，重复创建属于冲突；解锁口令错误属于未授权，不能全部压成内部错误。
- 签发邀请可以直接复用顶层应用入口，公开结果只需要邀请内容；有效期仍留在内部流程管理。
- `JoinSpace` 必须根据设备是否已完成设置选择首次加入或保留历史的空间切换，不能无条件调用首次加入入口。
- 完整生产节点当前不能在同一进程并存两个实例；接口测试不能用双生产实例做配对，真正双端握手继续由专用双端装配测试验证。
- 加入操作需要先保存宿主传入的设备名，因为现有握手从设置读取本机名称；随后按设置完成状态选择首次加入或空间切换。
- `QueryHistory`、`SendText`、`SendImage`、`ResendEntry` 和 `ExportEntry` 已接入真实核心；`SendFiles` 仍未开放。
- 现有文件和大图片的 iroh 传输缓存会保存原文。Plan 003 同时禁止改变 P2P 文件格式，因此在“保留旧格式”的决定下不能安全实现 `SendFiles`。
- 新核心的文本和图片发送已限制为最多 64 KiB，超出时在写历史和进入文件传输缓存前拒绝，避免扩大既有明文缓存问题。
- 锁定状态启动后再执行 `UnlockSpace` 现在会重新评估搜索索引；真实关闭、锁定重启、解锁和关键字查询流程已验证恢复成功。
- 中文跨平台核心接口文档已加入 `docs/README.md`，明确操作、生命周期、错误、宿主能力和文件限制。
- daemon、CLI 和 Tauri 的生产调用仍包含旧 `AppFacade` / `CliAppRuntime` 路径；在文件发送仍依赖旧入口时，Plan 003 禁止保留双轨的门禁尚未满足。
- Plan 002 的 `iroh-blobs` 已固定为 `UniClipboard/iroh-blobs` 的提交 `b33af91e8a4bd189cc0f954fc6584feb5ffd1823`，`Cargo.lock` 记录同一 Git 来源。
- 已新增生产形态连续十次启动、锁定解锁和关闭验收；十轮均能重新占用节点，安全存储中的网络身份保持完全一致。
- 已新增通用明文扫描器，支持同时扫描多个目录、二进制精确匹配、文件类型汇总和不泄露探针正文的失败报告；仍需接入真实核心数据与四平台设备导出目录。
- 桌面真实核心已用同一运行时探针发送文本和图片，关闭后扫描私有目录、缓存目录和临时目录通过；文件、标签、预览、平台日志和三种移动设备导出仍未覆盖。
- `.github/workflows/build-mobile-core.yml` 当前仍围绕旧 `uc-mobile` 产物，尚不能作为 Plan 004 新统一核心绑定的交付证据。
- 当前 PR 的 Rust 门禁只运行工作区编译检查，不运行 `uc-engine` 测试；新增十轮启停与明文探针若不单独接入 CI，不会成为持续门禁。
- 文档树中没有现成的明文探针操作说明；需要补充探针生成、目录导出、退出码和当前自动覆盖范围。
- PR 检查现已增加完整 `uc-engine` 测试门禁；配置通过本地 YAML 解析，当前环境缺少 `actionlint`。
- Plan 002 已只勾选有真实自动证据的“连续启停十次”；明文探针仍明确保留文件、标签、预览、日志和移动真机缺口。
- iOS 与 Android 的 `uc-engine` 依赖树都解析为 `iroh 1.0.0-rc.1` 和同一 `iroh-blobs 0.102.0` Git 提交 `b33af91e...`。
- 当前 Rust 工具链没有安装 HarmonyOS/OpenHarmony 目标，仓库内也未找到对应目标配置或构建脚本；不能用本机结果声称 HarmonyOS 构建完成。
- 从提交 `1c48d7e37` 生成的独立 `git archive` 检出在无父仓库、无子模块条件下成功执行 `cargo metadata --locked --format-version 1`，定制网络依赖来源可复现。
- `SearchCoordinator` 有五处使用不可取消、不可等待的 `spawn_supervised`：手动重建、启动重建、残留清理、损坏行修复和重建进度转发；其文档明确说明返回句柄不能终止真实工作。
- 核心关闭会等待登记在 `TaskRegistry` 的任务，但上述搜索子任务不在其中，因此十轮重启通过仍不能证明所有搜索后台工作都已结束。
- `tokio_util::task::TaskTracker` 已随现有依赖可用，适合让协调器同步登记短任务、关闭时配合 `CancellationToken` 取消并等待，无需把核心任务管理器反向暴露给应用层。
- 搜索协调器现已用私有 `TaskTracker` 和取消信号统一拥有手动重建、启动重建、残留清理和损坏行修复；重建进度转发改为与重建同一任务等待，不再独立存活。
- 启动检查本身也必须与取消信号竞争；否则手动重建先占锁时，关闭会卡在启动检查。第二条阻塞测试已固定这一竞态。
- `TaskTracker::close` 只允许等待结束，不阻止之后继续登记任务；搜索协调器若在关闭与新请求并发时没有额外门禁，仍可能在关闭返回后短暂创建后台任务。
- 命令行使用的同步搜索重建也必须登记到同一生命周期范围；否则关闭后的实例仍能重新执行工作，关闭并不是完整的停止边界。
- 当前 `adb devices -l` 没有连接设备；物理 iPhone 在 `xcrun devicectl list devices` 中处于不可用状态，`idevice_id -l` 也没有返回设备；`hdc` 未安装。
- 文本探针本来会生成预览并进入搜索投影，但旧测试没有直接断言预览含探针；日志目录也没有接入自动扫描。
- 文件发布用例实际把文件原文直接交给 iroh-blobs，接收用例也明确把拉取结果当作原文；这与 `BlobTransferPort` 的“只接收密文”约束和根安全红线冲突。
- 直接在发布前加密会让旧客户端把密文当文件原文，破坏“保留旧格式”的兼容要求。
- `iroh_blobs::BlobsProtocol` 只接收其固定 `Store` 客户端；生产 `FsStore` 把原字节保存到磁盘或引用原文件，没有可由当前仓库注入的透明加密适配接口。内存仓不适合任意大小文件。
- 在保持旧网络字节的同时实现落盘密文，需要修改所固定的 iroh-blobs fork 内部文件格式、分块加密和随机读取实现，不是当前 `BlobTransferPort` 外层能完成的替换；该设计阻断已被用户“文件落盘不加密”的最新决定解除。
- 物理 iPhone 16 Pro（`6F7C086F-2617-515D-9E7A-4D343B3A642A`）在交接检查时已配对且可用，但本轮 `xcrun devicectl list devices` 又显示为 `unavailable`；必须恢复并重新确认实时状态后才能开始真机验收。
- 旧记录中的 `/Users/mark/MyProjects/uniclipboard-android` 当前不存在；移动宿主搜索先以 `/Users/mark/MyProjects/uni/mobile` 和本工作区为准。
- `swiftui` 工作流引用的 `macos-apps/references/cli-workflow.md` 和 `cli-observability.md` 在本机不存在；真机操作改用 `xcodebuild`、`xcrun devicectl` 和系统日志工具。
- `xcrun devicectl device info details` 显示 iPhone 仍为已配对、开发者模式已开启，系统版本 27.0，但 `xctrace` 把它列在离线设备中，USB 列表也没有发现它；当前不能安装应用。
- 随后实时复查显示同一台 iPhone 已恢复为 `available (paired)`，`xctrace` 也将它列为在线物理设备；当前可以进入构建、安装和启动验证。
- 新核心真机宿主已成功构建、签名并安装，设备应用列表显示 `Engine Probe 0.1.0`；首次启动被系统以设备锁定明确拒绝，需要解锁后重试。
- `/Users/mark/MyProjects/uni/mobile` 有可构建的 iOS 工程和 `UniClipboardCore.xcframework`，但构建脚本、Swift 绑定和产品说明都明确指向旧 `uc-mobile`；正式计划也明确该入口是 LAN HTTP 客户端，不能作为完整 P2P 新核心的真机证据。
- 当前工作区只发现 `crates/uc-mobile/scripts/run-ios-demo.sh` 和旧 XCFramework 构建脚本，没有现成嵌入 `uc-engine` 的 Xcode 真机宿主。

## Technical Decisions

### 2026-07-20 提交分组

- `fix(engine)`: 空间创建、加入、解锁成功前恢复接收能力。
- `fix(application)`: 托管文件缓存改用匿名编号，并清除接收日志中的文件名。
- `fix(application)`: 非文件传输内容在进入持久传输仓前加密，文件正文继续走已批准的原始字节例外。
- `fix(infra)`: 原子入站提交在落库前加密内联内容。
- `fix(infra)`: 传输适配日志不再记录源路径。
- `fix(cli)`: 分别补齐移动同步 resurfaced 结果和文件导入持久化错误的显示。
- `test(ios)`: 增加直接嵌入统一核心的真机探针和构建入口。

| Decision | Rationale |
|----------|-----------|
| `uc-bootstrap` 保留桌面宿主准备，纯装配逐步移入 `uc-engine` | 平台差异只应停留在宿主层，新入口必须拥有核心运行时 |
| 本次先建立 `wiring::desktop`，保持顶层 `wire_dependencies` 兼容 | 先明确内部所有权，再在后续生产 `Engine` 迁移中删除旧公开入口 |
| P2P 身份只存安全存储，旧文件仅作一次迁移来源 | 保持身份不变并避免密钥材料落入普通文件 |
| 事件落后时显式要求重新查询 | 有界队列不能静默丢失状态 |
| 先完成不依赖文件格式升级的 Plan 002 本地门禁 | 可以继续收集十轮启停、依赖一致性和明文探针证据，同时不违反 Plan 003 的协议禁改要求 |

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Android 设备未连接 | 保留相关计划为未完成 |
| `hdc` 不可用 | 保留 HarmonyOS 真机验收为未完成 |

## Resources

- `plans/README.md`
- `plans/002-prove-four-platform-full-node.md`
- `plans/003-introduce-unified-core-interface.md`
- `plans/004-ship-mobile-bindings-and-conformance.md`
- `plans/005-extract-single-core-repository.md`
- `VISION.md`

## 2026-07-21 Phase 2 第七次接续发现

- `/device/me` 返回的是跨平台稳定身份信息，不是桌面连接诊断，因此应由核心拥有；连接地址、通道和 peer 诊断仍留在桌面外壳。
- 本机设备查询现已由核心统一读取和规范化，公开结果只包含设备编号和显示名；调试输出只记录显示名是否存在。
- 当前 webserver 仍需借已运行的旧装配调用核心内部实现，原因是 daemon 尚未持有唯一 `Engine`；这只是避免并排启动两套数据库、P2P 节点和后台任务的迁移接入点，最终所有权切换后必须删除。
- 设备查询切片提交后工作区干净；剩余 `app_facade_or_error` 命中集中在加密、配对、成员、搜索、历史、blob、生命周期和 WebSocket 等核心调用面。
- 加密状态、主动锁定和安全存储访问属于跨平台核心；HTTP 路径仍可沿用 `keychain-access` 名称，但公开核心使用跨平台的安全存储名称。
- 主动锁定不能只清除密钥，还必须同步关闭接收入口，否则锁定后仍可能接受需要加密落库的业务数据。
- 加密接口剩余的彻底重置不是普通锁定：它还清除持久密钥、设置完成状态和待处理邀请，应保持一个完整核心动作。
- `ResetSpace` 与 `FactoryResetSpace` 必须长期保持两个明确操作：前者保留密钥供恢复，后者先擦除密钥再清设置，不能用布尔参数合并成含糊入口。
- 彻底重置成功后关闭接收入口；密钥擦除失败时保持原设置状态，避免用户被导向必然再次失败的初始化流程。
- `crates/uc-webserver/src/api/encryption.rs` 现除取得迁移期运行对象外，所有加密业务动作都通过核心单一实现；下一步可转向 `member.rs`。
- 成员偏好查询和更新共享同一种稳定结果，避免为同一字段形状维护两个公开结果；更新输入保留每个字段的可选状态，保证局部更新语义。
- `RemoveMember` 只返回完成，不向宿主暴露成员、信任和地址三类记录；核心内部继续由现有成员能力保证级联清理顺序。
- `ListDevices` 已从运行时内联实现收敛进成员模块，并获得与其他成员动作一致的稳定错误分类。
- `member.rs`、`pairing.rs` 现只负责 HTTP 字段和状态映射，不再直接取得 `MemberRosterFacade`。
- 搜索统一接口必须完整保留预览、文件名、文件路径、链接和自定义标签供正常渲染，但这些字段全部从 `Debug` 和 tracing 中排除。
- webserver 原先把完整搜索查询词写入 tracing；迁移时已改成只记录是否存在查询词，避免用户内容进入日志。
- `search.rs` 现只负责 URL 参数、HTTP 状态和 DTO 映射；查询、标签、状态、重建及稳定错误分类由核心拥有。
- 搜索能力还有 WebSocket 初始快照消费者；现在同样复用 `QuerySearchStatus` 的核心实现，失败日志只记录稳定编号和类别。
- 历史相关调用面比搜索更分散，下一步必须区分已有 `QueryHistory`、`ExportEntry`、`ResendEntry` 与桌面 HTTP 所需的详情、删除、收藏、恢复、统计和投递视图，不能重复定义相同语义。

## 2026-07-21 Phase 2 第八次接续后续发现

- 三种恢复模式已能由一个核心操作表达；内容丢失与模式不适用属于稳定业务结果，记录不存在与内部失败继续使用稳定错误，桌面 400、404、410 和 500 形状均可无损保留。
- 剩余剪贴板调用应继续拆成四组：投递视图、重发、文本发送、二进制资源读取。投递视图只有完整结果、记录不存在和存储失败，可先独立迁移。
- 重发不能沿用当前过度简化的 `EntryResent` 结果；桌面接口需要保留接受、重复、离线、失败和待处理的逐类统计，以及多种结构化 4xx 失败。
- 文本发送需要保留逐目标结果，不能只返回新记录编号；blob、缩略图和文件读取还要保留不同的未找到提示、媒体类型和并发内存保护。

## 2026-07-21 第九次接续发现

- 重发迁移完成后，后台真正缺少稳定核心结果的调用面收敛为文本发送以及 blob、缩略图、文件资源三类二进制读取；其他大量 `app_facade_or_error` 命中主要是已经迁移业务仍借用旧运行对象的临时接入点。
- 现有桌面文本发送返回快照摘要、时间、接受/重复/离线/失败总数和逐目标结果；当前 `EntrySent` 只有记录编号，直接替换会丢失用户可见信息。
- 核心现有发送路径会先写入加密历史、更新搜索并记录投递状态，桌面旧接口则只直接投递；不能为迁移另建一套并行发送流程。应先扩充核心发送结果，资源结果补齐后再随 daemon 唯一所有权切换让 HTTP 直接执行正式 `Engine`。
- `ClipboardOutboundOutcome` 已经取得完整底层 `DispatchEntryOutcome`，但当前只保留五类总数和 blob 数量；可以在不重复网络发送的前提下向上保留快照摘要、时间和逐目标结果。
- 三类二进制读取必须保留各自媒体类型；文件读取还要保留经清理的下载文件名。blob 与文件读取继续受 daemon 现有四并发内存门禁约束，缩略图保持不占该门禁。
- daemon 所有权切换的下一真实前置是桌面 `HostCapabilities`：当前生产代码没有为 `Engine::start` 构造桌面目录、安全存储、系统剪贴板和文件句柄能力的入口，直接替换会失去系统剪贴板与旧身份迁移。
- 正确 seam 在 `uc-bootstrap` 的桌面宿主准备层：它只解析桌面路径、应用待导入配置、建立安全存储迁移包装和系统剪贴板适配，然后把 `HostCapabilities` 交给 `Engine::start`；不得在这里再调用 `wire_dependencies`。
- 当前 `HostClipboard` 仍缺变化通知，`HostFileAccess` 也没有桌面句柄注册表。桌面所有权最终切换前还必须让 daemon 的平台监听器只触发核心捕获，并为显式文件导入/导出提供真实句柄管理，不能用本机路径冒充句柄。
- 宿主文件剪贴板表示还必须携带原始格式分类；仅保留媒体类型会让文件在跨平台边界后失去原有分类。核心通过宿主文件能力分块读取到匿名临时文件，并另加受加密持久化保护的显示名元数据，既保留用户可见文件名又不把名称写进临时路径。
- 宿主文件剪贴板临时目录在核心启动时清除上次异常退出的残留，并在正常关闭时删除；文件内容本身仍按仓库允许的原始字节规则处理，文件名不明文落盘。
- 剪贴板变化通知应是单消费者信号流，不携带业务快照；桌面宿主把平台监听器已经读出的快照暂存一次，核心收到信号后优先取走它。这样既不向公开边界暴露平台类型，也不会在 Wayland 上因重新读取而丢失一次性数据源。
- 本机变化的来源判断、加密就绪门禁、捕获、活动状态推进、刷新事件、搜索和发送现可由核心同一流程拥有；发送由核心会话任务登记并可在暂停或关闭时取消，监听循环不会被慢对端阻塞。

## 2026-07-21 Phase 2 历史维护迁移复核

- `CleanupWorker` 仍由 daemon 的旧服务装配启动，而 `Engine` 尚未成为 daemon 唯一运行对象；此时直接删除 worker 会让生产历史维护停止。
- 让 `CleanupWorker` 转调 `uc-engine` 虽可在迁移中保持行为，但会迫使核心暴露仅供旧装配使用的过渡入口，并把临时结构写进正式门禁。
- 更符合 Plan 003 的做法是把历史维护所有权与 daemon 的 `Engine` 所有权切换放在同一迁移切片：核心登记维护任务，daemon 同时删除旧 worker 及其装配，不提交转发外壳。
- 当前未提交测试要求 `cleanup.rs` 保留 `run_history_maintenance` 调用，固化了不必要的过渡形态；正式实现前应将门禁改为验证核心拥有任务、daemon 不再装配旧 worker。

## 2026-07-21 Phase 2 daemon 最终所有权审计

- daemon 启动链仍从 `process_runtime_start` 组装完整 `AppFacade`，再把生命周期能力装入同一对象；`DaemonApp`、服务装配、配对状态监控和 API 状态都继续持有它。
- webserver 多数已迁移处理函数复用的是 `uc-engine::internal` 的单一业务实现，但仍从 `DaemonApiState::app_facade_or_error()` 取得内部依赖，并未直接调用生产 `Engine::execute`。
- 最终切换不能只删除清理、保活和文件超时工作者；必须先让 daemon 状态持有唯一 `Engine`，将 API、事件和桌面兼容职责分流后，才能删除旧 `AppFacade` 装配而不丢行为。
- 历史维护门禁应验证最终所有权，但实现切片必须包含足够的 daemon 所有权切换，避免新核心任务与旧核心任务并行运行。
- `DaemonApiState` 目前有四十余处通过 `app_facade_or_error()` 取得旧对象；多数处理函数已经有一一对应的 `Operation`，可直接改为共享同一个 `Arc<Engine>` 并调用 `execute`。
- `DaemonApiState` 还从旧对象派生配置迁移、诊断、移动 LAN、设置和升级能力，并直接实现 peer 连接诊断；这些是桌面外壳职责，切换时必须作为明确的桌面服务输入保留，不能为了删 `AppFacade` 把它们误塞进公共核心接口。
- 核心业务调用与桌面专属服务应分批迁移：前者直接使用 `Engine`，后者拆成显式宿主依赖；最终状态不再有可取回整个 `AppFacade` 的方法。
- daemon 当前最外层 `host::run` 先调用 `wire_dependencies`，自行启动 blob 任务、P2P 会话和旧 worker；`Engine::start` 会再次完成这些核心装配，因此两者不能在生产路径中并排启动。
- 正确切换点是 `host::run`：先保留实例锁与桌面进程准备，再通过 `prepare_desktop_engine_host` 构造宿主能力并只启动一个 `Engine`；退出时由同一运行任务调用 `Engine::shutdown` 后再释放实例锁。
- `Engine` 已在会话内拥有搜索、剪贴板入站/出站、文件超时和 P2P 关闭顺序，daemon 旧 `search-coordinator`、clipboard worker、file-sync worker 与显式 `SyncEngineAssembly::shutdown` 均是待删除的重复所有权。
- webserver 剩余非核心能力集中为配置迁移、诊断、移动 LAN 兼容、设置、升级和连接诊断；其余四十余处旧对象取得点均已有稳定核心操作可替换。
- 这些桌面能力目前都从旧 `AppFacade` 派生，部分共享设置和持久化接口；切换设计必须证明它们能作为明确的桌面服务与唯一核心共存，不能重复打开完整核心或把桌面功能扩进跨平台公共操作。
- ADR-005 明确公共启动只能返回 `Engine` 与 `EventStream`，宿主能力只允许目录、安全存储、剪贴板、文件句柄和生命周期通知；不能新增返回内部服务包的桌面专用启动入口。
- 同一 ADR 同时禁止把 LAN HTTP 兼容实现放进核心，因此后续应把通用业务动作补成稳定 `Operation`，而 HTTP listener、产品分析等纯桌面机制继续由外壳持有；不得以导出内部 facade 的方式共享数据库。
- `PeerKeepAliveWorker` 的退避状态机、已配对设备查询、可达性拨号和 presence 订阅都不依赖桌面系统能力，属于统一 P2P 核心会话职责。
- 旧实现收到 Online 事件后通过 `uc_observability::spawn_supervised` 启动不受 daemon 服务或核心任务表管理的即时拨号；迁入核心时必须改为父保活任务拥有的子任务集合，关闭时统一取消。
- 现有保活规则需原样保留：25 秒基础周期、失败后 60 秒与 5 分钟退避、第三次失败休眠、30 分钟兜底探测、Online 事件唤醒并立即预热出站路径。
- daemon 的 peer HTTP/WS 需要两项跨平台核心能力：读取当前 peer 快照，以及主动刷新全部 peer 可达性；二者都已有 `AppFacade` 单一实现，可映射为稳定操作而不重复业务逻辑。
- peer 快照必须保留连接通道、实际连接地址、候选地址、配对状态和在线状态，但调试输出只能暴露条目数量；主动刷新结果只需总数、在线、离线和错误数量，不应暴露逐设备错误。
- daemon 生命周期接口只读取旧生命周期状态并执行“接收准备 + retry_to_ready”；最终可由核心生命周期状态与现有宿主接收准备协调替代，不需要长期保留整个 `LifecycleFacade`。
- 设置查询、局部更新和 relay 探测都通过 `SettingsFacade`；设置同时驱动核心同步、保留和网络规则，因此必须由唯一核心持久化，不能由 desktop 另开一套设置仓。
- HTTP 更新设置后的 telemetry/analytics 运行时 gate 属于 desktop 外壳副作用，应继续留在 webserver；核心操作只负责验证、持久化并返回稳定结果。
- 设置稳定边界需要完整覆盖当前 HTTP 字段：通用、同步、保留、安全、配对、快捷键、文件、网络和快捷面板；时长在边界统一使用秒，`Option<Option<T>>` 的“不改/清空/设置”三态必须保留。
- 更新校验失败需要保留用户可见原因，但不能放进 `EngineError` 或调试输出；应作为结构化业务结果返回。relay 探测的 DNS/TLS/握手/超时等也属于正常分类结果，只有能力未装配才是稳定错误。
- 设置快照与 patch 可能含设备名、主题覆盖、语言、快捷键、自动保存目录和 relay 地址；这些公开类型不得派生会输出字段值的默认 `Debug`，`OperationResult` 调试只报告结果类别。
- 设置能力提交后复查网页服务确认：`DaemonApiState` 仍没有 `Engine`，现有所谓核心委托只是从旧运行对象取得 `AppFacade` 后调用 `uc-engine::internal` 函数。设置接口若要真正调用 `Engine::execute`，必须先让 daemon 生产装配拥有同一个核心实例；不能在网页状态旁再启动第二个核心。
- 配置导入导出和日志导出当前 HTTP 契约接收并返回本机路径，但跨平台核心公开边界已有 `HostFileHandle`。稳定核心动作应通过句柄分块读写，核心只在自己的临时目录处理文件，不能把路径继续带进公开接口。
- 升级检测只依赖核心装配内已有的版本游标和设置状态；当前版本已经是 `EngineConfig` 输入，应由核心保存并作为唯一检测版本，不应让每个调用方重复传版本字符串。

## 2026-07-21 Phase 2 daemon 单核心切换复核

- 配置迁移已作为独立核心能力提交，daemon 切换前所需的通用设置、升级、诊断、配置迁移和 peer 状态操作现已齐全。
- 旧入口不是迁移目标，也不需要新增兼容层；现有 `wire_dependencies`、`AppFacade` 和旧 worker 只是必须在切换提交中被替换的当前生产路径。
- `build_process_runtime` 当前调用 `wire_dependencies`，已经会启动完整旧核心；若只给 `DaemonApiState` 再加一个 `Engine`，就会并排运行两套数据库、P2P 节点和后台任务。
- 单核心切换必须从 daemon 进程最外层启动路径开始：用 `prepare_desktop_engine_host` 准备平台能力，只调用一次 `Engine::start`，把同一实例交给网页接口，并在退出时调用 `Engine::shutdown`。
- `CleanupWorker`、`PeerKeepAliveWorker` 和 `FileSyncOrchestratorWorker` 的职责现已由核心会话拥有，应随生产所有权切换删除装配，不能转发到核心形成新的旧入口。
- HTTP、运行时统计开关和移动 LAN listener 继续属于桌面外壳；它们应持有明确的最窄依赖，不能通过可取回整个 `AppFacade` 的接口继续访问核心内部。

## 2026-07-21 Phase 2 移动同步设置边界

- `SettingsPatch` 只覆盖通用、同步、保留、安全、配对、快捷键、文件、网络和快捷面板设置；移动兼容总开关与 LAN 监听开关不在通用设置契约内，不能借 `UpdateSettings` 偷渡。
- 现有移动设置用例已经是这组字段的唯一持久化规则，覆盖总开关、LAN 监听、广告 IP、完整广告地址和端口，并保留三态清空与输入校验。
- 核心应增加独立的移动同步设置查询与更新动作；更新只负责校验、持久化和返回落盘后的目标状态。LAN listener 的实际启停仍由桌面外壳执行，完成后再用现有 endpoint 更新动作把监听结果报告给核心。
- 真实登记流程必须先通过设置动作开启 `enabled` 和 `lan_listen_enabled`，再报告监听地址，最后执行登记；仅报告监听地址不能代替用户设置门禁。

## 2026-07-21 Phase 2 LAN 内容与流式上传边界

- 现有 `MobileSyncFacade` 已完整拥有内容可用性检查、最新同步文档、同步文档写入、文件读取和三段式流式上传；本切片只建立稳定核心操作与结果映射，不重写底层规则。
- 同步文档必须完整保留类型、正文、附件名、大小、兼容 hash 和稳定 content id 供协议响应使用，但这些用户内容与标识均不得进入调试输出。
- 文档写入必须保留已应用、重新激活、重复跳过、解码失败和等待附件五种业务结果；底层入站、编码和内部失败继续只返回稳定脱敏错误。
- 文件读取必须区分找到与未找到，找到时保留媒体类型和完整字节；调试输出只允许字节数和媒体类型是否存在。
- 流式上传的公开句柄必须由 `uc-engine` 自己拥有且保持不透明，不能公开 `uc-core::StagingHandle`、UUID 或本机路径。开始后允许串行追加，完成或放弃会消费句柄；重复、伪造或生命周期切换后的旧句柄必须拒绝。
- HTTP 请求体大小、媒体类型嗅探、传输进度和 listener 仍属于桌面外壳；核心操作负责 staging 和最终入站业务结果。暂停或关闭不能让未完成上传在恢复后继续。
- 提交前审阅发现公开上传编号直接使用了底层 `StagingHandle` 的 UUID 字符串，虽然类型不同，但序列化值仍泄露底层句柄，不满足“公开编号由 `uc-engine` 自己拥有”的约束。公开编号必须独立随机生成，只在核心内部映射到底层句柄。
- 当前追加操作在取得底层句柄后立即释放登记表锁，同一公开编号的并发追加可能同时进入底层未定义行为。每笔活动上传需要独立串行锁；完成、放弃和暂停清理必须先消费同一活动状态，使已经排队的迟到追加稳定返回编号失效。
- 完整核心套件中的剪贴板通知超时不是负载噪音。最小复现为同一进程先运行移动内容入站，再关闭该 `Engine` 并启动新实例：旧的进程级 `SelfWriteLedger` 仍保留 60 秒远端写入 fallback，导致新实例第一笔真实本地复制被误判为旧实例的远端回声并跳过。
- `SelfWriteLedger` 是单个核心实例的运行期状态，不是进程事实。暂停和恢复同一实例应继续共用；彻底关闭后新实例必须取得空 ledger。移除 `uc-infra` 的 `OnceLock`，由 `uc-engine` 每次装配独立创建，既修复测试污染，也满足反复启动的公开生命周期契约。

## 2026-07-21 Phase 2 daemon 最终切换事件审计

- Plan 003 明确要求 daemon 成为第一个只使用 `uc-engine` 的生产宿主，迁移后删除 `CliAppRuntime`、`AppFacade` 和旧装配的外部入口，不允许运行时双轨。
- 当前 `Operation` 已覆盖 loopback HTTP 的核心业务、设置、诊断、配置迁移、升级、移动兼容和 LAN 内容动作，网页路由可以统一改为调用同一 `Engine::execute`。
- 当前生产 `EngineEvent` 只实际转发传输进度和粗粒度刷新；`IncomingEntry` 尚无生产发送点。旧 daemon 仍独立产生完整剪贴板入站通知、传输终态、投递状态、peer 变化和搜索事件，CLI `watch` 依赖其中的 `clipboard.inbound_notice` 正文事件。
- 因此不能只把 `DaemonApiState` 字段机械替换为 `Arc<Engine>` 后删除 worker；必须先让稳定核心事件边界承载现有生产消费者真正需要的通知，再由 daemon 转为旧 WebSocket 形状。否则编译和三项源码门禁可能通过，但桌面与 CLI 行为会退化。
- `receive_readiness` 仍作为内部应用端口直接注入 `DaemonApiState`，并被 health、setup、unlock 和 lock 流程使用；核心操作已经在内部执行 readiness 协调，但 health 仍需一个稳定、非 port 的核心查询结果才能删除该泄漏。
- 2026-07-21：网页 API 的 `Arc<Engine>` 迁移在当前工作区已可通过 `cargo check -p uc-webserver`，只剩 5 个未使用项告警；下一实际阻断点已转为 `crates/uc-webserver/src/mobile_lan/` 和 daemon listener 仍持有 `MobileSyncFacade` / `FileTransferFacade`。旧入口不是兼容目标，只能在这些生产职责被同一 `Engine` 覆盖前短暂存在，切换时必须删除外部调用路径。

## 2026-07-21 Phase 2 移动 LAN 上传生命周期续作

- 本轮从磁盘恢复的真实状态：工作区有 42 个相关未提交文件；网页 API 与移动 LAN 请求已迁入共享 `Engine`，最近一次 `uc-webserver` 全套 149 项通过，移动 LAN 单核心门禁转绿。
- 当前唯一前置缺口是文件上传生命周期：开始、节流进度、成功和失败通知仍未完整归属核心，旧 HTTP 外壳仍承担部分传输关联职责；在补齐并验证前不能切换 daemon，也不能删除旧装配。
- 按仓库规则，该缺口属于结构性问题：同一上传生命周期状态由核心和 HTTP 外壳共同维护。修正必须让核心成为唯一权威来源，外壳只解析请求与映射响应，不能新增旧入口、并行状态或失败兜底。
- 隐私底线保持不变：文件内容可按原始字节暂存；文件名、路径、正文、标识和失败细节不得明文持久化或进入日志。生命周期事件只允许携带协议所需的脱敏标识与计数。
- 边界规则要求事件发布与状态存储分开，消费者只依赖所需的最小能力。本轮若需要扩展事件面，应从实际上传行为和现有消费者反推最小事件契约，不能把完整内部传输对象或底层存储接口暴露给 HTTP 层。
- Rust 异步事件不能静默吞错；上传失败必须对上层可观察，但日志只记录稳定类别和非敏感计数。网络接收循环也不能等待依赖同一循环推进的业务操作。
- 既有同仓库交付经验要求不能停在局部编译：当前切片完成后至少要覆盖核心、网页服务、daemon 构建和差异检查；提交按行为拆分，并以干净工作区作为本地阶段终点。
- 当前未提交路由已经删除旧 `FileTransferFacade` 的开始、进度、失败调用，并把设备编号、传输编号和总字节传给核心 `begin_upload`；但路由仍保留节流时钟却不报告进度，最终字节数也只存入未使用变量，说明生命周期迁移尚未完成。
- 旧流程会在开始时建立接收投影并报告进行中，每 250ms 报进度，超限、追加失败和请求体错误时报失败；当前核心路径仅追加、放弃和完成，必须用测试证明这四类可观察行为由核心完整接管。
- 公开开始输入已经包含文件显示名、媒体类型、来源设备、传输编号和可选总大小，具备由核心建立生命周期所需的信息；完成输入只需要核心上传句柄和最终媒体类型，说明这些上下文应由活动上传状态持有，不能由 HTTP 在完成时重复传入。
- 既有应用层入站流程会在同步文档真正应用后报告最终成功或失败，因此 PUT 完成只应报告字节已进入暂存区的最终进度，不能提前把整次内容接收标成完成。
- 核心运行时已经持有 `file_transfer_lifecycle` 和活动上传表，现有事件适配器会把进度转为 `EngineEvent::TransferProgress`、把状态变化转为刷新事件；无需新增事件通道或基础设施。
- 当前工作区处于接口先行的未完成状态：来源设备、传输编号、总字节已经从“完成输入”移到“开始输入”，但运行时仍从完成输入读取它们，活动状态也只保存暂存句柄和文件名。下一步应让失败测试固定“开始保存上下文、追加累计并上报、放弃报告失败”的行为，再补运行时。
- 目标状态流：开始时核心建立接收投影并报告进行中；活动状态保存来源设备、传输编号、总字节和累计字节；追加成功后由核心按既有 250ms 频率上报；结束暂存前补最终进度；追加、结束、显式放弃和生命周期清理失败时都由核心报告稳定失败。真正的完成状态仍由后续同步文档应用产生。
- 现有宿主事件映射足以验证行为：进行中/失败状态会产生刷新事件，进度会携带传输编号、已收字节和总字节。测试应从真实 `Engine` 事件流观察，不用替身验证内部调用。
- 事件流从核心启动起会保留状态变化，所以行为测试需要按条件读取并跳过无关事件，且设置超时，避免依赖事件先后数量或无限等待。
- 现有传输服务明确提供开始、进度、失败五类独立动作和接收投影写入；本轮只编排这些已有动作，不新增端口或复制状态规则。
- 深读现有发布器后发现，开始/失败状态在还没有真实历史记录编号时会被主动跳过；只有进度事件允许仅靠传输编号送给宿主。因此旧路由虽调用了开始和失败，早期状态并不一定真正到达界面。不能用“旧代码调用过”代替可观察行为证据。
- 本轮先用零字节进度作为开始的可观察信号，并让后续追加/结束进度证明生命周期已经成功启动。失败状态是否扩展为新的稳定核心事件属于 daemon 全事件边界的一部分，必须先核对现有消费者与测试，不能借本切片塞入空历史编号。
- 核心实现现已让每笔活动上传持有来源设备、传输编号、总字节、累计字节和节流时间；开始建立接收投影、启动生命周期并发零字节进度，追加在 250ms 窗口后上报，结束总会补最终进度，追加/进度/结束/放弃/暂停失败都走同一失败动作。
- HTTP 文件路由已删除节流时钟和最终进度计算；普通 PUT 与历史表单上传都只在开始时交付上下文，完成时只交句柄和最终媒体类型。
- 历史表单上传原本在四条日志中记录客户端文件名；已用门禁锁定并删除。日志现只保留字节计数、限制、媒体类型和稳定错误类别。
- 当前 `desktop_host_boundary` 中已有 29 项 daemon 最终切换红灯，覆盖单一 `Engine` 启停、HTTP 核心调用、桌面专属能力和删除旧 worker。它们是下一阶段的验收要求，不是本轮上传生命周期回归；完整核心命令会在该测试文件处按预期失败。
- 按正式接口重新审计后，29 项中大多数门禁仍要求 HTTP 层调用 `uc-engine::internal::execute_*`，这是早期过渡形态；当前最终接口应是同一共享 `Engine` 的公开 `Operation`。这些门禁必须改为验证公开操作，不能为了让旧断言转绿而重新暴露内部函数。
- 目前确认的真实生产缺口是 daemon `host.rs` 尚未拥有 `Engine::start/shutdown`，`process_bootstrap/process_runtime_start` 仍运行 `wire_dependencies`/`AppFacade` 旧装配，且 cleanup、peer keepalive、file sync 三个 worker 仍由 daemon 创建。
- 加入与切换空间共用同一个公开 `JoinSpace` 操作，核心根据当前状态决定新加入或迁移并通过结果中的迁移数量区分；门禁不能虚构不存在的模式字段。
- daemon 当前实际编译错误已经收敛为四处旧接线：`DaemonApiState::new` 需要共享 `Engine`、文件句柄能力和 LAN 网卡能力，`DaemonApp` 仍传 `AppFacade` 与旧 `SecurityState`；移动 LAN spawner 仍解析 `MobileSyncFacade` 并传独立文件传输对象。
- `prepare_desktop_engine_host` 已创建核心文件句柄表，但它被封在 `HostCapabilities` 内且只支持读取；网页导入、配置导出和诊断导出无法向同一张表登记输入/输出路径。正确接口是由宿主准备结果额外暴露同一份不透明句柄登记器，核心与 HTTP 共享它，不传本机路径穿过核心操作面。
- 共享句柄登记器现已支持输入读取和输出分块写入/落盘，输入与输出模式互相隔离；`DesktopEngineHost` 可把同一实例交给核心能力与 daemon 网页适配器。

## 2026-07-21 提交后续作恢复

- 当前工作区干净，分支最新三笔提交为 `21e4ac181`、`952afe0a1`、`ed8c6b56e`；它们分别完成核心兼容操作、网页与后台服务入口共享核心、桌面共享文件句柄。
- 上一轮按用户当时的明确要求跳过了测试，因此这些提交虽然已经过差异检查，但本轮不能直接视为验证完成。
- 现有记录中的 Plan 003 文件名已经失效，继续判断完成度前必须从 `plans/` 实际文件清单重新定位正式计划。
- 本轮恢复脚本在仓库本地与技能安装目录都不存在；技能目录只有说明、参考和模板，后续恢复应直接读取三份规划文件与 Git 状态。
- 正式计划文件是 `plans/003-introduce-unified-core-interface.md`；其桌面验收要求后台服务自己启动和关闭唯一核心、删除旧运行时双轨，并让桌面与命令行生产路径不再暴露旧入口。
- 当前网页请求已经通过共享核心执行，但 `apps/daemon/src/daemon/process_bootstrap.rs` 仍调用旧装配，`process_runtime_start.rs` 仍组装旧应用入口、启动旧会话并创建重复后台任务；Plan 003 不能标记完成。
- `crates/uc-engine/tests/desktop_host_boundary.rs` 已把真实剩余缺口固定为生产宿主启停、旧后台任务删除和旧装配删除；后续应通过公开核心入口替换，不得为了过门禁重新暴露内部函数。
- 上一轮三笔提交的前置验证已补齐一半：`uc-bootstrap` 34 项库测试、2 项配置迁移、3 项宿主契约和 5 项双端流程通过；`uc-webserver` 149 项库测试及 16 项集成测试通过，只有既有局域网压力测试按标记跳过。
- 桌面最终门禁目前 41 项中 36 项通过，5 项失败：生产宿主唯一核心启停、启动恢复、启动升级检测、移动 LAN 生命周期和三个重复后台任务。
- 启动恢复与升级不能在宿主持有共享 `Engine` 前单独迁移；当前实现只持有旧 `AppFacade`，若先局部创建核心会违反“单实例、无双轨”要求。下一步必须先完成宿主唯一核心接线，再让所有启动动作共享该实例。
- `cargo check -p uc-daemon --locked` 当前有 4 个编译错误，全部来自宿主接线未完成：`DaemonApiState` 需要共享 `Engine`、共享文件句柄和 LAN 网卡能力，旧 `DaemonApp` 仍传 `AppFacade` 与 `SecurityState`；移动 LAN spawner 仍传旧 facade 和文件传输对象。
- `prepare_desktop_engine_host` 已是唯一正确的桌面准备入口，一次返回核心配置、平台能力、存储路径和共享文件句柄。daemon 应从此启动 `Engine`，不能继续调用 `wire_dependencies` 再装第二份业务栈。
- 不能只删除旧 daemon worker：`EngineHostEventEmitter` 当前把剪贴板新内容、传输终态和投递变化都压成粗粒度刷新，只保留简化进度；旧入站 worker 还单独提供命令行 `watch` 依赖的原始入站通知。直接切换会造成真实功能退化。
- Plan 003 主切换的前置已从“宿主参数接线”收敛为“补齐统一核心事件边界”。需要先让核心事件完整表达现有实时消费者所需的信息，再由 daemon 做旧 WebSocket 形状转换；不能保留旧 worker 作为永久旁路。
- 统一核心事件现已完整保留新内容、待接收占位、接收阶段、传输进度、传输终态和投递变化；所有包含预览、文件名、设备编号或失败原因的公开载荷都使用脱敏调试输出。
- 这六类事件的测试均先因公开类型或字段不存在而失败，补最小映射后 6 项通过；剩余事件缺口是 CLI `watch` 的原始入站通知和 peer 在线变化。
- 原始入站通知和 peer 在线变化也已进入同一 `Engine` 事件流；原始内容只在内存中传递，调试输出只记录字节数和字段是否存在，暂停恢复继续使用同一事件通道。
- `uc-webserver` 已拥有统一核心事件到现有 WebSocket 形状的单一转换，原始入站通知与完整传输进度的字段级测试通过；daemon 不再需要理解或复制这些协议字段。
- 宿主参数仍可继续收敛：移动网卡查询可以新增统一核心操作复用现有移动设置能力；健康状态只需核心内部接收门禁的稳定摘要，不应继续向网页层传 `EnsureReceiveReadyPort`。
- 网卡查询与接收健康旁路已经删除：网页只调用稳定核心操作，健康接口不再持有内部接收接口，移动网卡也不再另带平台接口。
- daemon 生产宿主现从 `prepare_desktop_engine_host` 启动唯一 `Engine`，网页、移动 LAN、启动恢复、升级检测、事件流和最终关闭都共享同一实例；旧装配与重复 worker 源码已删除。
- 主切换门禁 42 项全部通过，daemon 41 项库测试全部通过，`cargo check -p uc-daemon --locked` 通过。
- 运行时设置更新仍需补一条事件驱动链路：启动时已按设置启停移动 LAN，但更新操作成功后监听控制器目前不会立即重对齐；必须在最终验收前补齐。
- 设置即时切换已补齐：核心只在设置更新成功后发出新的监听目标，daemon 事件转发器复用同一监听控制器执行启停或换端口；真实核心设置流程已验证事件确实发出。
- 当前差异为 45 个文件、约 1463 行新增和 5024 行删除，主要删除旧 app/process/service/search/worker 装配；新增集中在共享核心事件、网页协议转换和单核心宿主。
- 剪贴板变化测试已先通过历史查询证明内容被处理；其超时点只是仍等待旧 `RefreshRequired` 事件。新契约应精确匹配同一条内容的 `IncomingEntry`，不应放宽超时。
- 手机上传进度生产路径会用 `mobile_upload_peer_id` 将来源设备转为转输事件的对端标识；失败断言当前直接期望原始来源值，需先跑定向测试确认实际差异，再改测试。
- 剪贴板新内容事件的 `preview` 是稳定提示语 `New clipboard content`，不是真实剪贴板内容；真实内容由历史查询返回。测试应分别证明两者，不应要求事件携带原文。
- 旧入口复查确认：桌面 daemon、Tauri 和正式 CLI 已不再运行旧装配；唯一真实代码剩余是 `uc-cli` 的 `dev-tools` 功能，它仍通过 `uc-bootstrap::CliAppRuntime` 对外取得 `AppFacade`。按 Plan 003 “外部宿主不得继续透出”的明确要求，这不能因为只在开发功能中就视为完成。

## 2026-07-21 Plan 003 旧入口收尾恢复

- 当前生产宿主切换和真实进程验收均已完成，剩余范围应严格限制在 CLI `dev-tools` 对旧应用入口的外部依赖，不重新拆解 Plan 003。
- `vendor/` 是本轮开始前已存在的未跟踪目录；在确认内容和用途前保留不动，不把它混入当前改动。
- CLI 旧进程内会话被邀请、目录捕获、种子数据、历史导出、文件发送、移动同步调试和原始 blob 调试共同使用，不能只替换 `app_session.rs` 的类型声明。
- 统一核心已公开会话恢复、邀请、文件发送、历史查询、移动同步文档和文件操作；迁移应复用这些操作，不为命令行复制业务规则。
- 原始 blob 发布/按 ticket 拉取属于开发诊断能力，当前统一核心没有等价公开操作；需核对它是否已被正式文件发送和资源读取入口替代，避免为遗留调试命令扩大稳定核心接口。
- 可直接迁移的能力包括会话恢复、普通邀请、文件发送、历史查询、移动同步文档写入/读取和移动文件分块上传/读取。
- 尚无稳定核心等价项的是指定网卡地址邀请、仅捕获目录清单、直接写入种子历史和原始 blob ticket 发布/拉取；这些均为隐藏开发命令，不能为了保留旧实现而重新公开内部 facade。
- CLI 会话可由桌面宿主准备结果启动独立 `Engine`，但必须保留事件流并在退出时正常关闭；是否采用该路径取决于剩余开发命令的替代或退役结论。
- `docs/architecture/adr-008-p5-cli-as-client.md` 已锁定处理方式：`dev`、`mobile debug` 和相关 E2E 入口迁到仅调试构建存在的 daemon dev 端点，release 构建不包含路由或命令；因此不应让 CLI 再独立启动一份 `Engine`。
- 历史计划明确 `blob publish/fetch` 是长期诊断命令，不能直接删除；它也应通过受调试构建硬门控的 daemon 入口保留。
- 当前仓库尚未实现这些 daemon dev 端点，CLI 旧入口收尾实质上是完成 ADR-008 P5-3，而不是机械改名。
- 仓库根目录 `target` 是指向 `/Volumes/ExternalSSD/cargo-targets/MyProjects/uniclipboard/target` 的断链，Cargo 默认构建会失败；本轮验证需显式设置 `CARGO_TARGET_DIR=/tmp/uni-desktop-target`。
- 新边界测试确认旧入口分布在 10 个 CLI 源文件及 `uc-bootstrap` 两个公开位置，共 19 个匹配；迁移完成必须让这些匹配全部消失，而不是只隐藏 `app_session.rs`。
- 适合本阶段的接口形状是 `Engine` 持有的独立开发诊断操作面：输入输出使用 `uc-engine` 自己的类型，内部仍复用现有 facade；这样不把 `AppFacade` 或 port 交给外部，也不把开发操作混入移动绑定所消费的稳定 `Operation`。
- 新 CLI 运行时由桌面宿主准备结果启动 `Engine`，保留事件流和同一份文件句柄登记器，并在命令结束时走统一关闭；旧 `wire_dependencies` CLI 装配已删除。
- 文件发送现在通过宿主不透明句柄执行统一 `SendFiles` 操作，仍保持核心存活到用户停止，接收端拉取行为不变；CLI 不再手工拼装传输包。
- 手机调试文件写入现在真实走开始、追加、完成三步核心上传，再应用同步文档；比旧的直接 facade 调用更接近手机协议流程。
- 真实隔离配置证明种子明文可由核心正确解密读取，同时正文、根目录名、相对文件名均未在数据、缓存或日志中留下明文。
- 最终联合编译和旧入口扫描均通过，Plan 003 的代码验收项已经具备完成证据。
- 开发诊断类型已为正文、路径、文件名、令牌、blob 内容和配对结果提供脱敏调试输出；人工复查发现邀请地址类型仍自动显示完整地址，需在计划收尾前补同等级保护。
- 邀请地址已改为只显示脱敏占位，具体地址和端口不再出现在调试输出；覆盖正文、路径、令牌、blob 数据和地址的同一隐私检查已通过。
- Plan 003 的六项完成标准均已有实际证据，正式状态可安全更新为完成；整体路线图下一项仍是 Plan 002 的外部真机证据，而不是直接进入 Plan 004。
- 本机存在名为 `Pixel_8` 的 Android 模拟器，Android SDK 与 Rust 目标构建工具已安装；模拟器可以补充完整节点、暂停恢复、内容与落盘验证，但不能替代实体设备的蜂窝换网、厂商后台策略和硬件安全能力结论。
- 工作区目前没有 Android 共享核心验收宿主，只有 iOS 验收宿主和旧移动兼容客户端；要让模拟器证据有效，必须新增使用统一核心入口的最小 Android 验收应用。
- `Pixel_8` 模拟器是 Android 14 / API 34 的 ARM64 Google APIs 镜像，具备网络、文件级系统加密和 Android Keystore 服务；它能提供有效的软件密钥库证据，但不能证明实体设备的硬件安全模块。
- 模拟器使用 NAT 网络，主机与模拟器的局域网发现行为可能不同；后续应分别记录 relay/显式地址可达性与局域网自动发现，不把 NAT 限制误判为核心缺陷。
- Android 验收宿主应与现有 iOS 验收宿主共用命令、结果转换、文件句柄和核心生命周期；两端只分别实现 Keychain 与 Android Keystore，避免复制 1600 余行验收逻辑。
- Android 原生依赖要求宿主在启动任何后台线程前初始化 `ndk-context`，并让应用 Context 的全局引用存活到进程结束；每次命令替换该引用会在恢复网络时触发系统级 JNI 中止。
- 模拟器已证明 Android Keystore 密文可跨应用进程和 APK 覆盖安装恢复，同一空间与设备身份保持不变；这属于有效的软件密钥库与生命周期证据，不代表硬件安全模块验收。
- Android 仅靠短时广播接收器无法承载配对和持续 P2P 节点：广播结束后系统会冻结进程，即使 relay 地址已发布也无法接收连接；验收和产品都需要用户可见、符合政策的 `dataSync` 前台服务来维持在线窗口。
- Android 14 不允许自定义后台广播直接启动前台服务；验收宿主必须先经前台入口启动 `dataSync` 服务，再由广播只负责命令转发，否则系统会抛出 `ForegroundServiceStartNotAllowedException` 并终止进程。
- 前台服务真正进入运行态后，桌面与 Android 模拟器可经同一空间和云端邀请在数秒内完成配对；此前三次连接超时的根因是 Android 进程被冻结，不是 relay 或统一核心协议不通。
- 共用验收核心的平台选择必须同时受测试约束：Apple 使用 Keychain、Android 使用 Keystore、非移动目标才返回安全存储不可用；仅靠目标编译无法发现分支实现写反。
- 桌面 `UC_PROFILE` 已通过应用目录、后台端口和安全存储完成隔离，不能再改写统一核心的本地 Space 密钥范围；否则从默认范围移动节点导入的 keyslot 会在 daemon 重启后被误判损坏。
- 模拟器与桌面完整内容矩阵可通过同一统一核心完成，文本、图片和文件均双向成功；文件不能只看“接受”，必须在接收端导出后以字节数和 SHA-256 证明内容一致。
- 当前网络日志证明中继路径已建立并可用，随后同机网络环境又升级为直连；要单独证明 relay 承载内容，需要临时阻断模拟器 UDP 直连后再次发送，而不能只以 relay 地址存在作为证据。
- Android 侧临时丢弃全部 UDP 后，既有 relay 的 TCP 通道仍能完成文本传输；桌面精确记录 `purpose=clipboard` 选择 relay，且 Android 唯一历史查询命中，证明 relay 不是只发布未使用。
- Android 软件 Keystore、进程重启、5 轮暂停恢复、双向内容和私有目录明文扫描均已在模拟器成立；仍不能外推到实体设备的硬件密钥模块、蜂窝换网和厂商后台限制。
- 用户已明确跳过剩余实体设备验收；后续计划状态必须区分“跳过”和“通过”，Plan 002 与 Plan 004 的真机完成标准不得勾选。
- Plan 004 要求新建独立 `uc-engine-uniffi`，现有 `uc-mobile` 继续只代表 LAN HTTP 兼容客户端；两者发布名、依赖图和行为边界不能混合。
- 根工作区尚未包含 `uc-engine-uniffi`；新 crate 可沿用 `uc-mobile` 已固定的 UniFFI 版本和生成方式，但只能依赖 `uc-engine` 的公开接口。
- 仓库没有可直接复用的全局 crate 依赖边界测试；首个切片需在新 crate 内建立公开契约测试，并用 `cargo tree` 独立核对禁止依赖不存在。
- 根 `target` 是断开的旧外置磁盘符号链接；不能据此修改用户环境，本轮 Rust 命令需显式使用系统临时编译目录。
- 本机已安装 iOS ARM64、iOS 模拟器、Android ARM64/x86_64 Rust 目标，`cargo-ndk 4.1.2`、Xcode iPhoneOS SDK 与 Android SDK 均可用，可直接执行 Plan 004 的三目标构建验证。
- 旧 `uc-mobile` 的 UniFFI 生成入口可以复用工具形状，但 Android AAR 脚本仍是主动失败的占位；新正式绑定必须拥有自己的生成和发布路径。
- 新 `uc-engine-uniffi` 的首个切片已证明三目标构建、同版本标识和 Swift/Kotlin 生成；它当前只开放版本、错误、目录、安全存储、启动、创建空间和关闭，不能提前标记完整绑定完成。
- 绑定线程必须持续运行异步事件循环；若在两次同步宿主调用之间用阻塞接收暂停执行器，节点网络后台任务也会被暂停。
