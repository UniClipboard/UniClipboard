# 执行记录

## Session: 2026-07-20

### Plan 003

- **Status:** in_progress
- 已核对工作区干净，提交历史与交接记录一致。
- 已阅读 Plan 003、产品红线、代码修改规则、架构规则和 Rust 验证规则。
- 已确认下一步是分离桌面启动准备与纯核心装配。
- 已先验证边界测试因缺少桌面模块而失败，再完成模块分离并验证转为通过。
- 已修正旧身份目录说明，明确它只用于旧备份迁移，当前身份保存在安全存储中。
- 已提交桌面启动准备与纯核心装配分离：`5d2c90a07`。
- 已把旧身份文件升级逻辑移回桌面启动准备，统一核心不再依赖桌面存储实现。
- 已提交旧身份升级职责移动：`8c18a8240`。
- 已将网络策略和启动数据清理迁入 `uc-engine`，旧位置仅保留过渡转发。
- 已提交首批纯启动模块迁移：`00ae6dc33`。
- 已将文件接收临时区清理与文件传输生命周期迁入 `uc-engine`。
- 已提交文件传输运行层迁移：`ca6c74101`。
- 已将组装结果类型、剪贴板接入状态与后台文件处理任务迁入 `uc-engine`。
- 已提交运行依赖与后台任务迁移：`9ad02412e`。
- 已把桌面剪贴板选择与通用加密存储组装分离，后者迁入 `uc-engine`。
- 已提交通用平台组装迁移：`33efe096d`。
- 已将数据库、加密、搜索、文件处理和基础业务的主组装迁入 `uc-engine`。
- 已提交主组装迁移：`4d3696e6d`。
- 已将 P2P 节点组装、启动清理和 daemon 生命周期入口迁入 `uc-engine`。
- 已提交 P2P 生命周期组装迁移：`ca7930186`。
- 已新增通用 facade 所有权边界测试，并确认它因 `uc-engine` 尚无 facade 模块而按预期失败。
- 已将通用 facade 装配迁入 `uc-engine`，命令行运行时与桌面环境解析保留在 `uc-bootstrap`。
- 已提交通用 facade 装配迁移：`b935c5606`。
- 已先确认公开配置契约因 `EngineConfig` 缺失而失败，再补入应用版本与资料空间标识并验证转为通过。
- 已提交稳定启动配置契约：`0ffcb916b`。
- 已先确认宿主安全存储适配缺失，再补入字节往返；随后用第二个失败测试补齐稳定错误类别映射。
- 已提交宿主安全存储适配：`f2d5e526b`。
- 已用先失败测试补齐宿主剪贴板内存表示的双向转换；文件表示保持明确拒绝，未写入明文临时文件。
- 已提交宿主剪贴板内存表示适配：`dca158203`。
- 已先确认宿主目录派生函数缺失，再补入只使用私有目录与缓存目录的核心路径布局。
- 已提交宿主目录到核心路径的派生：`2747d267e`。
- 已先确认资料空间提供者无法接收显式标识，再补入保留旧默认行为的显式构造函数。
- 已提交显式资料空间提供者：`3bbe7001d`。
- 已先确认核心平台组装仍固定资料空间，再把资料空间标识加入纯组装输入；桌面入口继续显式传 `default`。
- 已提交资料空间标识到核心组装：`cadabc8df`。
- 已先确认缺少宿主能力到完整核心依赖的组装，再用真实临时数据库与内存密钥库验证组装成功。
- 已提交宿主能力到核心依赖的完整组装：`9e2ee67ec`。
- 已先确认公开启动入口不存在，再让 `Engine::start` 建立真实持久依赖、P2P 会话和后台任务。
- 已先确认宿主事件没有进入公开事件流，再接入传输进度和权威刷新通知。
- 已用真实节点验证暂停释放会话、恢复重建会话、关闭结束后台任务且事件流保持同一条。
- 已将最基础的设备列表操作接到真实成员数据，证明生产入口可以执行现有业务能力。
- 已提交生产核心运行时：`91820c41c`。
- 已先确认创建与解锁操作仍返回未接入，再将两者接到真实空间业务入口并固定错误分类。
- 已提交空间创建与解锁操作：`8d03dd053`。
- 已先确认邀请操作仍返回未接入，再将其接到真实邀请签发入口并固定错误分类。
- 已提交邀请签发操作：`44f92726e`。
- 已先确认加入操作仍返回未接入，再补入设备名校验、设置状态路由和两类加入流程的稳定错误映射。
- 已提交加入空间路由：`8bfa7450c`。
- 已提交历史查询、定向发送、文本/图片发送、重发、后台任务所有权和分块导出等核心操作：`fb78967dc` 至 `e362c81bf`。
- 已确认现有文件传输缓存会保存业务原文；用户选择保留旧文件格式，因此未实现 `SendFiles`，也未把 Plan 003 标记完成。
- 已用失败测试证明超限发送会进入不安全路径，再限制新核心只接受最多 64 KiB 的文本和图片；提交 `a0195e113`。
- 已完成并接入中文跨平台核心接口文档；提交 `5054cef70`。
- 已用真实关闭、锁定重启、解锁和关键字查询流程复现搜索不恢复，再接入现有恢复动作；提交 `8b4efc985`。
- 已重新审计 Plan 001-005：Plan 001 完成；Plan 002 缺真机与明文扫描；Plan 003 缺文件发送和桌面唯一入口；Plan 004、005 尚未进入。
- 已确认 Plan 002 的定制网络依赖固定到可审计 Git 提交，iOS/Android 编译证据仍有效。
- 已确认 Plan 002 尚缺连续十轮生产启停测试与统一明文探针，下一步先补十轮启停验收。
- 已新增十轮生产核心启停验收；首次创建空间，后续每轮锁定启动后解锁，十轮均成功并保持同一网络身份。
- 已先确认明文扫描器不存在导致契约测试失败，再实现多目录扫描、泄漏检测、缺失目录拒绝和脱敏报告；3 项脚本契约测试通过。
- 已在真实核心中发送运行时生成的文本与图片探针，关闭后扫描实际数据库、搜索数据、缓存和临时目录，未发现明文。
- 已用文件名包含探针的失败测试发现报告路径会泄露探针，再把所有目录和文件路径统一遮蔽；脚本契约重新全绿。
- 已检查 PR 与移动构建流程：PR 仅编译 Rust，旧移动构建仍发布 `uc-mobile`；下一步把 `uc-engine` 验收接入 PR，并补扫描器说明。
- 已把完整 `uc-engine` 测试加入 PR Rust 门禁，新增中文明文探针操作说明并加入文档目录。
- 已在 Plan 002 中登记十轮生产启停的测试证据并勾选该完成项；其余未覆盖项保持未完成。
- 已核对 iOS/Android 依赖树，两者使用相同 `iroh` 版本和同一固定 `iroh-blobs` 提交；HarmonyOS 目标和构建配置在当前仓库/工具链中缺失。
- 已从当前提交生成独立临时检出并成功运行锁定依赖解析，证明定制网络依赖不依赖父仓库或子模块。
- 已审计搜索后台任务：五处独立任务不受协调器关闭控制；下一步用阻塞重建测试固定“取消后等待子任务结束”的契约。
- 已先用阻塞重建证明协调器会过早返回，再把全部搜索子任务纳入私有任务范围；取消测试转绿。
- 搜索任务所有权调整后，应用层 836 个单元测试和 10 个集成测试、核心全部测试、iOS 与 Android 编译均通过。
- 已用第二条失败测试复现“启动检查等待重建锁时无法取消”，再让启动检查同时监听取消；两条取消测试均通过。

## Test Results

| Test | Expected | Actual | Status |
|------|----------|--------|--------|
| 交接前 `cargo test -p uc-engine` | 通过 | 17 个测试通过 | pass |
| 交接前 `cargo test -p uc-bootstrap` | 通过 | 51 个单元测试及集成测试通过 | pass |
| 交接前 iOS 目标检查 | 通过 | 通过 | pass |
| 交接前 Android 目标检查 | 通过 | 通过 | pass |
| 桌面启动边界测试（修改前） | 因桌面模块不存在而失败 | 按预期失败 | pass |
| 桌面启动边界测试（修改后） | 通过 | 1 个测试通过 | pass |
| `cargo test -p uc-bootstrap` | 全部通过 | 52 个单元测试和 7 个集成测试通过；1 个环境测试按设计跳过 | pass |
| 桌面程序编译检查 | 通过 | `uc-bootstrap`、daemon、CLI 通过 | pass |
| iOS 目标编译检查 | 通过 | 通过 | pass |
| Android 目标编译检查 | 通过 | 通过 | pass |
| 身份迁移边界测试（修改前） | 因桌面模块尚未拥有迁移函数而失败 | 按预期失败 | pass |
| 身份迁移边界测试（修改后） | 通过 | 旧身份保持不变并进入安全存储 | pass |
| 身份迁移调整后的完整启动测试 | 全部通过 | 单元、备份恢复、配对、状态和传递测试全部通过 | pass |
| `uc-engine` 迁移模块测试 | 全部通过 | 网络策略 14 个、启动数据清理 6 个通过 | pass |
| 迁移后的 `uc-bootstrap` 完整测试 | 全部通过 | 32 个本地单元测试和全部集成测试通过 | pass |
| 迁移后的 iOS `uc-engine` 编译 | 通过 | 通过 | pass |
| 迁移后的 Android `uc-engine` 编译 | 通过 | 通过 | pass |
| 文件传输迁移后的完整启动测试 | 全部通过 | 原有单元与全部双端集成测试通过 | pass |
| 文件传输迁移后的 iOS `uc-engine` 编译 | 通过 | 通过 | pass |
| 文件传输迁移后的 Android `uc-engine` 编译 | 通过 | 通过 | pass |
| 结果类型迁移后的 `uc-engine` 独立测试 | 全部通过 | 26 个单元测试、11 个契约测试通过 | pass |
| 结果类型迁移后的完整启动测试 | 全部通过 | 原有单元与全部双端集成测试通过 | pass |
| 结果类型迁移后的 iOS/Android 编译 | 通过 | 两个目标均通过 | pass |
| 平台组装拆分后的完整测试 | 全部通过 | `uc-engine`、启动与双端集成测试全部通过 | pass |
| 平台组装拆分后的 iOS/Android 编译 | 通过 | 两个目标均通过 | pass |
| 主组装迁移后的完整测试 | 全部通过 | `uc-engine`、启动与双端集成测试全部通过 | pass |
| 主组装迁移后的 iOS/Android 编译 | 通过 | 两个目标均通过 | pass |
| 节点生命周期迁移后的完整测试 | 全部通过 | `uc-engine`、启动与双端集成测试全部通过 | pass |
| 节点生命周期迁移后的 iOS/Android 编译 | 通过 | 两个目标均通过 | pass |
| facade 所有权边界测试（修改前） | 因核心模块不存在而失败 | 按预期失败 | pass |
| facade 所有权边界测试（修改后） | 通过 | 通过 | pass |
| facade 迁移后的 `uc-engine` 完整测试 | 全部通过 | 27 个单元测试和 11 个契约测试通过 | pass |
| facade 迁移后的 `uc-bootstrap` 完整测试 | 全部通过 | 32 个单元测试和全部 7 个活动集成测试通过；1 个环境测试按设计跳过 | pass |
| `EngineConfig` 公开契约（修改前） | 因配置类型不存在而失败 | 按预期失败 | pass |
| `EngineConfig` 公开契约（修改后） | 通过 | 通过 | pass |
| `EngineConfig` 后的 `uc-engine` 完整测试 | 全部通过 | 27 个单元测试和 12 个契约测试通过 | pass |
| `EngineConfig` 后的 iOS/Android 编译 | 通过 | 两个目标均通过 | pass |
| 宿主安全存储字节往返（修改前） | 因适配模块不存在而失败 | 按预期失败 | pass |
| 宿主安全存储错误分类（修改前） | 因全部归为普通错误而失败 | 按预期失败 | pass |
| 宿主安全存储适配测试（修改后） | 全部通过 | 2 个测试通过 | pass |
| 宿主剪贴板读取转换（修改前） | 因适配函数不存在而失败 | 按预期失败 | pass |
| 宿主剪贴板写回转换（修改前） | 因写回尚未实现而失败 | 按预期失败 | pass |
| 宿主剪贴板内存表示适配（修改后） | 全部通过 | 读写 2 个测试通过 | pass |
| 宿主目录派生（修改前） | 因派生函数不存在而失败 | 按预期失败 | pass |
| 宿主目录派生（修改后） | 私有与缓存布局符合预期 | 通过 | pass |
| 显式资料空间提供者（修改前） | 因构造函数不存在而失败 | 按预期失败 | pass |
| 显式资料空间提供者（修改后） | 返回传入标识 | 通过 | pass |
| `uc-infra` 完整测试 | 全部活动测试通过 | 601 个单元、30 个集成、16 个文档测试通过；既有 6 个测试按设计跳过 | pass |
| 核心资料空间接入（修改前） | 因构造入口不存在而失败 | 按预期失败 | pass |
| 核心资料空间接入（修改后） | 返回配置标识 | 通过 | pass |
| 资料空间接入后的核心与启动层测试 | 全部通过 | 核心、启动层及全部活动双端集成测试通过 | pass |
| 资料空间接入后的 iOS/Android 编译 | 通过 | 两个目标均通过 | pass |
| 完整宿主能力组装（修改前） | 因组装函数不存在而失败 | 按预期失败 | pass |
| 完整宿主能力组装（修改后） | 真实数据库、加密依赖和资料空间均来自宿主输入 | 通过 | pass |
| 真实 `Engine::start` 契约（修改前） | 因公开启动入口不存在而失败 | 按预期失败 | pass |
| 宿主事件转发（修改前） | 因核心事件桥接不存在而失败 | 按预期失败 | pass |
| 入站变化刷新契约（修改前） | 发现事件桥接会构造不完整摘要 | 按预期失败 | pass |
| 真实 `Engine::start` 与恢复会话 | 建立数据库和完整节点，暂停后释放，恢复后重建，关闭后事件流结束 | 通过 | pass |
| 宿主事件转发（修改后） | 传输进度进入同一事件流，入站变化要求查询真实记录 | 通过 | pass |
| 生产 `Engine` 完整核心测试 | 全部通过 | 29 个单元测试、20 个契约与集成测试通过 | pass |
| 生产 `Engine` 启动层回归 | 全部通过 | 32 个单元测试和全部活动集成测试通过；1 个环境测试按设计跳过 | pass |
| 生产 `Engine` iOS/Android 编译 | 通过 | 两个目标均通过 | pass |
| `uc-engine` 收窄静态检查 | 除已确认旧告警外无告警 | 通过 | pass |
| 创建与解锁操作（修改前） | 生产运行时返回未接入 | 按预期失败 | pass |
| 创建与解锁操作（修改后） | 口令校验、创建、错误解锁和正确解锁均符合稳定分类 | 通过 | pass |
| 创建与解锁后的应用层测试 | 全部通过 | 835 个单元测试、10 个集成测试通过 | pass |
| 创建与解锁后的核心与启动层测试 | 全部通过 | 核心、启动层及全部活动双端集成测试通过 | pass |
| 创建与解锁后的 iOS/Android 编译 | 通过 | 两个目标均通过 | pass |
| 邀请签发操作（修改前） | 生产运行时返回未接入 | 按预期失败 | pass |
| 邀请签发操作（修改后） | 真实签发邀请并返回非空邀请内容 | 通过 | pass |
| 邀请签发后的核心与 iOS/Android 编译 | 通过 | 核心全套测试和两个目标均通过 | pass |
| 加入操作（修改前） | 生产运行时返回未接入 | 按预期失败 | pass |
| 加入操作（修改后） | 拒绝空设备名，并按真实设置状态选择首次加入或空间切换 | 通过 | pass |
| 加入操作后的完整回归 | 全部通过 | 应用层、核心、启动层及全部活动双端测试通过 | pass |
| 加入操作后的 iOS/Android 编译与收窄静态检查 | 通过 | 两个目标和静态检查均通过 | pass |
| 超限发送修复前 | 文本和图片应拒绝 | 文本被正常发送 | expected fail |
| 超限发送修复后 | 文本和图片应拒绝 | 均返回输入错误 | pass |
| 接口文档检查 | 链接存在、无空白错误 | 通过 | pass |
| 锁定重启后搜索修复前 | 解锁后关键字搜索应恢复 | 持续不可用 | expected fail |
| 锁定重启后搜索修复后 | 解锁后关键字搜索应恢复 | 找到原有记录 | pass |
| 最新 `cargo test -p uc-application` | 全部通过 | 835 个单元测试、10 个集成测试通过 | pass |
| 最新 `cargo test -p uc-engine` | 全部通过 | 32 个单元测试及全部契约、宿主测试通过 | pass |
| 最新 iOS/Android `uc-engine` 编译 | 通过 | 两个目标均通过 | pass |
| Plan 002 十轮生产启停 | 十轮启动、解锁、关闭且身份不变 | 19.26 秒内全部通过 | pass |
| 明文扫描器契约（修改前） | 干净目录通过 | 脚本不存在 | expected fail |
| 明文扫描器契约（修改后） | 干净通过、泄漏与缺目录失败、正文不出现在报告中 | 3 项通过 | pass |
| 桌面核心文本与图片明文探针 | 关闭后私有目录、缓存和临时目录无探针原文 | 通过 | pass |
| 探针文件名脱敏（修改前） | 报告不包含探针 | 文件路径泄露探针 | expected fail |
| 探针文件名脱敏（修改后） | 路径中的探针统一遮蔽 | 3 项脚本契约通过 | pass |
| PR 工作流配置 | YAML 可解析 | 通过 | pass |
| Plan 002 独立检出依赖解析 | 无父仓库、无子模块时 `cargo metadata --locked` 通过 | 通过 | pass |
| 搜索重建取消（修改前） | 协调器等待子任务结束 | 主循环提前返回 | expected fail |
| 搜索重建取消（修改后） | 阻塞重建被取消且协调器等待完成 | 通过 | pass |
| 搜索启动检查取消（修改前） | 等待重建锁时仍可关闭 | 超时 | expected fail |
| 搜索启动检查取消（修改后） | 任意启动阶段均进入统一收尾 | 通过 | pass |
| 搜索关闭后新增工作（修改前） | 后台与同步重建均立即拒绝 | 后台仍登记，同步重建超时 | expected fail |
| 搜索关闭后新增工作（修改后） | 后台与同步重建均立即拒绝 | 返回不可用且无任务登记 | pass |
| 搜索任务范围静态检查 | 不持锁等待、不丢弃后台工作 | 通过 | pass |
| 桌面预览与正式日志明文探针 | 预览确实含探针，全部持久目录和日志无原文 | 通过 | pass |

## Error Log

| Timestamp | Error | Attempt | Resolution |
|-----------|-------|---------|------------|
| 2026-07-20 | 文件化规划工具未附带恢复脚本 | 1 | 确认无旧记录，依据 Git 和路线图重建 |
| 2026-07-20 | 拆分后发现纯装配仍计算桌面环境后缀路径 | 1 | 把网络数据目录加入已准备输入 |
| 2026-07-20 | 身份迁移测试移动补丁未匹配当前代码 | 1 | 重新读取后按实际内容移动 |
| 2026-07-20 | 反向依赖查询对不存在的包返回错误 | 1 | 改用依赖树文本搜索 |
| 2026-07-20 | `uc-engine` 未声明通用分析契约依赖 | 1 | 增加直接依赖后重跑编译 |
| 2026-07-20 | `uc-engine` 单独测试缺少后台任务功能 | 1 | 在自身依赖中显式开启后重跑 |
| 2026-07-20 | 主组装仍从桌面模块导入已迁移函数 | 1 | 更新为 `uc-engine` 新位置 |
| 2026-07-20 | 文件化规划工具安装目录仍缺少会话恢复脚本 | 2 | 直接核对现有三份进度文件与 Git 状态，确认交接记录完整 |
| 2026-07-20 | 全仓格式检查发现三处与本次无关的既有格式差异 | 1 | 只格式化本次迁移的三个文件，保留用户原有内容 |
| 2026-07-20 | 首次读取旧配置时使用了已经不存在的单文件路径 | 1 | 按搜索结果改读 `crates/uc-core/src/config/mod.rs` |
| 2026-07-20 | 剪贴板写回转换同时借用字节并移动媒体类型导致编译失败 | 1 | 先复制要交给宿主的字节，再移动其余字段 |
| 2026-07-20 | 资料空间字段补丁未匹配启动层测试的实际字段顺序 | 1 | 读取当前测试后按真实内容补入 `default` |
| 2026-07-20 | 通配符读取 facade 子目录时被 zsh 提前展开并失败 | 1 | 改用 `rg --glob` 和明确目录读取，不再交给 shell 展开 |
| 2026-07-20 | 真实核心关闭测试使用五秒外层期限，与网络节点自身五秒保护窗口相等 | 1 | 按期限嵌套规则改用桌面生产路径一致的十五秒外层预算 |
| 2026-07-20 | 十五秒关闭仍耗尽期限 | 1 | 定位到后台 blob worker 忽略统一取消信号，改为取消到达即结束任务 |
| 2026-07-20 | 严格静态检查被 `uc-core` 与 `uc-app-paths` 的既有告警阻断 | 1 | 保留无关文件不动，改为只检查 `uc-engine` 自身 |
| 2026-07-20 | 仅检查 `uc-engine` 仍被早先迁入代码的五类既有告警阻断 | 2 | 命令行忽略已确认的旧告警类别，其余告警继续视为失败 |
| 2026-07-20 | 收窄后的静态检查被既有测试中的 `unwrap` 阻断 | 1 | 仅在检查命令中追加忽略测试旧告警，生产代码规则不变 |
| 2026-07-20 | 同一进程启动第二个生产核心实例失败 | 1 | 改用接口前置条件测试，完整双端配对继续由专用双端测试验证 |
| 2026-07-20 | 第二个实例改用独立资料空间标识后仍启动失败 | 2 | 确认属于进程级节点限制，不改生产配置绕过 |
| 2026-07-20 | 加入路由首次补丁未匹配格式化后的导入顺序 | 1 | 按当前文件拆分补丁并逐块应用 |
| 2026-07-20 | 空间切换类型从错误的总入口导入 | 1 | 改从其所属的空间模块导入 |
| 2026-07-20 | 全仓格式检查被三处既有文件阻断 | 1 | 改为只检查本次涉及的包，不修改无关文件 |
| 2026-07-20 | 新搜索恢复测试与既有真实节点测试并行抢占进程级节点 | 1 | 给两条真实启动测试加串行锁后整包稳定通过 |
| 2026-07-20 | 文件化规划工具安装目录仍没有会话恢复脚本 | 3 | 读取已有三份记录、五份计划与 Git 状态恢复上下文 |
| 2026-07-20 | 当前环境未安装 `shellcheck` | 1 | 已运行 `bash -n` 和三项脚本契约测试；保留后续 CI 安装后补跑 |
| 2026-07-20 | 当前环境未安装 `actionlint` | 1 | 已用 Ruby YAML 解析验证结构；工作流真实执行留给 CI |
| 2026-07-20 | 全新检出验证命令因包含临时目录自动删除而被安全规则拒绝 | 1 | 改为保留系统临时目录后重新运行，不使用删除命令 |
| 2026-07-20 | `TaskTrackerToken` 不在 `tokio_util::task` 简写导出中 | 1 | 改从其公开子模块导入后重跑测试 |
| 2026-07-20 | 首次使用 `--exact` 时测试名未含模块路径，实际筛选为 0 项 | 1 | 去掉 `--exact` 后确认测试先失败再通过 |
| 2026-07-20 | `uc-application` 严格静态检查被 81 处既有问题阻断 | 1 | 不修改无关文件，改为定向检查本次涉及的持锁等待与后台工作规则并通过 |
| 2026-07-20 | zsh 未展开带版本通配符的 iroh-blobs 源码路径 | 1 | 改用 `rg --files` 找到固定 Git 检出的真实路径后继续核对 |

## 2026-07-20 接续记录

- 已核对交接摘要、三份规划文件、五份正式计划、Git 历史与工作区状态。
- 当前仅有搜索后台任务生命周期的两处未提交改动，范围与交接摘要一致。
- 已确认现有两条测试分别覆盖关闭等待子任务结束、启动检查等待期间仍可关闭。
- 下一步先完成核心测试、两种移动目标编译、格式与差异检查，再审查关闭和新任务并发发生时的行为。
- `cargo test -p uc-engine --locked` 全部通过；iOS 与 Android 的 `uc-engine` 编译均通过；本次范围格式检查与差异检查通过。
- 复查确认任务追踪器关闭后仍允许新增任务，下一步先用失败测试固定“关闭后拒绝新增后台工作”，再补关闭门禁。
- 新增测试先复现关闭后仍登记后台任务，补门禁后转绿；随后把公开结果补为“不可用”，测试先因缺少该结果而失败，再在结果映射完成后转绿。
- 同步重建的关闭测试先复现超时，接入同一任务范围后转绿。
- 最终 `uc-application` 838 个单元测试、10 个集成测试通过；`uc-engine` 全部测试通过；iOS 与 Android 编译通过；格式、差异和定向静态检查通过。
- 搜索任务生命周期已提交：`ca1be99a8 fix: own search task lifecycle`。
- 已实际核对设备：Android 未连接；物理 iPhone 被系统列出但当前不可用；HarmonyOS 工具未安装。
- 已让真实核心探针明确进入历史预览，并用正式日志器生成日志；关闭后扫描私有数据、缓存、临时目录和日志目录通过。
- 已核对文件传输的发布、接收和第三方仓储入口：外层直接加密会破坏旧客户端，底层也没有可注入的透明加密接口；保持旧格式与落盘密文仍是明确冲突。
- 用户已明确改为“文件落盘不加密”，此前文件加密冲突不再作为 `SendFiles` 阻断；下一步先统一仍然矛盾的仓库文档。
- 交接时物理 iPhone 16 Pro 可用，但本轮实时复查已变为 `unavailable`；正在检查连接与开发者服务状态，恢复前不计入任何真机证据。
- `/Users/mark/MyProjects/uniclipboard-android` 当前不存在，后续不再把它当作候选宿主路径。
- 已核对现有移动应用：其 iOS 工程仍嵌入旧 `uc-mobile`，不能计入新核心 P2P 验收。
- 已确认工作区没有现成的新核心 iOS 真机宿主；后续需要在完成 `SendFiles` 后补绑定和最小宿主。
- 设备详情显示配对和开发者模式正常，但当前连接离线；无连接期间继续完成可离线进行的宿主准备。
- 已统一根规则、产品原则、正式计划、接口说明和明文探针说明：文件内容本体允许原始字节落盘，文件名、路径、标签、预览及其他内容仍必须加密。
- `SendFiles` 的首个测试闭环已完成：宿主文件分块导入后可发送，暂停恢复后仍可完整导出；磁盘路径使用匿名编号，原始文件名在加密历史中保留，私有、缓存和临时目录的文件名探针扫描通过。
- 文件发送规划已验证会用加密保存的原始名称覆盖匿名磁盘名，避免对端收到编号文件名。
- iPhone 已在实时复查中恢复上线，`devicectl` 显示 `available (paired)`，`xctrace` 也确认在线。
- 已撤销格式化工具误触的三处无关旧文件，仅保留本任务范围改动。
- `SendFiles` 改动已通过 `uc-application` 839 个单元测试和 10 个集成测试、`uc-engine` 全部测试、iOS 目标编译、文件名元数据测试、差异检查和收窄静态检查。
- 新增不发布的 iOS 真机验收宿主；Rust 运行层 2 项测试通过，iOS 17 静态库、SwiftUI 应用、自动签名和安装均成功。
- 首次真机启动因 iPhone 锁屏被系统拒绝；错误明确为设备未解锁，等待解锁期间继续准备桌面配对端。
- 已读取 `swiftui` 真机测试指导；其引用的两份命令行参考文件缺失，后续直接使用 Xcode 自带命令完成构建、安装、启动和日志采集。

## 5-Question Reboot Check

| Question | Answer |
|----------|--------|
| Where am I? | Plan 003 文件格式门禁与 Plan 002 剩余本地验收 |
| Where am I going? | 完成 Plan 002、闭合 Plan 003、移动绑定和独立仓库 |
| What's the goal? | 四个平台使用同一套完整 P2P 核心 |
| What have I learned? | 见 `findings.md` |
| What have I done? | 见本文件和 Git 历史 |

## 2026-07-20 iOS 验收与提交收尾

- iOS 与桌面双向文字、图片、文件、暂停恢复、强制退出恢复和历史搜索均通过。
- iPhone 关闭 Wi-Fi 后，桌面发送的 41 字节唯一文字在手机逐字节验证通过，传输记录确认使用 relay。
- 手机和桌面持久目录未命中该文字原文；测试进程已全部停止。
- 当前目标是把所有剩余变更按单一意图提交，并确认最终工作区干净。
- `planning-with-files` 恢复脚本仍未随当前安装提供；已直接读取现有记录恢复上下文。
- 已完成完整差异审查，并将剩余变更划分为八笔单一意图提交。
- 已提交 `d8850aa66`：空间访问成功后恢复接收能力。
- 八笔剩余变更已全部提交，最新提交为 `a318a2cbf`。
- 最终应用层、核心、基础层和 iOS 探针测试全部通过；命令行与后台服务构建通过。
- `git diff --check HEAD~8..HEAD` 通过，最终工作区干净。

## 2026-07-21 Phase 2 接续

- `planning-with-files` 恢复脚本在指定安装路径中不存在；已改为核对三份规划文件和 Git 工作区。
- 工作区当前干净，没有尚未同步进度的代码改动。
- 已重读测试先行要求和 `VISION.md`；本轮继续遵守先失败、后实现、再完整回归的顺序。
- 已核对正式路线图：Plan 003 的状态行尚未同步，不能作为实际完成度依据。
- 已执行 Plan 003 的旧入口搜索：daemon 和 CLI 生产路径仍命中 `AppFacade`、`AppDeps`、`CliAppRuntime`，确认 Phase 2 尚有真实迁移工作。
- 已梳理 daemon 与 webserver 调用面，确认不能用外壳包装旧 facade，也不能把 daemon HTTP 放入核心。
- 已确认普通 CLI 业务命令已走 daemon，旧运行时只剩开发诊断命令；不能以删除功能代替迁移。
- 已核对 ADR-005 的依赖铁律：核心不得依赖 webserver/daemon，迁移只能由外壳调用稳定操作。
- 已读完核心接口文档与 ADR 阶段细则，确定先收紧 daemon 内部旧对象的可见性，并加持续边界门禁。
- daemon 旧字段边界测试修改前按预期失败，准确列出 5 处公开的 `AppFacade`/`AppDeps` 字段；已收紧为 crate 内可见并将门禁扩展到整个 daemon 源码树。
- 全仓格式检查仍被 3 处既有无关差异阻断；本轮只格式化新增测试，不修改无关文件。
- 定向 `cargo fmt` 实际仍格式化了全仓；已逐项撤回它对 3 个无关文件造成的变化，并保留本任务范围。
- daemon 旧字段边界门禁已转绿；`cargo check -p uc-daemon`、`cargo test -p uc-engine --locked`、`cargo test -p uc-daemon --locked` 全部通过。
- 已提交 `3ce712a81 refactor: hide daemon legacy runtime fields`，并把计划中已实际完成的公开操作映射项同步为完成。
- 已审计 CLI 剩余旧路径，确认不能把移动 LAN 调试混入统一核心；本轮先继续收紧 daemon 自身边界。
- 下一步用失败门禁固定 `run_loop` 不再依赖 `AppFacade`，再把升级检测和启动恢复移到桌面启动层。
- 已新增运行循环边界测试，预期在移动代码前因仍引用 `AppFacade` 而失败。
- 运行循环边界测试已按预期失败；升级检测和启动恢复现由 daemon host 在进入运行循环前触发，运行循环只保留服务运行与有序关闭。
- 复查后将启动工作保留在原有后台任务内，避免改变 `start_in_process` 返回时序；daemon 60 项测试、运行循环边界门禁和差异检查通过。
- 已提交 `28e669450 refactor: isolate daemon run loop`。
- 使用深模块规则核对下一处 seam：不新增透传 trait，改为加深现有 `DaemonProcessRuntime`，逐步删除其 `app_facade()` 泄露。
- 已新增 daemon 库不再导出进程内旧装配入口的失败门禁，准备删除无人使用的公开入口。
- 旧装配导出门禁已按预期失败；`ProcessRuntimeHandles` 与 `start_in_process` 已改为 crate 内可见，并从 daemon 库公开清单移除。
- daemon 三条边界门禁和编译检查通过；公开装配入口删除范围已完成差异审阅。
- 已提交 `82c7ce385 refactor: close daemon legacy assembly entry`；随后发现并把更早一层的 process bootstrap 导出加入下一条失败门禁。
- process bootstrap 导出门禁已按预期失败；构建函数和上下文已改为 crate 内可见，并从 daemon 库公开清单移除。
- process bootstrap 导出删除已通过三条边界门禁、daemon 编译和差异检查。
- 已提交 `85b198bb5 refactor: internalize daemon process bootstrap`；开始加深 `DaemonProcessRuntime`，先以失败门禁删除任务登记器访问器。
- 任务登记器门禁已按预期失败；blob 后台任务启动现由 `DaemonProcessRuntime` 自己完成，host 不再获取内部登记器。
- 任务所有权改动已通过四条 daemon 边界门禁、daemon 编译和差异检查；门禁禁止任何形式的登记器访问器。
- 已提交 `32176891c refactor: own daemon background task registration`；开始用失败门禁删除 `app_facade()` 访问器。
- facade 访问器门禁已按预期失败；daemon host 现通过 `DaemonProcessRuntime::start` 启动，不再取得 `AppFacade`。
- 差异审阅发现并消除了 process runtime 与 host 的双向依赖；启动实现、关键顺序说明和旧名称已统一迁入运行时私有实现。
- 运行时启动所有权改动已通过五条边界门禁和 daemon 60 项测试；host 已不再引用 `AppFacade`、`AppDeps` 或旧启动 helper。
- 已提交 `49fff4492 refactor: own daemon startup in process runtime`；正式旧入口搜索现只剩 CLI，但 daemon 内部仍待真实切换。
- 下一步测试先行新增统一“恢复会话”操作，为删除 daemon 的直接解锁/恢复编排建立稳定入口。
- 已把锁定重启搜索测试改为通过安全存储恢复会话；修改前预期因公开操作尚不存在而失败。
- 安全存储恢复测试已按预期因缺少输入、操作和结果失败；现已补入最小稳定契约和生产映射。
- 新恢复操作的核心全套测试和 iOS 目标编译通过；Android 首次重跑因当前 shell 未配置 NDK 编译器失败，正在定位本机工具链。
- 显式使用本机 NDK 27 的 API 24 工具链后，Android `uc-engine` 完整目标编译通过。
- 严格静态检查仍被 `uc-core` 和 `uc-app-paths` 的既有告警阻断；将用 `--no-deps` 检查本次包自身。
- `uc-engine --no-deps` 只命中计划已登记的 8 处旧告警；仅忽略对应四类后严格检查通过，新恢复操作无新增告警。
- `RecoverSession` 已通过安全存储重启搜索实测、核心全套测试、iOS/Android 目标编译和差异检查。
- `uc-bootstrap` 全套测试与 daemon/desktop/Tauri/CLI 联合编译通过。
- 工作区全套测试首次在 Tauri 构建前因缺少本机 daemon sidecar 停止，正在生成临时产物后重跑。
- sidecar 生成后工作区测试继续，真实发现 iOS 探针未映射 `SessionRecovered`；正在补齐。
- iOS 探针已补稳定的恢复结果映射和对应测试。

## 2026-07-21 Phase 2 再接续

- `planning-with-files` 指定的恢复脚本仍不存在；已直接核对三份规划文件、Git 历史和工作区状态恢复现场。
- 上一轮启动的 `cargo test --workspace --locked` 进程仍在运行，尚未把结果记为通过。
- 当前工作区干净，最新提交为 `76a9dd606 fix(ios): map engine session recovery result`。
- 本轮完成标准：确认全仓测试结果，完成一个测试先行的 daemon 稳定操作迁移切片，跑完相关回归并独立提交。
- 上一轮全仓测试进程随后结束，但原终端退出状态无法取得；本轮最终会重新执行，不能沿用为通过证据。
- 已重读 `VISION.md`、测试先行规则和深模块规则；启动恢复迁移必须替换旧编排，不能再加一层透传接口。
- 已确认不能同时启动旧 daemon 运行时与新 `Engine`，否则会重复装配数据库和 P2P 节点。
- 已确认所有 daemon 运行模式都会在解锁后自行放行延迟服务；下一步先加边界测试，要求启动恢复文件不再包含旧解锁、空间恢复和接收恢复编排。
- 启动恢复边界测试已按预期失败，准确命中旧解锁函数、空间恢复、在线刷新和直接接收恢复五类残留；现在开始把它们替换为统一核心的单一实现。
- 已将恢复会话编排收敛为 `uc-engine` 内唯一实现，正式核心入口和 daemon 启动恢复共用；daemon 旧解锁函数及空间恢复步骤已删除，边界测试转绿。
- `cargo check -p uc-daemon` 通过；`cargo test -p uc-engine --locked -q` 全部通过（34+6+12+2+3+10 项）；`cargo test -p uc-daemon --locked -q` 60 项通过。
- 差异审查后已把最初仅转发名称的浅文件改为真正拥有恢复逻辑，并明确它只是 daemon 完成迁移前的内部接入点。
- 自审删除了一条失效注释和一个已无调用方的运行模式判断；意外结果日志不再输出完整结果，避免未来结果扩展后把用户内容写入日志。
- 桌面相关项目联编、iOS/Android 目标编译、核心与 daemon 严格静态检查均通过。
- 全仓测试运行到 `uc-platform` 时发现 `parameterized_text_plain_classifies_as_text_plain` 触发媒体类型输入保护检查；当前先按真实失败处理，不能把全仓测试记为通过。
- 已确认失败根因是测试数据违反现有输入规则：`public.utf8-plain-text` 属于系统格式名，不是标准媒体类型；样例已移入格式名回退用例。
- 平台单元测试 91 项通过；文档测试随后发现私有安全存储函数的示例无法从外部访问，已删除这段不可运行且会修改进程环境的伪示例。
- 第二次全仓测试越过平台层后发现 daemon 进程地址模块 6 个示例缺少作用域；已为公开函数补导入，并删除私有实现的不可运行示例。
- `cargo test --workspace --doc --locked -q` 已完整通过；下一步进行最终全仓回归。

## 2026-07-21 Phase 2 第三次接续

- `planning-with-files` 恢复脚本仍未安装；已直接核对交接摘要、三份规划文件、Git 历史、工作区和未提交差异。
- 当前代码与交接一致：最终全仓测试已经通过，剩余改动可拆为文档示例、平台测试数据、daemon 恢复迁移三项独立提交。
- 已重读产品底线、Rust 规则、架构规则和测试先行要求；统一核心不能接管 daemon 的 HTTP 或桌面专属行为，旧恢复编排必须保持删除状态。
- 分支 `independent-crates` 名称检查通过；新增恢复实现文件已单独审阅，未发现未纳入交接的改动。
- 已提交 `60e51ab8e docs: fix workspace examples`，只修正文档示例的可运行性。
- 已提交 `2586062a8 test(platform): respect clipboard MIME contract`，只修正平台测试数据归类。
- 已提交 `f8d168b81 refactor: route daemon session recovery through engine`，daemon 启动恢复已改用统一核心实现，旧编排保持删除。
- 正式 Plan 003 验收仍未满足：daemon 内部和 CLI 开发工具仍有旧入口；本轮不会提前更新计划状态。
- 已确认当前不能再次调用 `Engine::start`，否则会重复装配数据库和 P2P 节点；下一切片改为测试先行迁移真实的历史重发动作。
- 审阅历史重发响应后发现统一核心会丢失逐目标统计，因此停止该方向；改为先迁移结果形状可完整保持的“发出配对邀请”动作。
- 邀请到期时间测试先因统一结果缺少字段而失败，证明测试命中了真实缺口；补入服务端权威到期时间后，真实核心测试转绿。
- 已将邀请发出和稳定错误转换收敛到 `uc-engine` 的单一内部实现；`cargo test -p uc-engine --locked` 全部通过。
- 已提交 `677059477 refactor(ios): tolerate engine result extensions` 和 `e775f2bc0 feat(engine): expose invitation expiry`；下一步用失败边界测试迁移 daemon 邀请接口。
- daemon 邀请边界测试先准确命中旧业务调用而失败；改用统一核心邀请实现后转绿，`cargo check -p uc-webserver --locked` 通过。
- `cargo test -p uc-webserver --locked` 全部通过：140 项通过，1 项长期压力测试按设计忽略，所有独立集成测试通过。
- daemon 60 项测试全部通过；daemon、desktop、Tauri、CLI 与 iOS 验收宿主联合编译通过。
- 提交前自审确认邀请成功响应字段保持不变，常见失败仍映射为原有 503/500 分类；核心没有反向依赖 HTTP 层。
- webserver/daemon 收窄严格静态检查通过；只在命令中忽略了既有的主动中止和生成工具终端输出告警。
- 最终 `cargo test --workspace --locked -q` 完整通过；所有实际执行用例均无失败，原有设备、网络和长期压力用例按设计忽略。
- 已提交 `781319289 refactor(webserver): route invitations through engine`；配对邀请的生产接口现复用统一核心实现。

## 2026-07-21 Phase 2 第四次接续

- `planning-with-files` 恢复脚本仍未安装；已直接核对三份计划记录、Git 历史和干净工作区。
- 当前继续迁移下一个已有稳定核心操作的真实后台服务动作；先比较创建空间与加入空间的现有响应，保证不减少用户可见信息。
- 创建与加入路径都存在设备名回退和结果字段缺口，直接迁移会改变现有行为；改查成功结果更简单的解锁路径。
- 解锁路径可迁移，但需先补空间标识、拆分两个状态错误，并确保搜索/接收恢复只运行一次。
- 已确认 daemon 的接收 port 本身包含门禁与延迟服务通知；下一步用失败测试固定空间标识和错误编号区分。
- 三个失败证据已取得：成功结果缺空间标识、两种状态错误共用 1211、损坏材料与内部故障共用 1213。
- 补齐结果和错误区分后，两个错误测试与真实核心解锁流程均已转绿；开始收敛为统一核心单一实现。
- 解锁逻辑已收敛到 `uc-engine` 单一模块；核心编译无告警，iOS 验收宿主 3 项测试通过。
- 统一解锁模块差异审查和 `cargo test -p uc-engine --locked` 全部通过；分支名称检查通过。
- 已提交 `8075ee64c feat(engine): return unlocked space identity`；开始用失败边界和错误映射测试迁移 daemon 口令解锁。
- daemon 解锁边界测试和统一错误映射测试已转绿；自审恢复了静默解锁原有的广播与搜索时序。
- webserver 140 项测试、daemon 60 项测试全部通过；daemon、desktop、Tauri、CLI、iOS 验收宿主联合编译通过。
- webserver/daemon 严格检查、格式和差异检查通过；修正边界测试的函数截取后重新验证通过。
- 最终 `cargo test --workspace --locked -q` 完整通过；所有执行用例无失败，既有设备、网络和长期压力用例按设计忽略。
- 已提交 `c37b6cf03 refactor(webserver): route passphrase unlock through engine`；继续迁移相邻的静默解锁路径。
- 静默解锁边界测试已按预期失败；发现恢复失败和接收失败需先拆分稳定编号才能保持原状态码。
- 恢复失败编号拆分测试、静默解锁边界、口令解锁边界和 HTTP 状态映射测试均已转绿；旧会话就绪业务函数已删除。
- 静默解锁迁移后 webserver 141 项测试、daemon 60 项测试、桌面联合编译和收窄严格检查全部通过。
- 最终 `cargo test --workspace --locked -q` 再次完整通过；所有执行用例无失败。
- 已提交 `798f2a48e refactor(webserver): route silent unlock through engine`；静默与口令解锁现都复用统一核心。

## 2026-07-21 Phase 2 第五次接续

- `planning-with-files` 恢复脚本仍不存在；已按既有三份计划记录、Git 状态和当前源码恢复，工作区起点干净，最新提交为 `798f2a48e`。
- 本轮完成标准保持完整 Phase 2：daemon 核心业务只经 `uc-engine`，CLI 外部旧入口删除，Plan 003 全部验收通过后才更新正式状态。
- 已开始全量旧入口审计；确认 webserver 剩余直接业务调用至少覆盖创建、加入、取消邀请、重置、设置状态、切换空间、迁移进度、历史重发和移动局域网设备列表，不能只按交接摘要迁移三个设置动作。
- 旧类型命中还分布在 daemon 启动装配、移动局域网生命周期、搜索和网络后台任务及 CLI 开发会话；下一步按核心业务、桌面职责、诊断工具分类并形成可删除清单。
- 创建空间公开契约测试先按预期失败，准确证明设备名不能省略且成功结果缺本机设备与指纹；补齐契约和生产结果映射后转绿。
- 全新真实核心传空设备名仍按原规则返回无效输入；这不是回退缺陷，因为新环境没有已保存名称。已恢复显式名称，并保留应用层“已有保存名称时允许省略”的专门覆盖。
- 创建空间后台边界测试先按预期失败，迁移到统一核心单一实现后转绿；后台成功响应保持原有空间、本机设备和身份指纹字段，错误详情不再回传。
- `uc-engine` 全套测试、`uc-webserver` 全套测试和桌面/iOS 相关联编通过；严格检查首次只命中统一核心已登记的旧告警，下一步按既有收窄规则重跑。
- 严格检查第二次只命中测试辅助代码和移动局域网旧告警；收窄到 `uc-engine` 与 `uc-webserver` 正式库后通过，本次新增代码无新告警。
- 分支名称 `independent-crates` 检查为明确业务名称；创建空间迁移差异已完整审阅，准备按单一职责提交。
- 已提交 `f11bb4277 refactor(webserver): route space creation through engine`；创建空间生产接口现复用统一核心，成功字段完整且内部错误脱敏。
- 加入空间公开契约测试先按预期失败，证明设备名仍被强制要求且成功结果丢失双方身份与迁移数量；补齐契约和生产映射后转绿。
- 加入与切换两条 daemon 边界测试均先按预期失败；现已分别改用统一核心的首次加入与切换模式，保留各自前置条件和响应形状，两条门禁均转绿。
- 加入/切换迁移通过 `uc-engine` 全套测试、`uc-webserver` 全套测试、daemon/desktop/Tauri/CLI/iOS 联编、iOS 验收宿主 3 项测试和两个正式库的收窄严格检查。
- 自审确认公开自动加入仍按设置状态选择首次加入或切换，两个既有 HTTP 接口则使用显式模式；双方身份、迁移数量和原有 HTTP 状态均保留，内部错误详情改为脱敏响应。
- 已提交 `d5e137946 refactor(webserver): route space joins through engine`；首次加入和空间切换生产接口均复用统一核心。
- 取消邀请的公开契约与 daemon 边界测试先按预期失败；补统一操作后转绿。首次实现暴露运行期设置服务装配差异，修正为明确的暂不可用结果后通过。
- 取消邀请通过 `uc-engine` 全套、`uc-webserver` 全套、桌面/iOS 联编和正式库严格检查；没有待取消邀请仍返回冲突，内部失败响应已脱敏。
- 已提交 `0bfcebb1a refactor(webserver): route invitation cancellation through engine`；取消邀请生产接口现复用统一核心。
- 重置空间公开契约和 daemon 边界测试先按预期失败，补统一操作后转绿；设置服务未安装返回暂不可用，存储和内部失败不再把原始详情回传。
- 重置空间通过 `uc-engine` 全套、`uc-webserver` 全套、桌面/iOS 联编和正式库严格检查。
- 已提交 `77d342397 refactor(webserver): route space reset through engine`；重置空间生产接口现复用统一核心。
- 设置状态公开契约和 daemon 边界测试先按预期失败，补统一查询后转绿；结果完整保留完成状态、当前邀请和设备名，调试输出只报告字段是否存在。
- 设置状态通过 `uc-engine`/`uc-webserver` 全套、桌面/iOS 联编和正式库严格检查；后台成功响应字段保持不变，失败详情已脱敏。
- 已提交 `e4edbcaf7 refactor(webserver): route setup state through engine`；设置状态生产接口现复用统一核心。
- 迁移进度公开契约和 daemon 边界测试先按预期失败，补三个粗粒度阶段与备份数量结果后转绿；内部运行编号和目标空间未暴露。
- 迁移进度通过 `uc-engine`/`uc-webserver` 全套、桌面/iOS 联编和正式库严格检查；`setup.rs` 已无旧设置服务和直接业务调用。
- CLI 旧入口复审确认仅存在于 `dev-tools`，但承载多类真实诊断能力；后续按统一核心、桌面诊断和移动局域网兼容三类迁移，不以删除功能冒充完成。

## 2026-07-21 Phase 2 第六次接续

- 已重新读取 `planning-with-files`、测试先行和模块设计规则，并核对产品、架构、Rust 与 crate 级约束。
- 恢复脚本仍不存在；当前分支为 `independent-crates`，最新提交 `771afed25`，工作区干净。
- 当前完整目标不变：daemon 只持有并调用唯一 `Engine`，CLI 旧入口删除，Plan 003 全部验收通过后才结束 Phase 2。
- 实时代码确认 daemon 与 `Engine::start` 目前各有独立完整装配路径；直接并排启动会重复数据库、P2P 节点和后台任务，必须先替换所有权而不是增加桥接层。
- 本轮先完成 daemon 所有权与调用面的可删除清单，再以失败门禁开始替换生产装配。
- 已完成 daemon/webserver 调用面第一轮分类：跨平台业务必须改走 `Engine::execute`，桌面升级、HTTP、移动 LAN、连接诊断和产品分析留在外壳并拆除对总 facade 的依赖。
- 已确认现有 `Engine::start` 与 daemon 旧运行时都会启动搜索、剪贴板、文件传输和 blob 后台任务；不能以并排运行作为迁移步骤。
- 已先补齐 `CancelInvitation`、`ResetSpace`、`QuerySetupState`、`QueryMigrationProgress` 的中文接口说明并提交 `9c516f993 docs(engine): document setup operations`。
- 升级外壳依赖门禁先按预期命中启动与 HTTP 两条旧总入口；首次实现后因多行链式调用未被文本门禁识别，现改为显式取得升级能力后重跑。
- 升级能力门禁已转绿；启动版本判断和两个升级 HTTP 接口现直接依赖 `UpgradeFacade`，不再经总入口读取。
- `uc-engine`、`uc-webserver`、`uc-daemon` 联合测试全部通过；daemon 60 项、核心全部接口与宿主测试、webserver 145 项及集成测试均无失败。
- 本次五个文件的格式与差异检查通过；严格检查仅需忽略 webserver 已登记的 5 处旧 `expect`，其余警告保持失败并通过。
- 已提交 `6aa6bbe5a refactor(daemon): isolate upgrade capability`。
- 诊断能力门禁先按预期失败，迁移后转绿；调试状态、调试开关和日志导出三条接口现直接依赖 `DiagnosticsFacade`。
- webserver 145 项测试和全部集成测试、18 条 daemon 边界门禁、daemon 联合编译及收窄严格检查全部通过。
- 已提交 `586999244 refactor(webserver): isolate diagnostics capability`。
- 配置迁移能力门禁先按预期失败，迁移后转绿；配置导出、导入预览和导入暂存现直接依赖 `ConfigMigrationFacade`。
- webserver 145 项测试和全部集成测试、19 条 daemon 边界门禁、daemon 联合编译、格式、差异与收窄严格检查全部通过。
- 已提交 `b54684148 refactor(webserver): isolate config migration`。
- 设置能力门禁先按预期失败；首次补丁因格式化后的导入行不匹配而完整拒绝，读取真实排版后拆分应用并转绿。
- 设置读取、更新和 relay 探测现直接依赖 `SettingsFacade`；同时修正了与真实探测路由矛盾的旧注释。
- webserver 145 项测试和全部集成测试、20 条 daemon 边界门禁、daemon 联合编译、格式、差异与收窄严格检查全部通过。
- 已提交 `5c26967fb refactor(webserver): isolate settings capability`。
- 审计存储维护接口后停止直接拆 facade：存储归核心所有，统计和清理必须改成稳定核心操作；未为追求搜索命中而把 `StorageFacade` 当作桌面外壳暴露。
- 深读移动 LAN 装配确认旧总入口还承担构造循环的槽位：移动同步设置控制监听器，监听器反过来需要移动同步能力。下一笔用专用一次性槽位替代总 `AppFacade`，不改变 LAN 行为。
- 移动 LAN 总入口门禁先按预期失败，改用只保存弱引用的 `MobileSyncFacadeSlot` 后转绿；旧 `AppFacadeListenerSpawner` 已删除。
- daemon 60 项测试、21 条边界门禁、daemon/desktop/Tauri/CLI 联合编译、格式、差异与严格检查全部通过。
- 已提交 `6d1de2bd8 refactor(daemon): isolate mobile LAN capability`。
- 移动同步 HTTP 门禁先按预期失败；首次实现因链式调用换行未被文本门禁识别，改为显式取得可选能力后转绿。
- 移动设备管理、移动兼容设置和 LAN 网卡列表现直接依赖可选 `MobileSyncFacade`；webserver 145 项测试与全部集成测试、22 条边界门禁、daemon 编译、格式、差异和严格检查全部通过。
- 已提交 `9a4d559b3 refactor(webserver): isolate mobile sync capability`。
- 外壳拆分后重新扫描：后台状态只有一个生产构造点，剩余旧总入口调用基本都是核心业务。下一步从存储统计与清理补稳定核心操作，不再新增子 facade 外壳字段。
- 存储公开契约测试先因操作、结果和统计类型不存在而失败；补契约后编译器继续以运行时匹配不完整阻止半成品。
- 结果投影和稳定错误编号测试先失败，新增核心唯一存储执行模块并接入正式 `Engine::execute` 后转绿；两条测试首次因 `--exact` 运行 0 项，去掉精确筛选后各真实运行 1 项通过。
- 后台存储边界门禁先命中直接 `StorageFacade` 调用而失败，迁移到核心唯一实现后转绿；成功响应字段保持不变，失败响应不再包含底层路径或详情。
- 联合编译发现 iOS 验收宿主未覆盖两个新增结果；补只含分类字节数的稳定 JSON 和测试后，iOS 4 项测试与 daemon/desktop/Tauri/CLI/iOS 联合编译通过。
- `uc-engine`、`uc-webserver`、`uc-daemon` 全套测试通过；23 条 daemon 边界门禁、格式、差异和收窄严格检查全部通过。
- 已提交 `28760635e feat(engine): add storage maintenance operations`、`d6b5b825b refactor(webserver): route storage through engine`、`ad1ecb9ad docs(engine): document storage operations`。

## 2026-07-21 Phase 2 第七次接续

- `planning-with-files` 恢复脚本仍不存在；已按三份规划文件、Git 历史和当前工作区恢复，未丢弃交接中的未提交改动。
- “查询本机设备”已补成稳定核心操作；设备显示名不会出现在调试输出，后台接口失败时也不再返回底层错误详情。
- `uc-engine`、`uc-webserver`、`uc-daemon`、iOS 验收宿主联合测试全部通过；daemon/desktop/Tauri/CLI/iOS 联合编译和三个正式库的严格检查通过。
- 分支 `independent-crates` 已通过名称检查。
- 已提交 `d15ed0868 feat(engine): add local device query`、`79932b784 refactor(webserver): route local device through engine`、`70543841f docs(engine): document local device query`。
- 下一步重新审计剩余旧入口，优先迁移加密状态与锁定相关核心能力；daemon 唯一 `Engine` 所有权、CLI 旧会话和 `uc-bootstrap` 旧公开入口仍未完成。
- 加密状态、锁定和安全存储访问公开契约测试先按预期失败，补统一操作后转绿；锁定动作由核心同时关闭接收入口。
- 后台边界门禁先准确命中三条直接加密调用而失败，改用统一实现后转绿；原有响应字段和 `internal_error` 约定保持不变，底层详情不再返回。
- 真实生产核心流程已验证：创建空间后状态可用，安全存储可读，锁定后会话不可用，再次口令解锁成功。
- `uc-engine`、`uc-webserver`、`uc-daemon`、iOS 验收宿主联合测试通过；桌面相关联编、严格检查和本次文件格式检查通过。
- 已提交 `64b15192f feat(engine): add encryption session operations`、`56082ad66 refactor(webserver): route encryption through engine`、`e96409a6c docs(engine): document encryption operations`。
- 下一步迁移彻底重置动作，保持密钥清除、设置复位和邀请取消的原子语义。
- 彻底重置公开契约和后台边界门禁先按预期失败，新增独立 `FactoryResetSpace` 后转绿；没有把它与仅清除设置的 `ResetSpace` 混用。
- 真实核心流程确认彻底重置会清除密钥、设置状态和待处理邀请，随后能用新口令重新创建空间；成功后接收入口同步关闭。
- 后台仍保留 `KEY_MATERIAL_WIPE_FAILED`、`STORAGE_FAILED`、`INTERNAL` 三种前端错误编号，但不再返回磁盘或安全存储详情。
- 联合测试、桌面相关联编、严格检查和本次文件格式检查全部通过。
- 已提交 `0aa81dd8a feat(engine): add factory reset operation`、`d0bbac570 refactor(webserver): route factory reset through engine`、`edf4f44ba docs(engine): document factory reset`。
- 下一步迁移成员列表与同步偏好，继续缩小 webserver 对旧总入口的依赖。
- 成员公开契约测试先按预期失败，新增偏好查询、局部更新和成员删除操作后转绿；已有设备列表也改用同一稳定错误映射。
- member/pairing 后台边界门禁先准确命中直接 roster 调用而失败，迁移后转绿；两个文件不再了解成员表、信任表和地址表的级联清理。
- 真实核心流程确认默认偏好可查询，局部更新只改指定字段，未提供字段保持原值；删除成员后再次删除稳定返回不存在。
- `codebase-design` 约束本切片只暴露成员偏好和完成结果，不把应用层成员模型或持久化清理步骤放入公开接口。
- 联合测试、桌面相关联编、严格检查和本次文件格式检查全部通过。
- 已提交 `8525b89b7 feat(engine): add member operations`、`b03fc92da refactor(webserver): route members through engine`、`44d40939b docs(engine): document member operations`。
- 下一步重扫剩余旧入口，优先评估搜索查询、标签、状态和重建能否作为同一核心模块迁移。
- 搜索公开契约测试先按预期失败，新增完整查询、标签、状态和重建操作后转绿；公开输入与结果的调试输出均隐藏用户内容。
- 搜索后台边界门禁先准确命中直接搜索和加密状态调用而失败，迁移后转绿；查询词不再写入 tracing 字段。
- 真实核心流程确认发送文本后能按关键词命中完整结果，标签和索引状态查询正常。
- 搜索错误现使用稳定编号映射到原有 HTTP 状态和错误编号，底层内部详情不再返回。
- 联合测试、桌面相关联编、严格检查和本次文件格式检查全部通过。
- 已提交 `2719b5e4e feat(engine): add search operations`、`60bfd745d refactor(webserver): route search through engine`、`f842d70a1 docs(engine): document search operations`。
- 后续重扫发现 WebSocket 搜索初始快照仍直接读取旧搜索对象；扩展边界门禁先失败，改用同一核心状态操作后转绿。
- WebSocket 补漏通过 webserver 全部库测试、严格检查和格式检查；已提交 `a02bd1051 refactor(webserver): route search snapshots through engine`。
- 下一步审计历史、恢复和投递视图，复用已有查询、导出和重发操作，补齐缺失核心动作。

## 2026-07-21 Phase 2 第八次接续

- 已重新读取 `planning-with-files` 和测试先行规则，并核对三份规划文件、最新提交与工作区状态；起点干净，最新提交为 `a02bd1051`。
- 完整目标保持不变：迁完 webserver 核心业务、切换 daemon 为唯一 `Engine` 所有者、删除 CLI 与 `uc-bootstrap` 旧入口，并执行 Plan 003 全部验收。
- 历史接口审计确认现有 `QueryHistory` 只能覆盖概要分页，详情、删除、收藏、统计、资源和清空仍缺稳定核心操作；本轮先测试先行补齐这一组。
- 重发、接收进度、取消和投递视图保留现有丰富结果，后续独立迁移，不以简化结果替代现有行为。
- 已完成历史基础接口字段审计；下一步先增加公开契约失败测试和 webserver 旧调用边界失败测试，再写生产实现。
- 历史公开契约测试先因七个操作和三类结果不存在而编译失败；后台边界测试先准确命中 `require_facade` 旧调用而失败，两道红灯均证明测试覆盖真实缺口。
- 新增历史列表、详情、删除、收藏、统计、资源和清空统一操作；用户内容在结果中完整保留，但调试输出只报告种类、数量和是否存在。
- 清空部分失败只跨统一边界返回失败条目标识，webserver 保持原有二元组响应形状并使用通用失败文案，不再向外传递底层删除异常。
- 七个 HTTP 处理动作已改用 `uc-engine` 唯一实现，旧历史辅助入口和直接调用已删除；边界测试转绿。
- `cargo test -p uc-engine -p uc-webserver --locked` 完整通过：核心 45 项、边界 28 项、宿主 12 项、公开契约 22 项、webserver 146 项及全部集成测试通过，原有压力与文档用例按设计忽略。
- 扩展真实核心流程后首次失败于历史统计：旧实现一次请求 10000 条，但列表用例硬性上限为 1000；这会让有数据的真实统计接口失败。
- 统计根因已修复为每批 1000 条分页累计；真实核心流程现完整验证列表、详情、收藏、统计、资源、删除、重复删除、清空和清空后零统计。
- iOS 验收宿主联合编译先因七种新增结果未穷尽而失败；补只含数量、标识和状态的稳定输出后，4 项测试全部通过，正文、预览、资源 URL 和内联内容不会进入探针输出。
- 最终联合验证通过：`uc-engine`、`uc-webserver`、`uc-daemon`、iOS 验收宿主全部测试无失败；daemon、desktop、Tauri、CLI 与 iOS 联合编译通过。
- 核心、webserver、daemon 正式库严格检查通过；应用层严格检查仅放行仓库已有告警类别后通过，本次统计分页没有新告警。
- 逐文件 `rustfmt --check`、`git diff --check` 和历史旧调用搜索通过；自审进一步删除核心历史错误日志中的原始详情，避免路径或用户内容进入日志。
- 分支名称 `independent-crates` 经 `branch-name-guard` 确认是明确业务名称；准备按核心能力、webserver 迁移、中文文档三笔提交。
- 已提交 `4e6905812 feat(engine): add history management operations`、`e2d4e78c3 refactor(webserver): route history through engine`、`de3afe093 docs(engine): document history management`；工作区恢复干净。
- 下一步继续迁移剪贴板恢复/捕获、接收进度与取消、投递视图、重发和文件传输取消，先按结果完整性重新分组。
- 已将下一切片定为接收进度与取消四个接口；字段和结果可以完整保留，重发与投递视图继续独立处理。
- 接收公开契约测试先因四个操作、进度类型和取消结果不存在而编译失败；后台边界测试先命中 `require_app_facade` 旧调用而失败。
- 首次实现补丁因运行时导入上下文不匹配而整份拒绝；确认没有半成品后按公开契约、统一实现和运行时接入三块应用。
- 单条/列表进度、条目取消和文件传输取消已纳入统一核心；底层错误不进入公开响应或核心日志。
- 四个 HTTP 处理函数已迁移到核心唯一实现，原有进度字段和八种取消结果完整保留；后台边界测试转绿。
- `cargo test -p uc-engine -p uc-webserver --locked` 完整通过，真实核心实例也确认空进度、空列表、未接收和无活动传输四种结果。
- iOS 验收宿主联合编译先命中四种新增结果未穷尽；补稳定进度和取消摘要后 4 项测试通过。
- 最终联合验证通过：核心 46 项、边界 29 项、宿主 12 项、公开契约 23 项、daemon 60 项、webserver 148 项及全部集成测试无失败；原有压力与文档用例按设计忽略。
- daemon、desktop、Tauri、CLI 与 iOS 联合编译通过；核心、webserver、daemon 正式库严格检查和逐文件格式检查通过。
- 差异审查确认后台已无接收进度、条目取消和文件传输取消的旧直接调用；`require_app_facade` 仅剩发送与重发使用。
- 已提交 `75ecaafc2 feat(engine): add receive control operations`、`3d8efc136 refactor(webserver): route receive control through engine`、`d8f1f86da docs(engine): document receive control`；工作区恢复干净。
- 下一步迁移本机剪贴板恢复与捕获动作，保持普通、纯文本和文件路径三种恢复模式及现有错误语义。
- 深读恢复契约后暂停直接迁移：现有 410 结构化字段无法由简化 `EngineError` 无损表达；改为先迁移无缺口的捕获当前剪贴板，恢复另行补稳定业务结果。
- 捕获公开契约和后台边界测试先分别因操作不存在、处理函数直接调用旧捕获对象而失败；新增统一操作并迁移后台后转绿。
- 核心与 webserver 全套测试通过；真实核心实例确认空系统剪贴板返回空记录编号而不是错误。
- iOS 验收宿主联合编译先命中捕获结果未穷尽；补“是否有记录”的稳定结果后 4 项测试通过。
- 捕获切片最终联合测试通过：核心 47 项、边界 30 项、宿主 12 项、公开契约 24 项、daemon 60 项、webserver 150 项及全部集成测试无失败；原有压力与文档用例按设计忽略。
- daemon、desktop、Tauri、CLI 与 iOS 联合编译通过；核心、webserver、daemon 正式库严格检查、逐文件格式和差异检查全部通过。
- 分支 `independent-crates` 经 `branch-name-guard` 确认为明确业务名称。
- 已提交 `40873a36f feat(engine): add current clipboard capture`、`60f25e4c0 refactor(webserver): route current capture through engine`、`436e69ca6 docs(engine): document current clipboard capture`；工作区恢复干净。
- 下一步为三种剪贴板恢复建立稳定业务结果，完整保留现有成功、不存在、不适用、内容丢失和内部失败响应。
- 恢复公开契约测试先因恢复模式、输入、结果和操作不存在而编译失败；桌面边界测试先准确命中三种旧恢复方法的直接调用而失败。
- 新增普通、纯文本和文件路径三种统一恢复模式；成功、内容丢失和模式不适用作为稳定业务结果返回，记录不存在和内部失败继续使用稳定脱敏错误。
- 桌面恢复接口已改用核心唯一实现，原有成功、400、404、410 和 500 响应字段保持不变，内部错误不再进入响应或日志。
- 真实核心流程确认普通与纯文本恢复成功、文本记录的文件路径恢复返回不适用、不存在记录返回找不到；核心映射测试覆盖内容丢失和内部错误脱敏。
- iOS 验收宿主编译先命中恢复结果未穷尽；补只含结果类别和内容状态的摘要后 4 项测试通过，记录编号、表示编号和原因文本不进入探针输出。
- 恢复切片最终联合测试通过：核心 49 项、边界 31 项、宿主 12 项、公开契约 25 项、daemon 60 项、webserver 150 项及全部集成测试无失败；原有压力与文档用例按设计忽略。
- daemon、desktop、Tauri、CLI 与 iOS 联合编译通过；核心、webserver、daemon 正式库严格检查、逐文件格式、差异检查和旧恢复调用搜索全部通过。
- 已提交 `4486fe9b8 feat(engine): add clipboard restore operation`、`a21483e5f refactor(webserver): route clipboard restore through engine`、`69e789782 docs(engine): document clipboard restore`；工作区恢复干净。
- 下一步重扫剩余核心旧调用，优先迁移投递视图，再分别处理重发、文本发送和文件资源读取。
- 投递视图公开契约测试先因操作、来源、目标和状态类型不存在而编译失败；桌面边界测试先准确命中旧投递视图直接调用而失败。
- 投递视图已纳入核心统一操作，完整保留本机、远端、历史来源，以及待处理、已送达、重复、不可达和四类失败原因；设备名和失败补充说明不会进入调试输出。
- 桌面投递视图处理和投影已改用核心稳定结果，应用层投递类型不再跨到接口层；记录不存在仍返回 404，存储失败不再回传底层详情。
- 真实核心流程确认本机文本记录返回本机来源和空目标列表，不存在记录返回找不到；iOS 验收宿主只输出来源类别、目标数量和状态列表。
- 投递视图最终联合测试通过：核心 50 项、边界 32 项、宿主 12 项、公开契约 26 项、daemon 60 项、webserver 150 项及全部集成测试无失败；原有压力与文档用例按设计忽略。
- daemon、desktop、Tauri、CLI 与 iOS 联合编译通过；核心、webserver、daemon 正式库严格检查、逐文件格式、差异检查和旧投递调用搜索全部通过。
- 已提交 `28636cb80 feat(engine): add entry delivery view`、`5d014300a refactor(webserver): route delivery view through engine`、`28ab6bd90 docs(engine): document entry delivery view`；工作区恢复干净。
- 下一步扩充并迁移重发，保留接受、重复、离线、失败和待处理统计，以及所有结构化 4xx 失败。
- 重发公开契约测试先因统计和业务结果类型不存在、现有结果只有记录编号而编译失败；桌面边界测试先准确命中旧重发直接调用而失败。

## 2026-07-21 Phase 2 第九次接续

- 已重新读取 `planning-with-files`、测试先行和实现规则，并核对三份规划文件、正式 Plan 003、Git 历史及未提交差异。
- `planning-with-files` 恢复脚本仍未安装；已直接确认当前唯一未提交切片为重发迁移，没有丢失其他工作。
- 本轮完整完成标准保持不变：后台生产路径只使用唯一 `Engine`，外部旧入口全部删除，Plan 003 每项验收取得当前证据后才更新 Phase 2 状态。
- 当前先完成重发切片的格式、联合测试、真实核心流程、桌面联编、严格检查、接口文档、旧调用搜索和三笔独立提交。
- 重发相关文件已格式化，`git diff --check` 通过。
- `cargo test -p uc-engine -p uc-webserver -p uc-ios-probe-core resend --locked` 通过：核心映射、公开契约、桌面边界和 HTTP 响应测试全部转绿。
- 真实 `Engine` 启动、暂停恢复和记录不存在重发流程通过，缺失记录以稳定业务结果返回。
- 中文接口说明已补充重发适用范围、五类统计、四类业务结果及两类内部错误的脱敏要求。
- 完整联合测试通过：核心 53 项、边界 33 项、真实宿主 12 项、公开契约 27 项、daemon 60 项、iOS 4 项、webserver 150 项及全部集成测试无失败；原有压力与文档用例按设计忽略。
- daemon、desktop、Tauri、CLI 和 iOS 验收宿主联合编译通过。
- 首次严格检查只命中 `uc-engine` 已登记的参数数量、类型复杂度、路径参数和 `expect` 四类旧告警；本次重发文件未产生告警，下一次检查仅放行这些既有类别。
- 仅放行已登记旧类别后的核心、webserver、daemon 正式库严格检查通过；逐文件格式、差异检查和后台旧重发调用搜索通过。
- 分支 `independent-crates` 经 `branch-name-guard` 确认为明确业务名称。
- 已提交 `2440da7f0 feat(engine): preserve clipboard resend outcomes`、`9daf56d89 refactor(webserver): route resend through engine`、`fb9f9552c docs(engine): document clipboard resend`；正式工作区恢复干净。
- 下一步重扫剩余后台核心调用，先迁移必须保留逐目标结果的文本发送，再处理 blob、缩略图和文件资源读取。
- 全量审计确认文本发送当前直接返回快照摘要、时间、五类总数和逐目标结果，而核心 `EntrySent` 只有记录编号；下一步先用失败契约固定完整发送结果，再扩充唯一发送路径。
- 为避免继续保留两套发送编排，HTTP 最终切换放在 daemon 真正持有唯一 `Engine` 时完成；在此之前先补齐发送与资源公开契约。
- 完整发送结果公开契约测试先因发送报告、逐目标类型不存在且 `EntrySent` 仍只有记录编号而编译失败，红灯准确覆盖真实字段缺口。
- 首次实现后联合编译命中移动兼容发送适配器的穷尽匹配；它只消费既有统计，正在明确忽略新增字段后重跑。
- 公开发送契约转绿；随后新增真实映射测试并先因唯一转换函数不存在而失败，开始把内联转换收敛为可验证单一实现。
- 发送结果映射测试、核心与 iOS 全套测试通过；应用层 843 项单元测试、10 项集成测试全部通过。
- 核心正式库严格检查通过；应用层首次严格检查只命中 81 处已登记旧告警，本次改动文件没有新增告警，正在按既有类别收窄重跑。
- 仅放行已登记旧类别后的应用层严格检查通过；格式和差异检查通过。
- 已提交 `a2262d0e2 feat(application): preserve outbound dispatch details`、`fc5927494 feat(engine): expose clipboard send outcomes`、`6ac304310 docs(engine): document clipboard send outcomes`；后台监听器兼容修复已并回第一笔，三笔提交均保持独立可编译。
- 下一步为 blob、缩略图和文件资源建立稳定核心读取结果，保留媒体类型、文件下载名、不同未找到提示和现有并发内存门禁。
- 二进制资源公开契约先因三类结果与结果类型不存在而编译失败；桌面边界门禁先准确命中 `.resource.blob` 旧直接调用而失败。
- 三类资源契约、桌面边界、错误编号和真实文件读取均已转绿；完整联合编译随后发现 daemon 剪贴板监听器未忽略新增发送结果字段，正在补兼容匹配并重跑。
- 资源切片完整联合测试和桌面相关联编通过；严格检查发现本次新增的一处多余引用，已直接修正并准备重跑。
- 修正后核心、webserver、daemon 正式库严格检查、逐文件格式、差异检查和旧资源直接调用搜索通过。
- 已提交 `9f2bcf221 feat(engine): add binary resource reads`、`4085ee3fd refactor(webserver): route binary resources through engine`、`33891eb5c docs(engine): document binary resource reads`；正式工作区恢复干净。
- 下一步进入 daemon 唯一核心所有权切换：先用失败门禁要求生产状态持有 `Engine` 并删除 `AppFacade`，再逐模块把已迁移操作改为直接执行公开核心操作。
- 所有权切换前审计发现桌面尚无 `HostCapabilities` 生产构造入口；下一切片先测试先行建立只做路径、安全存储和系统剪贴板适配的桌面宿主准备模块，不在其中装配核心。
- 桌面宿主准备契约测试先因 `DesktopEngineHost` 和唯一准备函数不存在而编译失败，红灯准确命中缺失入口。
- 首次实现编译发现剪贴板表示转换先移动媒体类型后又借用来源；正在按 Rust 所有权顺序先取来源再移动字段。
- 桌面宿主文件句柄已接入核心剪贴板读取：先写失败测试准确命中缺少文件读取能力和格式字段，再实现 64 KiB 分块导入、匿名临时路径和加密显示名元数据。
- 文件型剪贴板适配测试覆盖 70 KiB 跨块内容、原格式与媒体类型、匿名磁盘路径、显示名保留和调试输出脱敏，现已通过。
- 关闭清理先撤掉实现取得真实失败断言，再补回核心关闭清理；完整 `Engine` 流程确认捕获成功且关闭后临时导入目录消失。
- 宿主接口、桌面准备入口和桌面不透明文件句柄针对性测试全部通过。
- `uc-engine` 与 `uc-bootstrap` 全套测试通过：核心 55 项、核心边界 34 项、真实宿主 14 项、公开契约 29 项、桌面准备 33 项及全部相关集成测试无失败；局域网多播用例按设计忽略。
- daemon、desktop、Tauri、CLI 与 iOS 验收宿主联合编译通过。
- 严格检查首次仅命中核心与桌面准备模块已登记的六类旧告警；仅放行这些类别后两个正式库均无新增告警。
- 差异检查发现格式工具误触三处无关旧文件，已逐处恢复，当前工作区只剩文件剪贴板和桌面宿主准备相关改动。
- 分支 `independent-crates` 经 `branch-name-guard` 确认为明确业务名称，准备按核心文件剪贴板能力与桌面宿主准备两笔提交。
- 已提交 `29328406d feat(engine): import host clipboard files` 与 `9d16957fc refactor(bootstrap): prepare desktop engine host`，工作区在下一切片开始前恢复干净。
- 宿主变化通知契约测试先因通知类型、单消费者流和宿主取流方法不存在而编译失败；移除测试自身多余依赖后红灯只命中目标缺口。
- 核心现已消费宿主变化信号，统一执行加密门禁、来源判断、捕获、活动状态、刷新、搜索和发送；完整核心测试确认变化内容可搜索、刷新事件可见且关闭会停止通知流。
- 桌面待处理快照测试先因快照槽、单消费者保护和通知启停状态不存在而编译失败；实现后确认平台事件快照优先于重新读取系统剪贴板。
- 核心与桌面准备全套测试再次通过：核心 55 项、核心边界 34 项、真实宿主 15 项、公开契约 29 项、桌面准备 34 项及相关集成测试无失败。
- 通知发送改为核心会话登记的可取消后台任务，慢对端不会阻塞后续系统剪贴板事件；暂停和关闭会等待或取消这些发送。
- daemon、desktop、Tauri、CLI 与 iOS 验收宿主联合编译通过；仅放行六类已登记旧告警后的核心与桌面准备正式库严格检查通过。
- 已提交 `a26a5a4f1 feat(engine): process host clipboard changes` 与 `499d95e03 impl(bootstrap): stream desktop clipboard changes`。
- 中文接口说明已补充单消费者通知、锁定与暂停语义、平台快照接力、匿名文件导入和监听器关闭规则。

## 2026-07-21 Phase 2 历史维护迁移方案复核

- 用户追问为何需要保留旧入口后，重新核对 Plan 003、daemon 服务装配和核心会话装配。
- 确认转发型 `CleanupWorker` 只服务于分段迁移，不是目标架构必需；将其写入正式门禁会留下不必要的过渡接口。
- 下一步改为把核心历史维护任务登记与 daemon 所有权切换一起完成，并在同一切片删除 `CleanupWorker` 和对应服务装配；不提交旧入口转发层。
- 当前未提交边界测试需要先改成验证最终所有权，不能继续要求 daemon 的旧 worker 调用核心函数。

## 2026-07-21 Phase 2 daemon 最终所有权审计启动

- 已核对当前分支、最近提交、Plan 003、未提交测试和 Cargo 工作区根目录；生产代码仍停在通知阶段提交，只有一项过渡型门禁未提交。
- 已重扫 daemon、webserver、bootstrap、CLI、desktop 和 Tauri 对 `Engine`、`AppFacade` 与旧运行时的引用。
- 确认下一步需先建立 daemon 的最终核心状态和退出顺序，再迁移接口与桌面职责；不能把三个后台工作者当成孤立删除项。
- 已统计 webserver 旧对象取得点并区分两类：已有稳定核心操作的通用业务，以及仍应留在桌面外壳的设置、升级、诊断、移动兼容与连接状态。
- 下一步继续核对启动装配输出，确定桌面专属服务的最窄输入和 `Engine` 的创建、事件转发、关闭顺序。
- 已确认 `host::run` 是唯一安全切换点：旧 `wire_dependencies` 与新 `Engine::start` 都会创建完整核心，不能并存。
- 已确认新核心内部已覆盖 blob、搜索、剪贴板、文件超时和 P2P 生命周期；这些 daemon 旧服务应随所有权切换删除，而不是转发。

## 2026-07-21 Phase 2 核心历史维护

- 已把未提交的过渡型门禁改为最终核心所有权门禁，并运行得到准确红灯：核心会话未登记历史维护任务。
- 新增核心内部历史维护循环，保持启动立即执行、每五分钟执行、先核对缺失文件再清缓存和执行保留策略的既有顺序。
- 新增行为测试确认核对失败会阻止后续删除，缓存或保留策略失败不会改变固定顺序；门禁与两个行为测试均已转绿。
- 首次编译命中结果类型导入层级错误，已按实际公开模块修正并记录；下一步运行核心全套测试与严格检查。
- 自审发现首次维护原本可能与剪贴板、搜索和文件任务竞态；新增失败门禁后调整为启动核对完成再启动其他核心任务，周期任务从五分钟后开始。
- `uc-engine` 全套 57 项单元测试、35 项桌面边界、15 项真实宿主、29 项公开契约及其余集成测试全部通过；严格检查和差异检查通过。
- 分支 `independent-crates` 经检查为明确业务名称；历史维护切片已完成提交前审阅。
- 已提交 `b1cbfe305 feat(engine): own history maintenance`，提交后工作区干净。
- 继续审计 daemon 最终切换所需的非核心能力，已将接口调用分成可直接执行稳定核心操作与必须留在桌面外壳的两组。
- 已复核 ADR-005：拒绝新增桌面专用第二启动入口或内部服务包；继续按唯一 `Engine` + 稳定操作边界推进。
- 已完整读取 daemon 旧保活工作者及其测试，确认可无桌面依赖迁入核心；同时发现即时唤醒拨号未纳入统一关闭管理，迁移时一并修正。
- 核心保活会话登记门禁先按预期失败；实现后门禁与四项退避状态测试转绿。
- 迁移保留 25 秒基础周期、60 秒和 5 分钟退避、第三次失败休眠、30 分钟兜底探测与 Online 唤醒；即时拨号和常规拨号现在都由父任务拥有并响应取消。
- 新实现日志不再记录设备标识或底层错误详情；下一步运行完整核心回归、严格检查和差异审阅。
- `uc-engine` 全套测试通过：61 项单元测试、36 项桌面边界、15 项真实宿主、29 项公开契约及其余集成测试无失败；严格检查与差异检查通过。
- 差异审阅确认核心模块不再使用脱离任务所有权的后台启动，也不记录设备标识和底层拨号错误；准备独立提交。
- 已提交 `632c15567 feat(engine): own peer keepalive`，提交后工作区干净。
- 核心现已拥有历史维护、peer 保活和文件传输超时；daemon 对应旧工作者将在唯一所有权切换时直接删除。
- 已审计 daemon 的 peer 快照与主动刷新路径，确定下一切片新增两个稳定核心动作，并保持现有 HTTP/WS 字段不变。
- peer 连接公开契约先因两种操作、两种结果和连接方式枚举不存在而准确编译失败。
- 已实现 peer 快照查询与主动刷新，完整保留连接通道、地址、配对和在线字段；调试输出只含数量和布尔状态。
- 公开契约与四态映射、错误脱敏测试已转绿；下一步联合编译所有宿主并补齐穷尽匹配。
- 联合编译准确发现 iOS 验收宿主未覆盖两个新增结果；补数量、在线数和连接方式摘要后四项宿主测试与桌面相关联合编译通过。
- iOS 输出测试确认 peer 标识、设备名、候选地址和当前连接地址均不会进入诊断结果。
- `uc-engine` 全套 63 项单元测试、36 项桌面边界、15 项真实宿主和 30 项公开契约全部通过。
- 联合严格检查只命中 iOS 验收宿主两处既有安全说明缺失；将按已知类别收窄复查本次改动。
- 核心严格检查通过；iOS 验收宿主仅放行两处既有安全说明类别后也无其他告警，格式与差异检查通过。
- 已提交 `9784bf959 feat(engine): expose peer connection status`，提交后工作区干净。
- 开始审计生命周期与设置接口，确认设置读写归核心，运行时 telemetry/analytics gate 与接收准备仍归桌面外壳。
- 已完整核对应用设置 view/patch 与 daemon DTO 投影，确定稳定核心设置契约的字段、时长单位、三态清空和错误分类。
- 设置公开契约先因三种操作、三种结果和稳定类型不存在而准确编译失败；现已补完整边界类型与唯一映射。
- 查询、更新、三态清空、超大保留数量拒绝、中继分类和敏感调试输出测试转绿；本次多余导入告警已直接清除。
- 联合编译发现 iOS 验收宿主未覆盖设置结果；补只含版本、开关、数量与结果类别的输出后四项测试及桌面相关联合编译通过。
- 设置切片完整核心测试全部通过；随后严格检查发现设置更新输入与成功结果会放大所有公开操作值的内存占用，现已改为间接承载且不改变外部数据形状，准备重新执行全部验证。
- 重新验证时功能测试、移动验收宿主和桌面联编通过；严格检查继续指出总结果仍直接承载完整设置快照，已补齐最后一处间接承载，外部数据形状不变。
- 设置切片最终验证通过：核心 66 项单元测试、36 项桌面边界测试、15 项真实宿主测试、2 项宿主契约、3 项明文扫描、31 项公开契约及文档测试全部通过；iOS 验收宿主 4 项测试、桌面相关联合编译、两个正式库严格检查、格式和差异检查均通过。
- 最终差异审阅确认设置字段完整，时长、三态更新、32 位数量溢出、relay 结果分类和调试脱敏均符合既定契约；当前切片可以独立提交。
- 设置能力已独立提交：`62bc5833d feat(engine): own application settings`。
- 开始迁移网页设置接口；源码核对确认网页状态尚未持有新核心，现有业务委托仍借用旧运行对象。下一步先确定 daemon 唯一核心的真实装配切换点，禁止为了单个接口并行启动第二套核心。
- 长输出一度看似重复登记对端保活，按源码行和提交归属复核后确认只有一次，属于截断拼接假象，未做无效修改。
- 唯一核心切换必须同时解决网页通用业务、配置迁移、诊断、升级和移动兼容对旧装配的依赖；下一步逐类建立稳定核心动作，桌面外壳只保留 HTTP、运行时统计开关和 LAN 监听机制。
- 升级公开契约测试先因动作、四类状态和确认结果不存在而准确编译失败；现已补由 `EngineConfig` 版本唯一驱动的升级查询与确认映射。
- 首轮验证发现内部测试不应为构造版本对象新增依赖，已改为覆盖终态映射；iOS 验收宿主缺少升级结果分支，已补稳定类别与版本输出。
- 升级切片最终通过核心 67 项单元测试、36 项桌面边界、15 项真实宿主、2 项宿主契约、3 项明文扫描、32 项公开契约及文档测试；桌面相关联合编译、iOS/Android 目标编译、核心与 iOS 验收宿主严格检查、格式和差异检查均通过。
- 升级能力已独立提交：`ace71e1e4 feat(engine): own upgrade status`。
- 诊断公开契约先因状态、调试开关更新和日志句柄导出动作不存在而准确编译失败；现已补核心诊断动作，并建立可供配置迁移复用的临时文件到宿主句柄写出工具。
- 诊断核心严格检查通过；iOS 验收宿主按预期因新增结果匹配不完整而失败，已补脱敏摘要，并增加超过两个分块的真实宿主句柄写出测试。
- 真实生产核心已验证诊断状态读取、调试开关持久化与再次读取、日志包写入宿主句柄和完成收尾；完整核心、桌面联编、iOS/Android 目标、严格检查、格式和差异检查全部通过。
- 诊断能力已独立提交：`48e21e047 feat(engine): own diagnostics`。
- 配置迁移公开契约先因三种句柄动作、预览和暂存结果不存在而准确编译失败；现已接入双向分块复制、随机匿名临时目录和可区分业务结果。
- 配置迁移核心严格检查通过；iOS 验收宿主按预期因新增结果匹配不完整而失败，已补结果类别和身份字段存在性摘要，不输出资料空间标识、设备指纹或不兼容原因。
- 真实配置往返首次在预览元数据断言失败；核对发现配置包版本仍由基础设施包写死，已改为使用 `EngineConfig` 的唯一版本，存储模式则继续按真实宿主环境生成。
- 根因修正后真实配置导出、预览和暂存往返通过；另补未初始化导出拒绝与错误密码预览拒绝。`uc-infra` 严格检查仅命中六处既有无关告警，将按已登记类别收窄复查。
- 配置迁移切片完整验证通过：`uc-infra`、`uc-bootstrap`、`uc-engine` 和 iOS 验收宿主全部测试无失败；桌面相关程序联合编译、iOS 与 Android 目标编译、核心与验收宿主严格检查、格式和差异检查均通过。
- 下一步逐文件审阅配置迁移差异，剔除格式工具误触的无关文件后独立提交；随后直接切换 daemon 的唯一核心所有权，不新增或保留旧入口转发层。
- 已剔除自动排版误触的三个无关基础设施文件，配置迁移差异审阅和暂存区检查通过。
- 分支 `independent-crates` 经 `branch-name-guard` 确认为明确业务名称；配置迁移已提交为 `05d10e10c feat(engine): own configuration migration`。
- 下一步直接进入 daemon 单核心所有权切换，不建立旧入口兼容层。
- 新增 daemon 最终所有权三项门禁，分别要求生产宿主只启动并关闭一个 `Engine`、daemon 与 HTTP 状态不再持有 `AppFacade`、三类核心已接管 worker 不再装配。
- 三项门禁均已取得准确红灯：当前生产宿主仍调用旧进程装配，daemon/API 状态仍持有旧对象，清理、保活和文件同步 worker 仍在装配。
- daemon 最终门禁复核后确认移动 LAN 兼容仍缺稳定核心动作；切换顺序调整为先补移动凭据与内容操作，再一次性替换旧生产路径，旧入口不作为兼容层新增或保留。
- 移动凭据公开契约先因列表、撤销、鉴权、复查动作和脱敏凭据类型不存在而准确编译失败。
- 已补第一组移动凭据契约与核心会话映射；首次编译只命中同一入站能力的具体引用向既有抽象接口转换问题，修正后公开契约转绿。
- 真实生产核心已验证空移动设备列表、撤销不存在设备、无效授权拒绝和伪造凭据复查失败；移动验收宿主先因结果分支不完整而失败，补脱敏摘要后四项测试通过。
- 第一组移动凭据能力完整验证通过：核心全套测试、桌面相关联编、iOS 与 Android 目标、核心严格检查、格式和差异检查均无失败。
- 中文接口说明已明确移动凭据动作只服务用户显式启用的 LAN 兼容通道，HTTP 监听、网卡选择和端口管理仍归桌面外壳，凭据校验材料不得进入调试输出。
- 第一组移动凭据能力已提交为 `f29d9c155 feat(engine): own mobile credentials`。
- daemon 最终所有权的三项红灯已记录但未留在工作区，避免后续移动兼容切片的完整测试被预期失败阻断；真正进入一次性切换时将重新建立同样的最终门禁。
- 下一步补移动设备登记与编辑，再补 LAN 内容读写和流式文件上传；这些能力齐全后直接切换 daemon，不保留旧入口。

## 2026-07-21 Phase 2 移动设备管理续作

- 本轮完成标准：移动同步设置可经统一核心查询和修改；真实核心流程先开启移动同步与 LAN 监听，再完成空标签拒绝、设备登记、改名、自动换密和列表复查；公开结果保持脱敏；核心、桌面、iOS、Android、严格检查、格式和差异检查全部通过；记录更新并独立提交。
- `planning-with-files` 恢复脚本在当前安装中仍不存在；已改用三份规划文件、Git 状态、当前差异和提交历史恢复。
- 当前有 7 个未提交文件，均属于移动设备登记与编辑切片；公开契约已经转绿，真实核心流程因持久设置中的移动同步和 LAN 监听仍关闭而返回“监听未启用”。
- 已确认通用应用设置不包含这两个移动兼容开关；下一步先写移动同步设置的公开失败契约，再补唯一核心查询与修改动作，不绕过既有登记规则。
- 移动同步设置公开契约已按预期编译失败，准确缺少查询与修改动作、设置类型和结果类型；失败未混入其他原因，开始复用现有移动设置规则补最小实现。
- 已新增独立的移动同步设置查询与修改动作，复用现有校验和持久化规则；公开契约 1 项转绿，地址、监听失败和安装提示均不会进入调试输出。
- 真实核心流程在修改测试前再次准确返回“监听未启用”；现已按真实用户顺序先查询默认关闭状态、开启并保存两个开关、复查设置，再报告监听地址和登记设备。
- 修改后的真实流程 1 项通过，覆盖空标签拒绝、正常登记、改名、自动换密和列表反映更新；既有登记门禁未被弱化。
- iOS 验收宿主联合编译按预期因两个新增结果未覆盖而失败；已补只含开关、端口、地址/错误存在性、安装方式数量和更新类别的稳定摘要。
- iOS 验收宿主 4 项测试全部通过；地址、监听失败原因、安装提示和设置拒绝原因均未进入输出。
- `uc-engine` 全套测试通过：68 项单元测试、36 项桌面边界、15 项真实宿主、37 项公开契约及其余集成测试无失败。
- 全仓格式检查除本次七个文件外仍命中三处既有无关差异；本轮只格式化当前切片文件，并用差异范围检查防止误触。
- 本次七个文件逐文件格式与差异检查通过；`uc-engine` 全套测试复跑无失败，daemon、desktop、Tauri、CLI 和 iOS 验收宿主联合编译通过。
- `uc-engine --no-deps` 严格检查直接通过；iOS 验收宿主仅命中两处已登记的旧安全说明缺失，下一步只放行该类别复查。
- iOS 验收宿主放行两处已登记旧安全说明后严格检查通过；`uc-engine` 的 iOS 与 Android 目标编译均通过。
- 新增真实断言确认非法端口被拒绝且不会开启设置，相同设置重复提交报告“没有变化”；真实核心流程复跑通过。
- 中文核心接口文档已补登记、编辑、设置和监听结果报告，并明确设置保存与桌面 listener 启停是两步。
- 问题类型为结构性缺口：登记流程所需能力尚未进入稳定核心操作面。实现复用现有移动设置和设备管理规则，没有复制校验、持久化或监听控制，也没有新增旧入口或兼容转发层。
- 分支 `independent-crates` 经 `branch-name-guard` 确认为明确业务名称；8 个文件的逐文件格式、差异范围和提交前检查均通过，准备独立提交。
- 移动设备管理切片已提交为 `6af0d4023 feat(engine): own mobile device management`；提交后工作区干净。
- 下一步补内容是否存在、最新同步文档、写入同步文档、文件读取，以及开始、追加、完成、放弃上传八类稳定核心动作；这些动作完成后进入 daemon 唯一核心切换。

## 2026-07-21 Phase 2 LAN 内容与流式上传

- 已审计现有移动兼容能力，确认八类底层业务均已存在；本切片只建立稳定核心动作与不透明上传编号，不重写内容、入站或文件规则。
- 公开契约先准确编译失败，命中八种动作、文档/文件结果和上传句柄类型缺失；现已补稳定类型、脱敏调试输出和运行时映射，公开契约 1 项转绿。
- 首次真实文件完成操作返回稳定内部错误；根因是底层需要 HTTP 外壳预先建立临时接收记录，直接核心调用缺少该前置。
- 核心现在在完成上传时保证临时接收关联存在；HTTP 进度与媒体类型嗅探仍留在桌面外壳。修正后真实流程 1 项通过，覆盖空状态、文本读写、内容存在性、两段文件上传、文件读回、主动放弃和暂停后旧编号失效。
- iOS 验收宿主联合编译按预期因八个新增结果未覆盖而失败；已补仅含类型、大小、字段存在性和结果类别的稳定摘要。
- iOS 验收宿主 4 项测试通过，正文、文件名、hash、content id、解码原因、媒体类型和文件字节均未进入输出。
- 首次完整核心回归在与严格检查并行运行时，仅有既有剪贴板通知测试等待超时；下一步单独重跑确认是否为资源争用，不修改本次业务代码。
- 核心严格检查只命中已登记的 8 处类型复杂度、参数数量、路径参数和 `expect` 旧告警，本次新增文件与上传状态没有新增告警。
- 接续会话重新单独运行 `uc-engine` 全套测试时，仍只有既有剪贴板通知用例在 10 秒等待内超时；其他 68 项单元测试、36 项桌面边界和 15 项真实宿主测试均通过。由于本次并非与其他编译并行，先单独复跑并检查稳定性，不能继续仅归因于资源争用。
- 剪贴板通知用例单独复跑通过，随后 16 项真实宿主测试整组复跑也通过；确认是间歇性等待超时，本切片业务路径无失败。
- 桌面相关程序联合编译、iOS 验收宿主 4 项测试、核心与 iOS 验收宿主严格检查、iOS 和 Android 目标编译均通过。
- 提交前逐文件审阅发现公开上传编号仍复用底层 UUID，且同一编号的并发追加未由核心串行化；这两项属于本切片自身的边界缺口，先补失败测试和根因修正，再重新执行全部验证。
- 两条公开编号所有权测试先因独立生成函数不存在而准确编译失败；实现后确认公开编号带稳定版本前缀、每次唯一且不等于底层 staging UUID。
- 每笔活动上传现由独立状态锁串行化；完成、放弃、追加失败和暂停都会消费同一活动状态，已经取得引用但迟到的操作也不能再次使用底层句柄。
- 编号测试 2 项与真实移动内容流程 1 项转绿；真实流程继续覆盖分段上传、文件文档落地、文件读回、主动放弃和暂停后旧编号失效。
- 完整核心套件再次稳定复现剪贴板通知超时；按系统排障流程把复现缩到“移动内容流程 -> 新核心实例剪贴板通知”两项顺序运行，单独通知测试连续 10 次均通过。
- 临时计数确认通知读取 1 次、剪贴板读取 1 次、查询完成 400 余次但历史总数仍为 0；源码核对定位到 `uc-infra` 的进程级自写归因单例继承了上一实例未被宿主回声消费的 60 秒远端 fallback。
- 新增回归测试先稳定超时，准确证明新实例继承旧归因；改为每个 `Engine` 装配独立 ledger 后转绿。清除临时诊断计数后，原始两测试顺序复现也通过。
- 根因修正后的 `uc-engine` 全套测试通过：70 项单元测试、36 项桌面边界、17 项真实宿主、2 项宿主契约、3 项明文扫描、38 项公开契约及文档测试均无失败；此前不稳定的原始测试顺序在完整套件中通过。
- `uc-infra` 全套通过：602 项单元测试、各项数据库/网络集成测试和 16 项文档测试无失败；桌面联合编译、iOS/Android 目标、核心与移动验收宿主严格检查也全部通过。
- 为保持提交单一意图，实例隔离修复先提交为 `e2b517f07 fix(engine): isolate clipboard attribution by instance`；暂时收起功能改动后，该提交独立通过回归测试和核心编译。
- LAN 内容与流式上传提交为 `8e5dca2ed feat(engine): own mobile LAN content`；当前进入 daemon 唯一核心所有权切换。
- 重新建立三项 daemon 最终门禁并取得准确红灯：生产宿主尚未启动/关闭 `Engine`；网页状态仍持有 `AppFacade`；应用装配仍创建清理、peer 保活和文件同步三个已迁入核心的 worker。
- 继续审计 daemon 最终切换：确认不能只替换网页状态字段。新核心业务操作已经覆盖主要 HTTP 面，但生产事件尚未完整替代旧 daemon 的入站正文、传输终态、投递、peer 和搜索通知；已把补齐稳定事件边界列为切换前置，避免 CLI `watch` 和实时界面静默退化。
- 已重新检查当前半完成工作区：`cargo check -p uc-webserver` 通过，仅有 5 个未使用项告警。下一步从网页 API 编译收尾转入移动 LAN 单核心迁移；明确不新增、不保留旧入口转发层，旧装配只在实时事件、listener 和文件句柄职责完成替换前暂存。
- 网页 API 收尾完成：删除移动设备接口残留的旧 facade 错误转换，升级失败日志改为稳定核心错误类别，生命周期 ready 明确为客户端幂等确认；`cargo check -p uc-webserver` 无告警，`cargo test -p uc-webserver --lib api::` 86 项通过。
- 资源读取测试仍引用已私有化的核心错误编号，测试编译准确失败；已改为只验证公开错误类别与既有 HTTP 返回，不再跨层依赖内部编号。
- 三项设置边界回归测试仍直接装配旧 `SettingsFacade`，全套测试编译准确失败；已改为只覆盖网页 DTO 与公开核心设置类型的双向转换，设置保存规则继续由所属层测试负责。
- `uc-webserver` 全套验证通过：149 项库测试、1 项端口复用、5 项网络设置、7 项保留规则、3 项恢复同步设置和文档测试均无失败；1 项约 60 秒压力测试按既有标记忽略。网页 API 的 Engine 迁移收尾完成，进入移动 LAN。
- 新增移动 LAN 生产所有权门禁并取得准确红灯：鉴权、路由、server、历史、文件和 SSE 仍直接持有 `MobileSyncFacade` / `FileTransferFacade`。
- 移动 LAN 生产路径已改为共享 `Engine`：Basic Auth、凭据复查、内容存在性、最新文档、文档写入、文件读取、分块上传和放弃都经稳定核心操作；HTTP/SSE 格式不变。
- 测试使用私有执行替身，不改变生产所有权；`cargo check -p uc-webserver` 通过，`uc-webserver` 全套再次 149 项通过，移动 LAN 单核心门禁转绿。
- 当前已明确的下一缺口：旧 PUT 文件路径会报告开始、节流进度和失败，核心上传操作目前只在完成时建立传输关联；必须先把完整生命周期移入核心，不能以协议测试通过代替实时行为验收。

## 2026-07-21 Phase 2 移动 LAN 上传生命周期续作

- 本轮完成标准：核心拥有移动 LAN 文件上传的开始、进度、完成与失败行为；HTTP 外壳只保留协议解析；先取得准确失败测试，再让相关测试和完整回归通过；更新三份记录，不提前切换 daemon。
- 按 `planning-with-files` 先运行会话恢复脚本；技能安装目录缺少该脚本，未重复失败命令。
- 已直接读取三份规划文件并核对 Git：当前 42 个相关文件未提交，Plan 003 仍在 Phase 2。
- 已确认上一阶段已完成网页 API 与移动 LAN 请求迁移及完整测试；本轮从上传生命周期所有权缺口继续。
- 已阅读 `VISION.md` 与工作流规则，确认本轮必须修复双重状态所有权的根因，并保持文件元数据密文与日志脱敏边界。
- 已阅读架构与端口规则：后续从用户可见上传行为反推最小事件边界，事件发布与状态存储保持分离，避免扩大无关消费者的依赖面。
- 已阅读 Rust 与 crate 规则：生产代码不新增 `unwrap`/`expect`，异步失败必须可观察且脱敏，所有 Cargo 验证从仓库根目录执行。
- 已复核此前同仓库交付记录：本轮仍按行为拆分改动，局部测试转绿后继续跑相关完整套件、构建和差异审计。
- 已对照移动 LAN 文件路由的新旧差异：旧生命周期调用已删除，但核心尚未补齐进度与失败行为；开始参数已经预留设备、传输和总字节信息，可从此处完成所有权收敛。
- 已确认核心公开开始输入已携带生命周期上下文，最终成功仍由后续同步文档应用负责；接下来定位核心活动上传状态和既有事件发布器，先写真实行为测试。
- 已定位核心运行时与事件适配器：现成生命周期服务和事件流可以直接复用；当前接口迁移尚未接完，开始输入与运行时读取位置不一致。
- 已确定最小行为测试：真实核心开始上传后出现刷新事件，节流窗口后追加出现正确进度，结束补最终进度，放弃出现失败刷新；成功终态仍由同步文档应用负责。
- 已确认测试可直接读取真实 `Engine` 事件流；将用带超时的条件读取跳过启动事件，避免脆弱地断言固定事件序列。
- 已新增真实上传生命周期行为测试并运行；首次红灯准确命中完成操作仍读取已从公开输入移除的两个字段，证明当前接口迁移确实未接完。
- 最小接线后第二次红灯在等待开始通知处超时，证明核心尚未接管生命周期。进一步确认早期状态事件因无历史记录编号会被现有发布器跳过；开始通知改由零字节进度验证，失败事件边界另行核对后再决定。
- 新增核心所有权门禁并运行，准确因运行时缺少进度动作失败；网页路由已不含旧生命周期调用，红灯原因单一。
- 完成最小核心编排后，真实生命周期行为测试 1 项转绿，覆盖开始 0 字节、追加 3/3、结束最终 3/3 和显式放弃清理。
- 核心所有权门禁 1 项转绿，确认运行时拥有建立投影、开始、进度和失败动作，移动 LAN 文件路由不再引用旧传输服务或辅助函数。
- 核心与网页服务联合编译通过，但发现历史表单解析结果仍保留已移交核心的传输编号，产生 1 个未使用字段告警；同时确认该入口仍把客户端文件名写入日志。
- 新增隐私门禁并取得准确红灯；现已删除历史表单日志中的文件名、移除多余传输编号字段，并把累计字节转换改为显式错误处理。
- 隐私与所有权门禁复跑通过；真实上传生命周期行为测试复跑通过，日志清理未影响开始、进度、结束和放弃流程。
- 已只格式化本轮涉及文件，`git diff --check` 通过；差异审阅后调整会话确认顺序，并让显式放弃能够上报失败状态写入错误，暂停清理保留脱敏告警。
- 去除重复失败处理后重新格式化；`uc-engine` 与 `uc-webserver` 联合编译无告警通过。
- `uc-webserver` 完整套件通过：149 项库测试、1 项端口复用、5 项网络设置、7 项保留规则、3 项恢复同步设置均通过；1 项约 60 秒压力测试按既有标记忽略。
- `uc-engine` 完整命令中 70 项库测试通过，随后 29 项 daemon 最终切换预置门禁按预期失败；本轮新增上传行为、所有权和隐私门禁均通过。
- 排除 `desktop_host_boundary` 的下一阶段预置红灯后，核心其余套件全部通过：70 项库测试、18 项真实宿主、2 项宿主契约、3 项明文扫描和 38 项公开契约均无失败；连续重启十次与身份不变也通过。
- 使用模块接口审计重新分类 29 项红灯：多数仍验证已淘汰的内部函数入口，需要改为公共 `Engine` 操作门禁；真正未完成的是 daemon 单核心启停、旧装配和三个重复 worker。
- 门禁改为正式 `Engine` 操作后，41 项中 34 项通过；修正加入/切换共用 `JoinSpace` 操作的错误假设后，预计剩余 5 项是真实生产切换缺口。
- `cargo check -p uc-daemon --locked` 取得 4 个明确编译错误，全部指向新网页接口与旧 daemon 装配尚未接通，没有发现核心或网页服务内部错误。
- 开始补 daemon 接线的第一前置：桌面宿主共享文件句柄登记器。先用失败测试固定输入可读、输出可写和句柄不泄露路径，再接网页适配器。
- 共享文件句柄失败测试先准确缺少公共接口；两次编译分别补齐标准文件类型和写入 trait 后转绿。
- `uc-bootstrap` 34 项库测试与 3 项桌面宿主契约全部通过；严格检查仅命中旧观测代码三类已知告警，本轮文件无编译告警。
- 全目标严格检查还命中多份旧端到端测试告警；收窄到正式库并只放行已登记的终端输出、解包和测试映射三类旧告警后通过。
- 当前 daemon 最终切换门禁已从 29 项不准确红灯收敛到 5 项真实缺口；下一步先接共享文件句柄与共享 `Engine` 的移动 LAN spawner，再替换进程启动旧装配和三个重复 worker。

## 2026-07-21 当前变更提交收尾

- 用户要求提交当前所有 Git 变更，并明确跳过测试执行；本轮完成标准是按单一目的拆分提交、每笔通过暂存差异检查、最终工作区干净，不推送。
- `planning-with-files` 恢复脚本在当前安装目录中不存在；已直接读取三份规划记录并以 Git 当前状态恢复上下文。
- 分支 `independent-crates` 经名称检查为含义明确的本地分支，无需改名。
- 当前 46 个文件共 2307 行新增、1924 行删除；全量 `git diff --check` 通过。
- 提交边界确定为三组：核心接管手机上传过程、服务入口统一使用共享核心、桌面共享文件句柄。
- 已完成三笔单一目的提交：`21e4ac181`、`952afe0a1`、`ed8c6b56e`；每笔提交前的暂存差异检查均通过。
- 按用户要求，本轮没有运行测试；下一步只核对三笔提交的整体差异与最终工作区状态。

## 2026-07-21 提交后续作

- 本轮完成标准：先验证上一轮三笔已提交改动；再按正式 Plan 003 和当前源码找出一个真实剩余缺口，完成测试先行的最小迁移、相关回归与记录同步。
- `planning-with-files` 说明中的恢复脚本在仓库本地和实际技能目录都不存在；已停止重复尝试，并从三份规划文件、Git 历史与干净工作区恢复现场。
- 当前最新提交为 `ed8c6b56e`，工作区干净；上一轮明确跳过测试，本轮首先补齐验证证据。
- 旧记录中的 Plan 003 文件路径不存在，下一步从 `plans/` 实际文件清单定位正式计划。
- 已定位并完整读取正式 Plan 003；状态总表仍写 `TODO`，但实际代码与工作计划已在步骤 5，后续只按真实验收证据更新。
- 代码审计确认网页入口前置已完成，但后台服务仍使用旧装配、旧应用入口和重复后台任务；现有桌面宿主门禁覆盖这些真实缺口。
- 已补跑 `uc-bootstrap` 与 `uc-webserver` 完整测试：支撑层、网页服务、配置迁移、桌面宿主、双端传输和设置集成测试全部通过；仅既有约 60 秒局域网压力测试按标记跳过。
- 桌面最终切换门禁复跑：41 项中 36 项通过，剩余 5 项均准确对应正式验收缺口。
- 深入启动接线后确认恢复和升级不能先行独立迁移，因为后台服务尚未持有共享核心；本轮范围调整为先完成唯一核心宿主接线，再迁其余启动职责。
- `cargo check -p uc-daemon --locked` 当前准确失败 4 处，均为共享核心、文件句柄和 LAN 能力尚未传入旧宿主构造链；没有出现核心或网页服务内部错误。
- 已确认唯一正确入口是 `prepare_desktop_engine_host` 后调用 `Engine::start`，旧 `wire_dependencies` 主路径必须被替换而非并行保留。
- 继续审计事件消费者后发现主切换还缺稳定事件信息：现有核心只发粗粒度刷新和简化进度，旧 worker 仍承载命令行监听与界面实时状态。为避免行为退化，先补核心事件边界，再删除旧 worker。
- 已按测试先行补齐六类核心事件：新内容、待接收、接收阶段、完整传输进度、传输终态和投递变化。首轮测试准确因缺少公开类型失败，最小实现后 6 项通过。
- 事件载荷的调试输出已脱敏，不输出预览、文件名、设备编号和失败原因；下一步补原始入站通知与 peer 变化。
- 已补原始入站通知与 peer 在线变化事件；原始入站转换测试和 presence 转换测试均先红后绿，核心库 76 项全部通过，所有核心测试目标联合编译通过。
- 已在 `uc-webserver` 建立统一事件到现有 WebSocket 协议的单一转换；命令行监听和完整传输进度的字段级测试通过。
- 下一步移除网页状态里的网卡查询与接收状态旧旁路，把 daemon 构造参数收敛为共享核心、文件句柄和进程安全状态。
- 已新增稳定网卡查询与接收健康结果，删除网页状态中的两个旧旁路；对应公开契约、边界测试和网页编译通过。
- 已将启动恢复和升级检测切到共享核心，保留原有值守解锁判断；两条启动门禁转绿。
- 已用新宿主替换旧装配：生产路径只启动和关闭一个核心，事件转为现有 WebSocket 形状，手机 SSE 由核心当前剪贴板事件驱动。
- 已删除旧应用装配、旧进程运行时、旧服务清单、旧搜索装配、旧状态和全部重复 worker 源码。
- 桌面主切换 42 项门禁全部通过，daemon 41 项库测试全部通过，锁定编译通过。
- 最终回归前继续补“修改手机监听设置后立即切换端口/状态”的事件驱动链路。
- 已让核心设置更新成功后发出监听目标，并由 daemon 同一控制器即时启停或换端口；真实核心设置流程测试通过。
- 已审阅 45 个文件的整体范围，确认主要变化是删除整套旧装配与重复 worker；只格式化本次 Rust 文件，`git diff --check` 通过。
- 开始完整核心、网页、后台服务、桌面联编、静态检查和真实进程冒烟。

## 2026-07-21 最终验收续作

- 本轮完成标准：修正两个剩余核心契约测试，跑通核心、网页、后台服务及桌面联合验证，通过定向静态检查与差异检查，完成独立配置的真实进程启动、查询和优雅退出，最后复查旧入口。
- `planning-with-files` 恢复脚本再次确认不存在；已以三份规划文件、45 个未提交文件和两个明确失败测试恢复当前状态。
- 两个失败都位于 `host_adapter_contract`：一个仍等待已被细分事件取代的粗粒度刷新事件，另一个的手机上传对端标识预期未按生产转换规则表达。
- 完整 `host_adapter_contract` 复现为 16 项通过、2 项失败；已只修正过时契约：剪贴板改为精确核对新内容事件，手机上传改为核对生产路径实际使用的隔离对端标识；未修改生产实现。
- 首次重跑后手机上传已通过，剪贴板仅剩事件提示语与真实内容的预期混淆；已按既有规则改为核对固定提示语，历史查询仍独立核对真实内容。
- `cargo test -p uc-engine --test host_adapter_contract --locked` 最终 18 项全部通过；两个剩余失败已清零，且同一目标内其他宿主、恢复、隐私和生命周期用例无回归。
- `cargo test -p uc-engine --locked` 全套通过：78 项库内测试、42 项桌面边界、18 项宿主适配、2 项宿主公开契约、3 项明文扫描脚本与 39 项公开契约全部无失败。
- `cargo test -p uc-bootstrap -p uc-webserver -p uc-daemon --locked` 联合通过：支撑层、配置迁移、桌面宿主、双端传输、daemon 41 项、webserver 151 项及其他集成用例无失败；仅既有手动局域网压力测试按标记跳过。
- `cargo check -p uc-daemon -p uc-desktop -p uc-tauri -p uc-cli --locked` 联合编译通过，四个生产入口与新核心接线一致。
- 全仓格式检查命中 3 个 `uc-infra` 和多个 `uc-webserver` 既有文件，均不在当前 45 个改动文件内；未批量改写这些无关文件。
- 首次严格静态检查在 `uc-engine` 命中既登记的参数数量、类型复杂度、路径参数和旧测试复制写法；下一步只对这些具体类别放行后重跑，不放行其他告警。
- 当前改动且仍存在的 Rust 文件单独格式检查通过；daemon 全目标严格静态检查在零放行下通过。
- `uc-engine` 收窄检查仅剩旧宿主测试的 9 处 `unwrap`；`uc-webserver` 仅命中 5 处旧 `expect` 和三类旧测试写法，均不在本次改动文件。
- `uc-engine` 全目标定向静态检查已在精确放行旧类别后通过；`uc-webserver` 第二轮只剩生成工具必需的 2 处终端输出和旧设置测试 2 处布尔写法。
- `uc-webserver` 全目标定向静态检查已在仅放行六类已确认旧告警后通过；未出现本次新增告警。
- 真实 daemon 冒烟使用独立 `plan003-smoke-20260721` 配置：构建后前台启动无界面服务，公开健康接口返回 `ok` 且报告无界面常驻模式，正式 CLI 成功读取移动同步设置和设备列表。
- 全新未置备配置下，通用 `status` 查询按契约返回会话未解锁；这不影响健康和移动设置验收。
- 发送正常中断信号后，daemon 日志确认统一核心所有任务均正常收尾，进程以 0 退出，监听端口和进程都已消失。
- daemon 生产路径验收已完成并写回正式 Plan 003；该计划仍剩 CLI `dev-tools` 对 `CliAppRuntime` / `AppFacade` 的外部使用，不标记整体完成。
- 清理测试配置时，环境拒绝带强制参数的删除命令；将改用只针对本轮已知目录的普通递归删除。
- 独立冒烟配置的数据目录、日志目录和钥匙串网络身份均已成功删除，本轮未留下测试进程或本机配置。
- 本轮 daemon 子任务已完成；下一个独立任务是将 CLI `dev-tools` 从 `CliAppRuntime` / `AppFacade` 迁移到统一公开入口，不与本次 daemon 大规模删除混为一笔改动。

## 2026-07-21 Plan 003 旧入口收尾续作

- 本轮完成标准：删除命令行开发工具对 `CliAppRuntime` / `AppFacade` 的外部使用，保持开发工具现有行为，通过相关测试、编译、旧入口复查和差异检查，并同步正式计划与三份记录。
- `planning-with-files` 恢复脚本在技能说明指定目录中仍不存在；已确认安装目录未包含脚本，直接从三份规划文件、Git 状态和最新提交恢复。
- 当前分支为 `refactor/unified-core-independent-crates`，最新提交 `a55309ca7`；工作区只有未跟踪的 `vendor/`，在确认其归属前不改动。
- 上次已完成 daemon 单核心切换、完整回归、定向静态检查和独立配置真实进程冒烟；Plan 003 唯一已知代码缺口是 CLI `dev-tools` 仍直接取得旧应用入口。
- 已完成 CLI 旧入口调用面盘点，确认不是单点替换；下一步按每个命令的真实用途建立旧调用到统一核心或 daemon 接口的映射，并先补边界门禁。
- 已将调用面分为可直接迁移和仅旧内部入口支持的两组；正在核对提交历史与正式替代路径，避免为遗留诊断命令扩大稳定核心接口。
- 已找到已锁定的架构决定：开发与 E2E 命令统一迁到调试构建专用 daemon 端点，release 不含这些路由；原始 blob 诊断继续保留。后续按该边界实现并删除 CLI 进程内核心。
- 已新增 CLI 旧入口边界测试；首次运行未进入断言，因为仓库 `target` 指向未挂载外置硬盘。已确认后续改用 `/tmp/uni-desktop-target`，不修改用户符号链接。
- 改用临时构建目录后，边界测试准确红灯：19 处匹配全部来自 CLI 开发命令和 `uc-bootstrap` 的旧运行时公开路径，没有其他失败。
- 首次整块迁移补丁因 `non_gui.rs` 实际内容未匹配而完整拒绝；已改为按核心、支撑层、命令行拆分应用，未留下半完成修改。
- 核心开发诊断接口与新的 CLI `Engine` 运行时已编译通过；历史导出、种子数据、目录捕获和 blob 诊断完成迁移，CLI 错误从 40 个降到 16 个。
- 邀请、文件发送和手机同步调试均已迁入统一核心，CLI 源码扫描不再包含旧运行时或应用层门面；调试功能锁定编译零告警通过。
- CLI 全套 94 项测试通过，3 项需要预构建后台服务的目录捕获测试成功编译并按既有标记跳过。
- `uc-engine` 与 `uc-bootstrap` 完整回归通过：核心 78 项单元、43 项桌面边界、18 项真实宿主、39 项公开契约及支撑层、双端传输、配置迁移等测试均无失败；1 项局域网组播测试按既有标记跳过。
- 真实进程使用隔离配置完成空间创建、种子写入、历史读取和两层目录捕获；正文、目录名和两个文件名在数据、缓存、日志中精确扫描零命中。
- 隔离配置、日志、缓存和临时目录均已删除，没有遗留后台进程或钥匙串项。
- `uc-application` 完整回归通过：843 项库内测试、10 项文件传输测试和文档测试全部无失败。
- 四个正式入口联合编译通过；首次使用临时构建目录时补下载了图形界面依赖，短暂网络重试后自行恢复并在 1 分 14 秒内完成。
- 最终旧入口扫描零匹配，正式版 CLI 依赖树中仍无支撑层、核心引擎、应用层、基础设施或平台实现。
- 最终人工复查发现邀请地址对象的调试输出仍显示完整地址；新增断言先准确失败，改为脱敏输出后同一检查转绿。
- 修正后 `uc-engine` 完整回归再次通过：79 项库内测试、43 项桌面边界、18 项真实宿主、2 项宿主公开约束、3 项明文扫描脚本、39 项公开契约及文档测试全部无失败。
- 修正后四个正式入口联合编译再次通过；Plan 003 完成清单与总览状态已更新为完成。
- 当前工作阶段回到 Plan 002 剩余真机验收，仍等待 Android、HarmonyOS 设备矩阵和四端明文扫描证据。
- 最终差异检查通过，旧入口与 Plan 003 未完成项扫描均为零匹配；用户原有的未跟踪 `vendor/` 保持未改动。

## 2026-07-22 Android 模拟器续作

- 本轮先按单一目的提交 Plan 003 现有改动，再使用本机 Android 模拟器推进 Plan 002；模拟器结果单独记录，不替代实体设备的换网、后台政策和硬件密钥库最终证据。
- 已完成两笔提交：`fbd27d4df` 迁移命令行开发工具，`6119b4750` 完成 Plan 003 记录；提交后仅保留用户原有的未跟踪 `vendor/`。
- 分支已有远端关联，名称保持不变；两笔提交的暂存差异和最终两提交差异检查均通过。
- 本机 Android SDK、`adb`、模拟器和 Android Rust 构建工具可用，存在 `Pixel_8` 模拟器；当前未启动、无连接设备。
- 当前仓库只有 iOS 验收应用，没有 Android 共享核心验收应用；下一步先启动模拟器并核对系统、架构、网络和密钥库能力，再建立最小 Android 验收入口。
- `Pixel_8` 已无界面启动并完成开机，`adb` 状态正常；系统为 Android 14 / API 34，架构为 `arm64-v8a`。
- 模拟器的文件级系统加密、凭据服务、Android Keystore 服务、Wi-Fi 和蜂窝模拟网络均已启动；地址为模拟器 NAT 下的 `10.0.2.15`/`10.0.2.16`。
- 本机有 Java 17、NDK 27 和 `cargo-ndk`，但没有全局 Gradle；下一步查找可复用的项目级 Gradle Wrapper，不安装全局工具。
- 已确定最小结构：将现有 iOS 验收核心改为 iOS/Android 共用核心，Android 只提供 Keystore、应用目录和命令转发；先添加共享核心、系统密钥和薄宿主三条失败门禁。
- 三条边界门禁首次运行均准确因目标文件不存在而失败；最小结构接入后首次转绿运行被包改名引起的锁文件更新阻止，下一步先离线更新锁文件再恢复锁定验证。
- 锁文件已离线更新，三条边界门禁在锁定模式下全部转绿；共用验收核心真实编译随后发现两个新增结果未覆盖，正在补网卡数量和接收状态的脱敏摘要。
- 共用验收核心 4 项原有测试已通过；Android 首次完整目标编译已越过网络、数据库和加密依赖，只剩 JNI 错误构造与字符串转换两个局部类型错误。
- Android 原生库在“警告即失败”模式下完整编译通过；首次离线生成 Gradle Wrapper 因缓存缺少插件快捷索引失败，插件本体已存在，改为直接引用缓存坐标。
- Android ARM64 发布库已在 2 分 53 秒内生成并复制到应用；首次 APK 组装发现只有 Android 36 编译平台，保持目标系统 34 不变并改用现有编译平台。
- APK 已成功组装并安装；首次真实命令触发原生中止，系统墓碑明确报告 Android Context 未初始化，已新增回归门禁并按官方嵌入方式在 JNI 安装阶段注入全局应用 Context。
- 创建空间、设备列表和暂停已在模拟器成功；恢复时系统检测到 Context 全局引用已被后续命令释放，新增进程级引用寿命门禁并将 Context 改为单一终身所有者。
- 修正后 APK 重建、覆盖安装成功；从 Android Keystore 解锁原空间成功，空间编号和设备编号均与更新前一致。
- 模拟器暂停、恢复、正常关闭、强制结束进程、再次启动和解锁全部成功；没有再出现原生崩溃。
- 下一步建立隔离桌面测试配置，与模拟器完成真实 P2P 配对和双向文本、图片、文件，再导出 Android 私有目录执行明文扫描。
- Android 已签发邀请，桌面独立配置首次真实加入返回“邀请方不可达”；Android 进程仍存活，桌面一次性后台服务在失败后退出，正在用双方日志区分 NAT、relay 和宿主网络限制。
- 桌面日志证明邀请经云端解析成功、双方选择同一 relay，但 Android 广播结束后约十秒被系统冻结，三次连接均超时；已用失败门禁固定并新增纯保活的 `dataSync` 前台服务。
- 最新 APK 首次覆盖安装后，Android 14 明确拒绝从自定义后台广播启动前台服务并终止进程；新增前台启动入口后，5 项 Android 边界检查全部通过。
- 人工差异复查发现 iOS 安全存储选择分支写反；新增检查先准确失败，修正后确认 iOS 使用 Keychain、Android 使用 Keystore、其他目标才使用不可用回退。
- 修正后的 Android 原生库与 APK 完整重建成功；覆盖安装后从 Android Keystore 解锁原空间成功，空间编号仍为 `489c35bb-b292-488a-bf76-bd40d07710ee`。
- 系统服务状态确认探针处于 `dataSync` 前台服务，通知通道存在且进程未被冻结。
- Android 新邀请 `7294-0885` 经云端发布后，桌面隔离配置 `plan002-android-emulator` 在 3 秒内成功加入；双方空间编号一致，桌面识别 Android 原设备身份。
- 配对后首次桌面发送失败；日志确认统一桌面宿主把 `UC_PROFILE` 配置名误当成本地密钥范围，而配对导入仍保存 Android 的默认范围，重启后判为密钥损坏。
- 新增桌面密钥范围回归检查并恢复历史行为：配置名继续隔离目录、端口和安全存储，但不改变 Space 密钥范围；检查转绿且 CLI/daemon 重建成功。
- 双向文本通过：桌面发送 1 个目标接受且 Android 唯一查询命中；Android 发送 1 个目标接受且桌面实时监听取得完整唯一正文。
- 双向图片通过：Android 的 68 字节 PNG 被桌面识别为 `image/png`；桌面系统剪贴板放入 PNG 后 Android 历史新增图片记录。
- 双向文件通过：Android 到桌面 238951 字节、桌面到 Android 96721 字节，导出后两边 SHA-256 均与源文件一致。
- Android 事件摘要当前为 13 个操作完成、7 个入站事件、6 次传输更新、0 个致命错误；桌面主动探测 Android 在线。
- 模拟器 UDP 临时阻断期间 36 个数据包被丢弃，桌面日志明确记录剪贴板连接选择 relay，Android 仍接收唯一文本；退出时规则自动删除并确认网络恢复。
- Android 连续 5 轮暂停/恢复全部成功，事件摘要保持运行且 0 个致命错误。
- 强制结束 Android 进程后，进程编号从 5997 变为 6553；重新解锁仍得到原空间、原设备编号、两名成员和重启前的中继测试历史。
- 删除主动导出的明文测试副本后，完整导出 Android 私有数据、缓存和临时目录，共 15 个文件、约 1.3 MB。
- 口令、三段唯一正文、源文件名、外部源路径和导出文件名在全部文件内容及目录名中均为零命中；原始文件字节只存在于允许的文件内容缓存，缓存名称为不透明编号。
- 最终回归通过：共享移动核心 4 项、Android 边界 5 项、桌面边界 44 项全部通过；相关 Rust 文件格式和差异检查通过。
- Android ARM64 与 iOS ARM64 均在“警告即失败”模式下完成发布目标编译；Android APK 由有效的调试证书签名并通过系统签名验证。
- Android 模拟器可覆盖验收矩阵已完成并写回正式 Plan 002；唯一标签、蜂窝换网、硬件密钥模块和厂商后台策略明确保留给实体设备最终验收。
- 首次清理脚本因 zsh 保留变量覆盖命令搜索路径而未执行任何删除；改用普通变量和绝对命令路径后成功完成。
- Android 验收应用已卸载，应用数据与 Keystore 项随包清除；外部测试文件、桌面隔离配置、缓存、日志、本轮导出目录和模拟器进程均已删除。
- 第一笔提交检查发现索引中残留两个移动验收目录改名，导致范围混入；未推送前撤销该提交记录并保留全部文件内容，随后清空索引重新拆分。
- 已完成两笔独立提交：`82240459f` 修复桌面 Space 密钥范围，`6ceb8f757` 增加 Android 统一核心验收宿主；最后只提交计划与验收记录。

## 2026-07-22 跳过实体设备验收并进入 Plan 004

- 用户明确决定跳过剩余实体设备验证；Android/HarmonyOS 真机矩阵和对应明文扫描保留为未通过，不用模拟器结果冒充完成。
- 当前阶段切换到 Plan 004；六组真机互通同样按用户决定跳过，继续推进可由自动构建和模拟环境证明的绑定、系统接入与发布产物。
- Plan 004 第一个切片是新建 iOS/Android 共用正式绑定；现有 `uc-mobile` 是旧 LAN HTTP 客户端，必须保持独立，不能改名冒充完整 P2P 核心。
- 已同步更新路线图、Plan 002 与 Plan 004：Plan 002 保持未完成，Plan 004 进入进行中，所有跳过的实体设备项目继续保持未勾选。
- 文档差异检查通过；一次附加扫描因命令引号写法失败，未修改文件，已改用固定文本模式重新检查。
- 已提交跳过实体设备验收与 Plan 004 启动记录：`14c8bace6 docs(plan): record skipped device acceptance`。
- 已开始核对根工作区、旧移动绑定和 `uc-engine` 公开接口，准备先加入必然失败的边界检查。
- 已加入首个工作区边界测试；第一次运行被断开的 `target` 链接挡住，尚未到达预期断言，后续改用独立临时编译目录。
- 独立编译目录中的首轮编译发现测试类型比较错误；已改用元数据接口的正式产物类型，继续验证预期失败。
- 工作区边界测试已先按预期失败于缺少 `uc-engine-uniffi`，随后加入最小 crate、三种产物声明及 `uc-engine`/UniFFI 依赖，测试转为通过。
- 核心版本测试先失败于空结果，随后固定为 crate 版本派生的 `core-vX.Y.Z`，测试转为通过。
- 稳定错误映射测试先失败于错误类别被统一映射为内部错误，随后补齐八种逐一映射并转为通过。
- 绑定完整流程测试先失败于运行环境占位，随后接入真实 `uc-engine`：临时目录、宿主安全存储、空间创建和限时关闭均已通过。
- 将后台命令接收改为持续驱动的同一执行循环，避免节点在两次宿主调用之间停止推进；三项绑定测试复跑通过。
- 新绑定全部测试和工作区边界测试通过；全依赖严格静态检查被 33 个既有告警阻断，改为只严格检查新 crate 本身。
- 新 crate 自身全目标严格静态检查通过；开始用失败测试补绑定生成入口。
- 生成入口测试先失败于目标不存在，加入独立且可选的 UniFFI 生成命令后转为通过；同一份本机动态库成功生成 Swift 和 Kotlin 文件，并核对关键接口全部存在。
- iOS ARM64、Android ARM64 和 Android x86_64 发布构建均通过，产物架构正确且均包含 `core-v0.19.1`。
- 依赖白名单测试先失败于缺少内存清零支持；口令进入绑定队列时已立即转为自动清零内存，全部测试、严格检查和三目标发布构建再次通过。
- 提交前审阅撤销了 `cargo fmt --all` 产生的全部无关格式差异，并补齐关闭失败时的线程回收；最终测试、严格检查、三目标发布构建及当前代码的 Swift/Kotlin 重新生成全部通过。

## 2026-07-22 Plan 004 第二个绑定切片

- 已从提交 `db15c78db` 的干净状态继续，原有 `vendor/` 保持未跟踪且不修改。
- 当前切片目标是暂停/恢复、可轮询事件、邀请/加入和文本发送；继续只依赖公开 `uc-engine`，不进入 HarmonyOS。
- 已确定事件边界：生命周期与操作终态结构化传递，其他业务变化只传核心稳定事件种类并由宿主按需查询；文件和图片留给需要宿主句柄的下一独立切片。
- 暂停/恢复和状态事件已按测试驱动转绿；操作成功事件、其他变化种类、邀请签发、加入错误映射和本地文本发送摘要也已分别从预期失败转绿。
- 提交前复查发现事件使用无限队列会在宿主暂停读取时持续增长；失败测试固定该问题后，已改为容量 256 的队列并在溢出时返回 `ConsumerLagged`。
- 事件队列首次精确筛选使用了短名称，实际运行 0 项；已记录并改用完整模块名称重跑。
- 单项绑定测试均通过，但完整套件并行启动多个生产核心导致 4 项资源争用失败；仅涉及真实核心的集成测试已串行化，完整套件稳定通过。
- 关闭路径复查一度怀疑直接释放绑定会等待事件流；核对核心保证后新增真实回归测试，证明运行中的绑定可在限时内结束工作线程，无需修改生产关闭逻辑。
- 最终验证通过：2 项事件队列测试、8 项公开接口测试、1 项工作区边界测试、范围格式检查和严格静态检查全部成功。
- Swift 与 Kotlin 已从当前同一份本机动态库重新生成，暂停、恢复、事件读取、邀请、加入、文本发送和新增事件类型均存在。
- iOS ARM64、Android ARM64 和 Android x86_64 发布构建再次通过；产物架构正确，三份均包含 `core-v0.19.1`。
- 本切片完成；图片、文件、宿主剪贴板与文件句柄及最终 XCFramework/AAR 留给下一独立切片，实体设备验收继续保持跳过且未通过。
- 已提交第二个绑定切片：`0356af010 feat(mobile): add engine lifecycle and messaging bindings`。

## 2026-07-22 Plan 004 第三个绑定切片

- 当前先完成图片、文件、宿主剪贴板与文件句柄，再单独完成 XCFramework/AAR 打包；不进入 HarmonyOS，不改实体设备验收状态。
- 新行为继续采用测试驱动开发，先以公开 `uc-engine` 操作和宿主能力定义失败测试。
- 第一轮先固定共用发送摘要和图片发送入口；测试应因新接口尚不存在而失败。
- 图片发送红灯准确命中缺少共用摘要和入口；复用核心图片操作并让队列中的图片字节自动清零后，单项测试转绿。
- 第二轮开始固定不透明宿主文件句柄、元数据、分块读取和文件发送摘要；当前应因文件宿主接口尚不存在而失败。
- 文件发送红灯准确命中元数据类型、宿主读取回调和入口缺失；接入后核心通过不透明句柄分块读取，单项测试转绿。
- 第三轮开始固定结构化宿主剪贴板读取和主动捕获；当前应因快照类型、回调和捕获入口尚不存在而失败。
- 剪贴板捕获红灯准确命中快照类型、宿主读取回调和入口缺失；接入后真实捕获产生记录编号，单项测试转绿。
- 第四轮开始固定结构化宿主剪贴板写回和恢复结果；当前应因写回回调、恢复方式与入口尚不存在而失败。
- 剪贴板写回红灯准确命中回调、恢复方式和入口缺失；首次实现漏导入快照类型，补齐后真实写回测试转绿。
- 第五轮开始固定宿主文件分块写入、完成通知和记录导出；当前应因写回回调与导出入口尚不存在而失败。
- 文件导出红灯准确命中写回回调和入口缺失；接入后核心按不透明句柄写入完整内容并通知宿主完成，单项测试转绿。
- 完整回归通过：2 项事件队列测试、13 项公开行为测试、1 项工作区边界测试及严格静态检查全部成功。
- iOS ARM64、Android ARM64 和 Android x86_64 发布构建再次通过，三份产物架构正确且均包含 `core-v0.19.1`。
- Swift/Kotlin 已从当前同一份本机动态库重新生成，图片、文件、剪贴板捕获/恢复、记录导出和全部宿主文件回调均存在。
- 图片、文件、宿主剪贴板与文件句柄绑定切片完成；下一步单独实现并验证 XCFramework/AAR 打包。
- 已提交媒体与宿主能力切片：`49116117d feat(mobile): add media and host file bindings`。
- 打包切片先新增工作区契约，要求新绑定自己拥有可运行的 iOS/Android 脚本、Android 库项目、双架构产物、生成接口和校验值；当前应因文件不存在而失败。
- 打包契约按预期失败于 iOS 脚本不存在；补齐两端脚本与 Android 库项目后转绿。
- iOS 脚本真实运行成功，设备 ARM64、模拟器 ARM64+x86_64、Swift 接口、压缩包和校验值均已核对。
- Android 首次运行完成 Kotlin 编译和 AAR 组装，但最后复制使用了错误产物名，且 `cargo-ndk` 自动复制带入中间动态库；正在收窄为两种架构各一份正式绑定库后重跑。
- Android 修正后真实打包成功；AAR 只含 ARM64/x86_64 两份正式库，Kotlin 绑定已进入 `classes.jar`，校验值与文件一致。
- Swift 接口已在 iOS 17 模拟器目标下与 XCFramework 头文件一起通过类型检查；两端均报告 `core-v0.19.1` 和同一来源提交。
- 已补正式使用说明，明确 XCFramework/AAR 的接入方式、Android 运行依赖，以及它们不是可单独安装的 IPA/APK。
- iOS/Android 共用正式绑定及本地发布干跑完成；HarmonyOS、三平台正式系统接入和公开发布仍保持未完成。
- 最终回归通过：绑定 2 项队列测试与 13 项公开接口测试、打包边界 2 项、脚本语法、格式和差异检查全部成功；新增打包测试在仅放行三类已登记旧告警后严格检查通过。
- 提交前体积复查发现 iOS 压缩包约 449 MB；新增失败契约要求只剥离分发副本的调试信息，原始构建产物继续保留。
- 体积契约先按预期失败，分发副本剥离调试信息后转绿；XCFramework 压缩包降至约 63 MB，设备/模拟器架构、校验值和 Swift 类型检查再次通过。
