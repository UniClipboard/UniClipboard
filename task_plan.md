# 四平台 P2P 核心执行计划

## Goal

完成 `plans/README.md` 中 Plan 001 到 Plan 005 的全部阶段，并只在每项真实验收证据齐全后更新状态。

## Current Phase

Plan 004：HarmonyOS 薄绑定已通过模拟器验收；下一步接入三种移动系统安全存储与文件出口，实体设备验收继续按用户决定跳过。

## Phases

### Phase 1: 核对基线与约束
- [x] 核对分支、提交和工作区状态
- [x] 阅读路线图、Plan 003、架构与 Rust 规则
- [x] 记录设备验收阻碍
- **Status:** complete

### Phase 2: 完成 Plan 003
- [x] 固化公开契约与生命周期协调器
- [x] 定义宿主能力接口
- [x] 清除应用层对基础设施和产品分析实现的正式依赖
- [x] 将纯核心装配从桌面启动准备中分离
- [x] 实现可供生产使用的 `Engine::start`
- [x] 将公开操作映射到现有业务能力
- [x] 让 daemon 生产路径只使用 `uc-engine`
- [x] 删除外部可见的旧入口
- [x] 补齐中文接口文档
- [x] 完成 Plan 003 全部验收
- **Status:** complete

### Phase 2a: iOS 优先验证准备
- [x] 统一“文件落盘不加密”的仓库规则
- [x] 完成 `SendFiles` 的统一核心路径
- [x] 建立真正嵌入 `uc-engine` 的 iOS 最小宿主
- [x] 构建、签名、安装并启动真机应用
- [x] 跑完 iOS 与桌面的完整真机矩阵和落盘扫描
- **Status:** complete

### Phase 2b: 提交 iOS 验收与修复
- [x] 审阅所有已跟踪和未跟踪变更并划分单一意图
- [x] 按独立职责逐笔提交全部变更
- [x] 核对最终提交历史与干净工作区
- **Status:** complete

### Phase 3: 完成 Plan 002 的剩余真机验收
- [x] 同一进程生产核心连续启停十次且身份不变
- [x] Android 模拟器运行共享核心并完成可覆盖的验收矩阵
- [ ] Android 真机反复启停与内容矩阵（按用户决定跳过，不计为通过）
- [ ] HarmonyOS 真机反复启停与内容矩阵（按用户决定跳过，不计为通过）
- [ ] 四端持久化明文扫描（未覆盖实体 Android/HarmonyOS，按用户决定跳过）
- **Status:** skipped_by_user

### Phase 4: 完成 Plan 004
- [x] 完成 iOS/Android 共用绑定的首个可独立构建切片
- [x] 完成共用绑定的生命周期、事件、配对与文本操作切片
- [x] 完成图片、文件、宿主剪贴板与文件句柄绑定切片
- [x] 完成 XCFramework 与多架构 AAR 打包切片
- [x] 建立 iOS/Android 共用正式绑定
- [x] 将移动应用 P2P 生命周期接入统一状态与事件桥
- [x] 为 iOS/Android 移动应用接入空间创建、加入与邀请码范围提示
- [x] 建立 HarmonyOS 薄绑定
- [ ] 接入三种系统安全存储与文件出口
- [ ] 固化三种移动生命周期
- [ ] 六组真机配对与内容矩阵（按用户决定跳过，不计为通过）
- [ ] 统一发布产物验收
- **Status:** in_progress

### Phase 5: 完成 Plan 005
- [ ] 迁入独立核心仓库
- [ ] 发布候选版本并切换全部消费者
- [ ] 删除重复源码并完成最终审计
- **Status:** pending

## Decisions Made

| Decision | Rationale |
|----------|-----------|
| 当前先推进 Plan 003 | 其大部分工作可在本地完成，并且是后续移动绑定的前置条件 |
| 不把本地构建当作真机验收 | 路线图明确要求 Android 和 HarmonyOS 真实设备证据 |
| 每个行为变化先写失败测试 | 仓库要求使用测试驱动开发 |
| 每个独立结构变化单独提交 | 路线图和仓库要求保持提交意图单一 |
| 文件内容允许按现有格式落盘 | 用户已明确要求“文件落盘不加密”，此前阻断 `SendFiles` 的决定失效；实现前先统一仓库文档，避免规则互相矛盾 |
| 跳过剩余实体设备验收并进入 Plan 004 | 用户明确决定跳过；保留未通过状态和风险，不把跳过写成验收成功 |
| 用 HarmonyOS 模拟器补薄绑定运行证据 | 用户已安装 DevEco Studio，模拟器可以验证真实 HAP 构建、安装、启动和原生模块加载，但不替代真机能力结论 |

## Errors Encountered

| Error | Attempt | Resolution |
|-------|---------|------------|
| Android 模拟器阶段状态更新保留了新旧两行 | 1 | 立即删除旧状态行并复查当前阶段只保留 `in_progress` |
| 本机没有全局 `gradle` 命令 | 1 | 不安装全局工具，优先复用项目级 Gradle Wrapper 和现有 Android SDK/NDK |
| 移动验收包改名后锁定测试拒绝更新锁文件 | 1 | 先用离线模式更新工作区锁文件，再恢复 `--locked` 验证 |
| 共用验收核心缺少两个新增结果摘要 | 1 | 补只含网卡数量和接收状态的脱敏摘要，不输出网卡名称或地址 |
| Android JNI 首次编译有两个接口类型不匹配 | 1 | 补稳定错误说明并显式转换 Java 字符串，不改变桥接边界 |
| 离线 Gradle 缺少 Android 插件快捷索引 | 1 | 改为直接使用本机已缓存的正式插件坐标，再生成项目级 Wrapper |
| APK 首次组装找不到 Android 34 编译平台 | 1 | 保持目标系统为 Android 14，编译平台改用本机已安装的 Android 36 |
| Android 首次命令因原生上下文未初始化而中止 | 1 | Java 安装桥时传入全局应用 Context，原生层在启动核心前初始化 Android 上下文 |
| Android 上下文整块补丁未匹配格式化后的 JNI 排版 | 1 | 补丁完整拒绝；按依赖、Java 参数、Rust 初始化和记录四块精确应用 |
| Android 恢复时系统报告 Context 全局引用失效 | 1 | 将应用 Context 的全局引用提升为进程级单一所有者，后续命令只替换密钥桥引用 |
| 首次桌面加入的设备名未加引号 | 1 | 命令未发出网络请求；给完整设备名加引号并重试 |
| 首次读取桌面测试日志使用了不存在的日期文件 | 1 | 按实际目录清单读取现有日志文件，不重复假设文件日期 |
| 模拟器广播结束后应用被冻结导致配对超时 | 1 | 增加只负责保活的 `dataSync` 前台服务，核心与业务仍由共用验收库唯一拥有 |
| Android 14 拒绝后台广播启动前台服务 | 1 | 增加短暂前台启动入口，命令脚本先启动 `dataSync` 服务，广播只转发命令 |
| 共用验收核心的 iOS 安全存储分支写反 | 1 | 新增平台选择检查，恢复 iOS Keychain、Android Keystore 和非移动回退的正确映射 |
| 自定义桌面配置重启后把配对密钥判为损坏 | 1 | 保持 `UC_PROFILE` 的目录/端口隔离，但恢复桌面核心的默认 Space 密钥范围，并新增回归检查 |
| 桌面文件发送首次同时传入不支持的 `--peer` | 1 | 命令未发送内容；当前空间只有一个远端成员，移除该参数后重试并成功 |
| Android 文件大小检查的格式参数被远端 shell 拆分 | 1 | SHA-256 已一致，改用无空格格式取得 96721 字节，不重复错误参数 |
| 模拟器非特权 shell 无法读取防火墙规则 | 1 | 仅为 relay 验收临时重启调试桥为 root，测试后删除精确规则并确认应用网络恢复 |
| 清理脚本用 `path` 作为 zsh 循环变量导致命令搜索路径失效 | 1 | 首次未删除任何资源；改用非保留变量并以绝对命令路径重跑 |
| 第一笔提交意外带入之前已暂存的移动验收目录改名 | 1 | 提交未推送；只撤销该提交记录、保留全部文件内容，清空索引后按明确文件清单重新拆成三笔提交 |
| `planning-with-files` 恢复脚本在进入 Plan 004 时仍不存在 | 15 | 已核对三份规划文件、Plan 004、路线图和当前提交，直接从磁盘真实状态继续 |
| 文档卫生扫描的双引号包含代码围栏，导致终端解析失败 | 1 | 命令未修改文件；改用分别传入的单引号固定文本模式检查 |
| 根 `target` 是指向已不存在目录的旧符号链接，首次测试无法创建输出目录 | 1 | 保留用户现有链接不动，本轮统一设置独立的系统临时编译目录 |
| 首个边界测试把元数据产物枚举误当成字符串集合 | 1 | 测试未到达目标断言；改用 `cargo_metadata` 的正式枚举值后重新运行 |
| 新绑定严格静态检查被依赖模块的 33 个既有告警阻断 | 1 | 不修改无关旧代码；改用 `--no-deps` 继续对新 crate 全目标保持零告警 |
| 生成入口检查再次把元数据目标类型误当成字符串 | 1 | 测试未到达目标断言；改用 `TargetKind::Bin` 后重新运行 |
| 事件队列单元测试使用短名称配合 `--exact`，实际筛选到 0 项 | 1 | 改用完整模块测试名称运行，不把仅编译成功当作测试通过 |
| 绑定完整测试并行启动多个生产核心，4 项争用进程级网络资源返回暂不可用 | 1 | 仅给真实核心集成测试加进程内串行保护，纯映射测试继续并行 |
| 剪贴板写回首次编译缺少绑定快照导入 | 1 | 测试未运行到断言；补回写回映射实际使用的类型后重跑 |
| Kotlin 生成接口扫描在双引号中使用反引号 | 1 | 生成未受影响；改用不含反引号的固定方法名模式重新扫描并通过 |
| Android 首次 AAR 组装后复制了错误文件名且包含中间动态库 | 1 | Gradle 组装已成功；改用实际产物名，并只复制两种架构的正式绑定库后重跑 |
| 直接删除 Android 本地 Gradle 缓存被安全规则拒绝 | 1 | 未删除任何文件；增加库目录 `.gitignore`，确认缓存只显示为已忽略且不会提交 |
| 打包边界测试严格检查被 `uc-engine` 7 个既有告警阻断 | 1 | 不修改无关模块；仅在命令中放行已登记的复杂类型、参数数量和路径参数三类旧告警后复查新增测试 |
| `planning-with-files` 恢复脚本不存在 | 1-2 | 直接核对已有规划文件、Git 状态与五份正式计划恢复进度 |
| iPhone 由可用变为离线 | 1 | 已确认仍配对且开发者模式开启，但当前无物理/无线连接；继续准备宿主并定期复查 |
| 移动提交钩子拒绝全局 Node 实验参数 | 1 | 提交未创建且暂存内容不变；清除 `NODE_OPTIONS` 后使用项目要求的 Node 22 重试 |
| `bb-browser` 技能没有本机命令 | 1 | 未打开浏览器标签；改用只读网络请求读取 Expo 56 官方总览与 Expo UI 版本页 |
| 现有 iOS 应用仍使用旧 `uc-mobile` | 1 | 不把它当作新核心证据，改为建立嵌入 `uc-engine` 的最小宿主 |
| 首次统一文件规则补丁未匹配接口文档原文 | 1 | 拆分补丁并按实际段落更新，其他文件未发生半完成修改 |
| 文件发送测试在无可信对端时要求重发成功 | 1 | 现有业务规则会拒绝无目标重发；改为暂停恢复后导出原文件，真机重发留给双端矩阵 |
| 文件名探针扫描因宿主临时目录不存在而返回无效 | 1 | 测试按真实宿主契约预先创建私有、缓存和临时目录后重跑 |
| 文件名覆盖测试使用 `--exact` 筛选到 0 项 | 1 | 去掉 `--exact` 后真实运行 1 项并通过 |
| 严格静态检查被 8 处既有告警阻断 | 1 | 仅在命令中忽略已登记的四类旧告警，其余告警保持失败并通过 |
| iOS 探针把生命周期枚举当作可显示字符串 | 1 | 改用其稳定调试名称，避免为诊断宿主扩大公共接口 |
| iOS 应用首次链接缺少网络系统库且静态库最低版本漂到当前 SDK | 1 | 补链接 Network.framework，并用独立输出目录把 Rust 最低版本固定为 iOS 17 |
| 真机应用首次启动被锁屏拒绝 | 1 | 应用已安装，等待设备解锁后直接重试，不重复构建或安装 |
| 首次拆分后仍有桌面路径决定留在纯装配 | 1 | 将网络数据目录也改为桌面模块准备的输入，并保留旧身份目录供迁移兼容 |
| 身份迁移测试移动补丁未匹配格式化后的代码 | 1 | 重新读取实际行后，按当前内容移动完整测试模块 |
| 反向依赖查询无法表达“不存在的平台依赖” | 1 | 改为输出 `uc-engine` 依赖树并搜索 `uc-platform` |
| 迁移结果类型后缺少通用分析契约清单声明 | 1 | 给 `uc-engine` 增加 `uc-observability-contract` 直接依赖 |
| 单独测试 `uc-engine` 时后台任务功能未开启 | 1 | 由 `uc-engine` 自己启用 `uc-core/task-registry`，不依赖功能合并 |
| 拆分平台组装后主组装仍引用旧位置 | 1 | 改为调用 `uc-engine` 中的新位置 |
| 提交收尾首次补记录未匹配错误表原文 | 1 | 按当前文件实际段落拆分补丁，未产生半完成修改 |
| `planning-with-files` 恢复脚本在当前安装中仍不存在 | 4 | 直接读取并沿用仓库根目录已有的三份记录 |
| `planning-with-files` 恢复脚本在本次空间创建续作中仍不存在 | 1 | 已核对三份规划文件、两个仓库状态、正式 Plan 004 与当前差异，从邀请码范围缺口继续 |
| 移动定向测试被旧 Node 与项目参数组合阻断 | 1 | 使用项目已验证的 Node 22.22.1 并清除外部 `NODE_OPTIONS` 后重跑，真实红灯与转绿均已取得 |
| 移动完整测试仍锁定旧统一核心提交 | 1 | 新组件来源已经过校验，更新锁定检查到 `f204a4d4b` 后单项与完整重跑 |
| `planning-with-files` 恢复脚本在本次 Android 模拟器续作中仍不存在 | 13 | 已核对三份规划文件、最新两笔提交、Android 未提交差异与模拟器状态，继续从最新 APK 安装开始 |
| `planning-with-files` 恢复脚本在本轮指定安装路径中仍不存在 | 5 | 已核对三份规划文件和干净工作区，继续用正式计划与提交历史恢复准确进度 |
| `planning-with-files` 恢复脚本在接续会话中仍不存在 | 6 | 已核对交接摘要、三份规划文件、Git 状态与未提交差异，按磁盘真实状态继续 |
| `planning-with-files` 恢复脚本在下一接续会话中仍不存在 | 7 | 已核对三份规划文件、干净工作区和最新提交，继续按磁盘真实状态推进 |
| `planning-with-files` 恢复脚本在本次目标续作中仍不存在 | 8 | 已核对三份规划文件、最新提交 `771afed25` 和干净工作区，以当前源码重新审计 daemon 所有权链 |
| 开发诊断邀请地址仍使用自动调试输出 | 1 | 新增地址脱敏断言取得准确失败，改为自定义脱敏输出后转绿 |
| `planning-with-files` 恢复脚本在重发续作中仍不存在 | 9 | 已核对三份规划文件、未提交重发差异与最新提交，按磁盘真实状态继续 |
| `planning-with-files` 恢复脚本在移动设备管理续作中仍不存在 | 10 | 已核对三份规划文件、7 个未提交文件、最新提交 `f29d9c155` 和当前失败流程，继续按磁盘真实状态推进 |
| `planning-with-files` 恢复脚本在 daemon 切换说明前仍不存在 | 11 | 已直接核对三份规划文件、未提交门禁、最新提交和当前生产引用，确认 Phase 2 真实状态 |
| `planning-with-files` 恢复脚本在本次续作路径中仍不存在 | 12 | 已直接核对三份规划文件、42 个未提交文件和最新验证记录，从移动 LAN 上传生命周期缺口继续 |
| 本轮规划记录补丁连续未匹配 `findings.md` 结尾 | 1-2 | 两次整份补丁均被拒绝且未留下修改；分别读取真实文件结尾后按各自稳定末行追加 |
| daemon 切换发现记录补丁未匹配文件末尾 | 1 | 补丁完整拒绝；读取三份文件实际结尾后按稳定末行重新应用，未留下半完成修改 |
| 网页 API 收尾补丁未匹配移动同步测试实际排版 | 1 | 整份补丁未应用；按文件拆分后用准确测试内容重新应用 |
| 同一条 `cargo test` 命令传入两个测试筛选参数 | 1 | Cargo 只接受一个筛选串；改用共同前缀 `api::` 后 86 项测试通过 |
| 核心接口文档围栏搜索命令的双引号触发 shell 解析错误 | 1 | 改用不执行内容的单引号模式重跑，文件未受影响 |
| LAN 内容核心模块补丁未匹配内部模块清单顺序 | 1 | 整份补丁未应用；读取真实模块清单后拆分新增文件与接入步骤 |
| 首次查找核心接口文档时 shell 展开空通配符失败 | 1 | 改用 `rg --files` 先定位真实文件，再读取目标文档 |
| 升级能力边界门禁在实现后仍失败 | 1 | 链式调用被格式化为多行，门禁未识别直接字段；处理函数改为先明确取得升级能力，使依赖和测试都稳定可读 |
| 设置能力首个补丁未匹配格式化后的导入行 | 1 | 整份补丁未应用；重新读取当前排版后拆成精确补丁，未留下半完成修改 |
| 移动同步 HTTP 门禁在实现后仍失败 | 1 | 多行链式调用未保留连续字段文本；辅助函数改为先明确取得兼容能力，使依赖和门禁稳定可读 |
| 存储公开契约补齐后运行时匹配不完整 | 1 | 这是接口测试命中的预期红灯；新增唯一存储执行模块并让生产运行时覆盖两个操作后转绿 |
| 存储内部测试使用 `--exact` 筛选到 0 项 | 1 | 去掉精确筛选后两个测试各真实运行 1 项并通过 |
| iOS 验收宿主未覆盖存储结果 | 1 | 联合编译命中穷尽匹配缺口；补只含分类字节数的稳定输出和测试后转绿 |
| 创建空间真实核心测试在全新环境传空设备名返回无效输入 | 1 | 确认这是正确规则：仅在已有保存名称时允许省略；恢复新安装测试的显式名称，并沿用应用层已有回退覆盖 |
| 创建空间切片严格检查被既有告警阻断 | 1-2 | 首次命中 `uc-engine` 已登记告警，第二次命中测试辅助代码和移动局域网旧告警；收窄到两个正式库并仅忽略核心已登记类别后通过 |
| 取消邀请首次实现把设置能力当作 `AppFacade` 直接方法 | 1 | 按真实装配改为从运行期安装的设置服务读取，并为尚未安装保留可重试的暂不可用结果 |
| 邀请结果补丁未匹配 iOS 探针的实际字段命名 | 1 | 确认整份补丁未应用，读取真实代码后按现有下划线字段重新应用 |
| 邀请实现整理补丁未匹配核心模块的实际可见性 | 1 | 确认新文件和移动均未应用，读取模块声明后按真实内容重新应用 |
| iOS 验收宿主测试使用了目录名而非包名 | 1 | 按 Cargo 提示改用 `uc-ios-probe-core` 重跑 |
| Android 模拟器阻断全部出站 TCP 后邀请整体失败 | 1 | 已删除精确防火墙规则并确认网络恢复；改查正式局域网邀请配置入口，不重复硬断网络 |
| iOS 模拟器通过无效代理阻断云端后邀请整体失败 | 1 | 已恢复无代理正常启动；本地邀请码成功界面由自动测试覆盖，不把本次模拟器尝试记为通过 |
| 本机 Android SDK 缺少 `avdmanager` | 1 | 临时 AVD 进程已停止；将其精确配置项移入系统废纸篓，并确认原 Pixel_8 完整保留 |
| N-API crate 的普通 Rust 测试缺少宿主 N-API 符号 | 1 | 只在开发依赖启用空注册测试模式，正式动态库仍使用常规 N-API 链接且测试输出不刷宿主符号提醒 |
| HarmonyOS 绑定首次编译命中口令确认和宿主对象方向 | 1 | 创建空间同时传入确认值；含回调的宿主对象只允许从 ArkTS 传入，不生成回写转换 |
| N-API 宿主冒烟完成后 Node 进程不退出 | 1-2 | 关闭时强制中止会让宿主被信号结束；改为启动前取消三个回调的事件循环保活引用，回调仍可正常使用 |
| macOS 27 拒绝加载改名后的临时 `.node` 副本 | 1 | 崩溃报告确认是无效代码签名页；复制后对 `/tmp` 测试副本重新本地签名，正式 HarmonyOS 产物不受影响 |
| N-API v2 异步函数不能直接接收环境句柄 | 1 | 拆成同步 `prepareHost` 和异步 `startEngine`；前者取消回调保活，后者一次性消费宿主句柄 |
| N-API 生成器未识别完整路径形式的环境类型 | 1 | 同步入口改用直接导入的 `Env` 类型，外部接口不变且编译通过 |
| HarmonyOS 剪贴板映射首次编译缺少核心表示类型导入 | 1 | 补唯一缺失导入后继续同一捕获行为验证，不改变接口设计 |
| HarmonyOS 宿主返回对象在 N-API 空注册测试模式下缺少转换实现 | 1 | 保留正式动态库真实转换，调整测试构建边界后重跑，不关闭空注册模式 |
| 新增工作区依赖后直接使用 `--locked` 检查 | 1 | 先用离线模式更新 `Cargo.lock`，再恢复 `--locked` 验证 |
| webserver/daemon 严格静态检查被 5 处既有 `expect` 阻断 | 1 | 不改无关文件，只在验证命令中忽略该已知类别，其余告警仍视为失败 |
| webserver 严格静态检查继续命中生成工具的 2 处终端输出 | 2 | 生成工具本就需要报告输出路径；验证命令再忽略该既有类别，其余告警保持失败 |
| 首次解锁错误测试运行未返回最终结果 | 1 | 去掉过严的精确筛选后重跑，取得明确的失败断言 |
| 解锁实现整理后生产构建出现测试专用常量导入告警 | 1 | 将该常量导入限制在测试构建，重新执行无告警检查 |
| 接收恢复失败测试预期了错误的通用编码 | 1 | 按 `ApiError::service_unavailable` 的既有全局约定改为 `runtime_unavailable` |
| 解锁边界测试仍使用已修改的注释作为函数边界 | 1 | 改用下一个函数声明作为稳定边界，避免注释变化导致误判 |
| 全仓格式检查仍命中 3 处既有无关差异 | 2 | 只格式化本次新增的边界测试，并用差异检查保证不触碰无关文件 |
| Android 目标编译找不到 `aarch64-linux-android-clang` | 1 | 找到本机 NDK 27，显式指定 API 24 编译器、链接器和归档工具后通过 |
| `uc-engine` 严格静态检查被依赖包既有告警阻断 | 3 | 改用 `--no-deps` 仅检查本包，依赖包告警不纳入本次范围 |
| 重发切片严格检查命中核心 8 处既有告警 | 1 | 只放行已登记的参数数量、类型复杂度、路径参数和 `expect` 四类告警，其余告警仍保持失败 |
| 完整发送结果扩展后移动兼容适配器匹配不完整 | 1 | 该适配器只消费既有总数和 blob 数量，显式忽略新增快照与逐目标字段后重跑 |
| 发送结果切片应用层严格检查命中 81 处既有告警 | 1 | 只放行已登记的文档排版、锁 unwrap/expect、参数数量等旧类别，其他告警仍保持失败 |
| 完整联合编译发现 daemon 监听器未忽略新增发送字段 | 1 | 监听器只消费既有统计，显式忽略新增字段，并在收尾时归入发送结果提交以保持每笔提交可编译 |
| 资源切片严格检查发现一次多余引用 | 1 | 直接删除多余引用，不放行本次新增告警后重跑 |
| 桌面剪贴板适配首次编译发生部分移动 | 1 | 先取得负载来源，再移动格式与媒体类型字段后重跑 |
| 工作区全套测试缺少 Tauri daemon sidecar | 1 | 正在用仓库现有流程生成本机 sidecar 后重跑；产物不提交 |
| iOS 探针未覆盖新增恢复结果 | 1 | 工作区编译真实发现穷尽匹配缺口，正在补稳定输出并重跑 |
| 全仓测试中平台媒体类型用例触发输入保护检查 | 1 | 确认测试把系统格式名误放进标准媒体类型字段；已移到系统格式回退用例，待重跑全仓 |
| 平台安全存储私有函数示例无法编译 | 1 | 删除外部无法访问且会修改进程环境的伪示例，保留函数说明并重跑文档测试 |
| daemon 进程地址模块 6 个示例无法编译 | 1 | 公开函数示例补导入，私有函数示例删除，不扩大公开接口 |
| 历史真实流程中统计操作返回内部错误 | 1 | 发现旧统计一次请求 10000 条而列表上限为 1000，改为按 1000 条分页累计后真实流程通过 |
| iOS 恢复结果摘要补丁未匹配测试文件结尾 | 1 | 整份补丁未应用；按真实匹配位置拆分补运行结果和脱敏测试 |
| 投递视图核心补丁未匹配调试输出排版 | 1 | 整份补丁未应用；按当前源码将公开类型、运行时和模块拆开应用 |
| 重发接口测试改写留下多余结束符 | 1 | 格式化在提交前准确报错；按行号删除多余结束符后重跑格式和测试 |
| iOS 验收宿主未覆盖历史基础结果 | 1 | 联合编译命中穷尽匹配缺口，补不含用户内容的稳定摘要并通过 4 项测试 |
| 应用层全包严格检查命中 58 处既有告警 | 1-2 | 核心/webserver/daemon 分别严格通过；应用层仅放行输出中已有的文档排版、参数数量、锁 unwrap/expect 等类别后通过，其他告警仍保持失败 |
| 接收操作首个实现补丁未匹配运行时导入上下文 | 1 | 整份补丁未应用，读取实际排版后拆为公开契约、统一实现和运行时接入三块精确应用 |
| iOS 验收宿主未覆盖接收与取消结果 | 1 | 联合编译命中穷尽匹配缺口，补不含文件内容和路径的稳定摘要后通过 |
| iOS 验收宿主未覆盖捕获当前剪贴板结果 | 1 | 联合编译命中穷尽匹配缺口，补可选记录编号的稳定摘要后通过 |
| 宿主文件剪贴板首次实现无法推断临时目录类型 | 1 | 明确标注可选路径类型后重新编译，文件导入行为测试转绿 |
| 撤回关闭清理以取得失败测试时补丁未匹配字段顺序 | 1 | 确认整份补丁未应用，按当前源码位置拆分后取得准确失败断言 |
| 桌面宿主准备严格检查命中 2 处既有终端输出与可证明非空分支 | 1 | 与核心已登记四类旧告警一并精确放行，其他告警保持失败并通过 |
| 格式命令误触三处无关旧文件 | 1 | 按基线逐处恢复，后续只用 `rustfmt` 指定本次文件并以差异检查确认范围 |
| 宿主通知首个失败测试引用了未声明的随机编号依赖 | 1 | 改用隔离测试目录内的固定探针，重跑后红灯只剩通知接口缺失 |
| 通知测试把单消费者流直接放入线程共享宿主 | 1 | 用互斥容器保存并一次取出，不把通知接口错误扩大成多消费者约束 |
| 历史维护模块首次编译从 facade 顶层导入未统一导出的结果类型 | 1 | 改从公开的 `facade::clipboard_history` 实际所属模块导入后重跑 |
| 历史维护启动顺序门禁首次匹配到导入语句而非调用点 | 1 | 将匹配收窄到带真实参数的调用，避免测试误判源码顺序 |
| 保活迁移首个大补丁未匹配核心运行时导入位置 | 1 | 整份补丁未应用；拆成新增模块与精确接入两步后成功 |
| 新增 peer 连接结果后 iOS 验收宿主匹配不完整 | 1 | 补只含数量与连接方式的脱敏摘要和测试后联合编译通过 |
| peer 连接切片联合严格检查命中 iOS 验收宿主两处既有安全说明缺失 | 1 | 不改无关 FFI；仅对该包放行已知类别，其余告警继续失败 |
| 设置边界首次编译发现新内部模块有多余导入 | 1 | 删除生产未使用导入，测试改用明确路径后无告警 |
| 新增设置结果后 iOS 验收宿主匹配不完整 | 1 | 补只含版本、开关、数量和结果类别的脱敏摘要后通过 |
| 设置切片完整测试通过后严格检查发现两个公开枚举体积过大 | 1 | 将大载荷改为间接承载，保持外部序列化形状不变后重新验证 |
| 首轮体积修正后严格检查继续发现总结果直接承载完整设置快照 | 2 | 将设置快照结果也改为间接承载，覆盖输入、成功更新和查询三处大载荷 |
| 升级稳定核心契约首次编译缺少动作、状态和确认结果 | 1 | 预期红灯；新增由核心启动版本驱动的唯一升级查询与确认实现 |
| 升级映射单元测试直接构造版本对象但核心未直接依赖版本库 | 1 | 测试改为覆盖无需新依赖的终态映射，版本升降字段由公开契约固定 |
| 新增升级结果后 iOS 验收宿主匹配不完整 | 1 | 补只含升级类别和公开版本号的稳定输出 |
| 诊断稳定核心契约首次编译缺少状态、更新和句柄导出动作 | 1 | 预期红灯；新增核心诊断动作和可复用的临时文件到宿主句柄写出路径 |
| 诊断实现首个整块补丁未匹配内部模块清单实际顺序 | 1 | 补丁完整拒绝；按真实排版拆成模块文件和接线两步应用 |
| 新增诊断结果后 iOS 验收宿主匹配不完整 | 1 | 补只含开关、配置类别、文件数量和时间的脱敏输出 |
| 配置迁移稳定核心契约首次编译缺少三种句柄动作和结果 | 1 | 预期红灯；新增双向句柄复制、随机临时目录和结构化业务结果 |
| 新增配置迁移结果后 iOS 验收宿主匹配不完整 | 1 | 补结果类别和身份字段存在性摘要，不输出身份值与不兼容原因 |
| 真实配置预览未满足硬编码版本和存储模式断言 | 1 | 发现包清单版本未使用 `EngineConfig`；改为核心版本注入，存储模式按真实宿主环境验收 |
| 配置迁移切片的 `uc-infra` 严格检查命中六处既有告警 | 1 | 不修改无关模块；仅放行已确认的 owned-cow、文档缩进和缺省实现三类旧告警后复查本次文件 |
| 完整核心套件连续两次在剪贴板通知测试超时 | 1-2 | 建立同进程双实例最小复现，确认旧实例的进程级自写归因记录污染新实例；改为每个 `Engine` 独立拥有后回归转绿 |
| 上传生命周期行为测试首次编译失败 | 1 | 准确命中完成操作仍读取已迁到开始输入的来源设备与传输编号；先完成最小状态接线，再继续取得缺少事件行为的运行期红灯 |
| 上传生命周期所有权门禁按预期失败 | 1 | 准确命中核心运行时没有调用进度动作；开始补唯一核心生命周期编排，HTTP 路由不恢复旧调用 |
| 上传告警清理补丁未匹配历史表单构造排版 | 1 | 整份补丁被拒绝且未留下修改；读取真实构造与日志位置后先补隐私门禁，再按当前内容修改 |
| 核心完整套件被 daemon 最终切换门禁阻断 | 1 | 70 项核心单元测试通过，本轮上传行为与所有权测试通过；29 项失败均是工作区已预置的下一阶段切换要求，先跑完其余核心套件，再进入 daemon 切换使门禁转绿 |
| daemon 联合编译命中旧装配接线 | 1 | 4 个错误均来自 `DaemonApp` 仍向新网页状态传 `AppFacade`、缺文件句柄/LAN 接口，以及移动 LAN spawner 仍传旧 facade；按最终单 `Engine` 接线逐项替换 |
| 桌面共享文件句柄契约按预期编译失败 | 1 | 准确缺少 `DesktopHostFileHandles` 公共宿主接口；开始实现核心与网页共用的不透明输入/输出登记器 |
| 共享文件句柄首次实现缺少标准文件类型导入 | 1 | 编译仅命中 `OpenOptions` 未导入；补标准库导入后重跑同一真实读写测试 |
| 共享文件句柄第二次编译缺少写入 trait | 2 | `File::write_all` 需要标准 `Write` trait；补精确导入，不改变实现或测试 |
| `uc-bootstrap` 严格检查命中既有告警 | 1-2 | 全目标还命中多份旧端到端测试写法；收窄到正式库并只放行旧观测代码三类已知告警后通过 |
| `planning-with-files` 恢复脚本在本轮两个预期位置均不存在 | 13 | 已核对技能目录确认安装包未包含脚本；改用三份规划文件、Git 历史与工作区状态恢复，未继续重复调用 |
| 原始入站事件整块补丁未匹配运行时字段排版 | 1 | 补丁完整拒绝且未留下半成品；按公开类型、入站转换、运行时接线和 iOS 映射四块重新应用后转绿 |
| 新增 `dirs` 直接依赖后锁文件检查失败 | 1-2 | 首次 `--locked` 正确拒绝元数据变化；离线检查发现版本 5 未缓存，改为仓库统一的版本 6 后离线更新并恢复 `--locked` 通过 |
| 网卡与接收状态运行时补丁未匹配现有调用排版 | 1 | 补丁完整拒绝；读取真实匹配分支后拆成精确接入，公开契约与网页边界均转绿 |
| 移动监听设置事件整块补丁缺少预期导入上下文 | 1 | 补丁完整拒绝；按目标计算、事件转发和宿主顺序三个文件分别接入后通过 |
| `planning-with-files` 仓库恢复脚本仍不存在 | 14 | 已读取技能说明并确认缺失；直接以三份规划文件、Git 状态和失败测试恢复现场，不再重复调用 |
| 本轮规划记录补丁未匹配 `progress.md` 末行 | 1 | 补丁整体被拒绝且无半成品；按三个文件的真实末尾拆分重新应用 |
| 全仓格式检查命中既有无关差异 | 3 | 差异全部位于本轮未改的 `uc-infra` 与 `uc-webserver` 文件；改为对本次仍存在的改动文件单独执行格式检查 |
| 不放行告警的 `uc-engine` 静态检查命中既有告警 | 1 | 仅命中已登记的参数数量、类型复杂度、路径参数与旧测试复制写法；将按精确类别收窄重跑，其他告警仍保持失败 |
| `uc-engine` 收窄静态检查继续命中旧测试 `unwrap` | 2 | 9 处均位于现有宿主测试辅助代码，本轮无新增；补充放行该测试类别后重跑 |
| `uc-webserver` 零放行静态检查命中既有告警 | 1 | 命中 5 处旧 `expect` 和三类旧测试写法，均不在本次改动文件；仅按这些精确类别收窄重跑 |
| `uc-webserver` 收窄静态检查继续命中生成工具和旧测试告警 | 2 | 只剩生成工具报告输出路径的 2 处打印和旧设置测试 2 处布尔写法；补这两个精确类别后最后重跑 |
| 冒烟配置清理的强制删除参数被环境拒绝 | 1 | 未执行强制删除；改用仅针对已知测试目录的普通递归删除，并单独核对钥匙串项 |
| `planning-with-files` 恢复脚本在本次续作中仍不存在 | 15 | 已确认技能目录未提供脚本；改用三份规划文件、Git 状态和最新提交恢复，继续处理 Plan 003 最后一处已知旧入口 |
| 本次规划记录补丁未匹配 `progress.md` 末行 | 1 | 整份补丁被拒绝且未留下修改；读取三个文件真实末尾后按稳定位置重新应用 |
| 首次运行 CLI 旧入口边界测试无法创建 `target` | 1 | 仓库 `target` 是指向未挂载外置硬盘的断链；不改用户符号链接，后续 Cargo 命令统一使用 `/tmp/uni-desktop-target` |
| CLI 旧入口整块迁移补丁未匹配 `non_gui.rs` | 1 | 整份补丁被拒绝且未留下修改；按核心诊断接口、支撑层和命令行三组拆开应用并逐组编译 |
| 核心开发诊断接口首次编译缺少 `bytes` 直接依赖 | 1 | 改用现有 `tokio-util` 对同一 `Bytes` 类型的公开导出，不为单点映射新增依赖 |
| CLI 改为直接依赖统一核心后锁定检查拒绝更新 | 1 | 依赖版本未变化；先用离线模式更新 `Cargo.lock` 的包依赖元数据，再恢复 `--locked` 验证 |
| CLI 锁文件离线更新缺少 `anstream` 缓存 | 1 | 离线命令未改锁文件；改用正常检查下载既有锁定版本并更新依赖元数据，随后恢复锁定模式 |
| 首次差异文件格式化包含两个已删除模块 | 1 | 命令只报文件不存在；改用 `--diff-filter=d` 排除删除项后重跑，未恢复或改写已删除文件 |
| 三包严格静态检查被 `uc-app-paths` 既有文档缩进阻断 | 1 | 未改依赖包；改用 `--no-deps` 只检查本轮三个改动包，并继续对本包执行 `-D warnings` |
| 收窄静态检查命中核心四类既有告警 | 1 | 仅放行已登记的类型复杂度、参数数量、路径参数和测试复制写法，其他告警继续视为失败 |
| 第二轮静态检查命中旧测试与观测代码告警 | 2 | 再放行既有的测试 `unwrap/expect`、观测终端输出和旧测试映射写法，继续观察是否出现新类别 |
| 第三轮静态检查命中 CLI 未改文件四类旧告警 | 3 | 仅放行文档空行、冗余闭包、可合并分支和多余向量，完成最后一轮严格检查 |
| iOS 分发库通过类型检查但真实应用链接失败 | 1 | 定位到完成静态库被整体处理后破坏加密汇编重定位；新增边界检查并改为保留汇编目标、选择性处理普通目标后应用链接通过 |
| 选择性处理后的三架构 iOS 包仍约 277 MB | 1 | 正式脚本未固定依赖的构建期调试信息开关；新增边界检查并在交叉编译前将发布调试信息设为关闭 |
| `planning-with-files` 恢复脚本在本次续作中仍不存在 | 16 | 已确认技能安装目录缺少脚本；继续使用三份规划文件、Git 状态和交接摘要恢复，不再重复调用 |
| `uc-webserver` 全包格式检查命中本轮未改文件的既有差异 | 4 | 不扩大本次修复范围；改为对实际修改的投影文件单独检查并通过 |
| HarmonyOS 首次交叉编译无法创建默认 `target` | 1 | 仓库默认目录是已知失效链接；改用 `/tmp/uni-ohos-target` 并继续显式指定 DevEco 编译器 |
| ARM64 OHOS 交叉编译依赖脚本找不到 `cmake` | 1 | 不安装全局工具；改为显式指定 DevEco 原生 SDK 自带的 `cmake` 后重试 |
| HarmonyOS HAP 首次构建缺少 `DEVECO_SDK_HOME` | 1 | HarmonyOS 模式不读取普通 SDK 路径作为替代；构建脚本显式传入 DevEco 自带 SDK，不改用户全局环境 |
| HarmonyOS 本地测试签名证书链无效 | 1-2 | 单证书缺链；密钥库应用条目又是自签名证书。最终改用 SDK 根证书、中间证书和模板中与私钥匹配的应用证书 |
| `planning-with-files` 恢复脚本在本次续作中仍不存在 | 17 | 已读取三份规划文件、Git 差异和交接记录恢复当前状态，不再依赖缺失脚本 |
| HarmonyOS 页面无法读取核心版本 | 1-5 | 先修正 OHOS 条件与主动登记，再确认最终缺口是动态库没有链接鸿蒙 N-API 系统运行库；补齐依赖后页面真实显示核心版本 |
| 模拟器截图首次使用 PNG 后缀被拒绝 | 1 | 工具明确只接受 JPEG；改用 JPEG 后截图成功并完成视觉检查 |

## Notes

- 物理 iPhone 16 Pro（`6F7C086F-2617-515D-9E7A-4D343B3A642A`）当前已配对且可用，优先完成 iOS 真机验收；当前没有 Android 设备。
- DevEco Studio、HarmonyOS SDK、`hdc` 和 Pura 90 模拟器已可用；实体设备验收仍按用户决定跳过。
- Plan 002、004、005 在缺少真实设备证据时不得标记完成。
- 实现 `SendFiles` 前必须先把根规则、产品文档、正式计划和调研记录中的“文件必须加密”改成与用户最新决定一致。
- 当前下一步：保持真机验收跳过，进入 Plan 004 的三种移动系统安全存储与文件出口接入；不得把验收应用的内存回调写成系统能力已完成。
